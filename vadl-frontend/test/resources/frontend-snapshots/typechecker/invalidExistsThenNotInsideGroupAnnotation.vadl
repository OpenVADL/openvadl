instruction set architecture ISA = {
  program counter PC : Bits<32>

  format AType : Bits<32> = {
    a : Bits<20>,
    b : Bits<5>,
    c : Bits<7>
  }

  format BType : Bits<32> = {
    a : Bits<20>,
    b : Bits<5>,
    d : Bits<7>
  }

  instruction A : AType = PC := 0
  encoding A = {a = 0b00}
  assembly A = (mnemonic, "")

  instruction B : BType = PC := 0

  [select when : exists i in {OpA, OpB} then i.a = 0b01]
  encoding B = {a = 0b01}
  assembly B = (mnemonic, "")

  operation OpA = {A}
  operation OpB = {B}

  [assert : exists i in {OpA, OpB} then i.a = 0b01]
  group VLIW = OpA.OpB
}


//  Reported Diagnostics:
//
//  error: Invalid `exists-then` expression
//       ╭── test/resources/frontend-snapshots/typechecker/invalidExistsThenNotInsideGroupAnnotation.vadl:22:18
//       │
//    22 │   [select when : exists i in {OpA, OpB} then i.a = 0b01]
//       │                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
//       │
//       The exists-then expression is only permissible for annotations on the `group` definition.
//
//
// Part of the class vadl.ast.FrontendSnapshotTests