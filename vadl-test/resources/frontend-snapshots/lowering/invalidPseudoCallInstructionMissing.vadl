instruction set architecture ISA = {
  register X : Bits<5> -> Bits<32>

  pseudo instruction NOP( symbol: Bits<5>) = {
  }
  assembly NOP = (mnemonic)
}



  application binary interface ABI for ISA = {
      special return instruction = NOP
  special call instruction = DOESNOTEXIST
  special local address load instruction = NOP
  special absolute address load instruction = NOP
  alias register zero = X(0)
  stack pointer = zero
  return address = zero
  global pointer = zero
  frame pointer = zero
  thread pointer = zero
  return value = zero
  function argument = zero
  caller saved = zero
  callee saved = zero

  }


//  Reported Diagnostics:
//
//  error: Unknown Symbol: "DOESNOTEXIST"
//       ╭──[resources/frontend-snapshots/lowering/invalidPseudoCallInstructionMissing.vadl:13:30]
//       │
//    13 │   special call instruction = DOESNOTEXIST
//       │                              ^^^^^^^^^^^^ No symbol with this name exists.
//       │
//
//
// Part of the class vadl.ast.FrontendSnapshotTests