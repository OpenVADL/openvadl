package vadl.viam.graph.nodes.dependency;

import vadl.viam.graph.nodes.DependencyNode;

/**
 * Represents a node with side effect. Such nodes are dependencies
 * of {@link vadl.viam.graph.nodes.control.AbstractEndNode}.
 */
public abstract class SideEffectNode extends DependencyNode {
}
