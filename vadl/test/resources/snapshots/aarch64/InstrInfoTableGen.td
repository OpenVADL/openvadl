/*
 * Node representing the frame index.
 * The method SelectAddrFI is used to determine
 * if the specific register is a frame pointer.
 */
def AddrFI : ComplexPattern<iPTR, 1, "SelectAddrFI", [frameindex], []>;

// symbol operand used for matching load and call sequences.
// the size is determined by the program counter size.
def bare_symbol : Operand<i64>;

def SDT_CallSeqStart : SDCallSeqStart<[SDTCisVT<0, i64>, SDTCisVT<1, i64>]>;
def SDT_CallSeqEnd   : SDCallSeqEnd<[SDTCisVT<0, i64>, SDTCisVT<1, i64>]>;

// Target-dependent type requirements
def SDT_CPU_Call : SDTypeProfile<0, -1, [SDTCisVT<0, i64>]>;

class InstFormat<bits<5> val> {
    bits<5> Value = val;
}

class GenericInstCommon<dag outs, dag ins, string opcodestr, string argstr,
           list<dag> pattern, InstFormat format> : Instruction {
    let Namespace = "processornamevalue";

    dag OutOperandList = outs;
    dag InOperandList = ins;
    let AsmString = opcodestr # !if(!empty(argstr), "", "\t" # argstr);
    let Pattern = pattern;
    let TSFlags{4-0} = format.Value;
}

class GenericInst<dag outs, dag ins, string opcodestr, string argstr,
     list<dag> pattern, InstFormat format>
    : GenericInstCommon<outs, ins, opcodestr, argstr, pattern, format> {
    field bits<32> Inst;
    // SoftFail is a field the disassembler can use to provide a way for
    // instructions to not match without killing the whole decode process. It is
    // mainly used for ARM, but Tablegen expects this field to exist or it fails
    // to build the decode table.
    field bits<32> SoftFail = 0;
    let Size = 4;
}

class Pseudo<dag outs, dag ins, list<dag> pattern, string opcodestr = "", string argstr = "">
    : GenericInst<outs, ins, opcodestr, argstr, pattern, InstFormat<0>> {
    let isPseudo = 1;
    let isCodeGenOnly = 1;
}

// Target-independent nodes, but with target-specific formats
def callseq_start : SDNode<"ISD::CALLSEQ_START", SDT_CallSeqStart, [SDNPHasChain, SDNPOutGlue]>;
def callseq_end   : SDNode<"ISD::CALLSEQ_END", SDT_CallSeqEnd, [SDNPHasChain, SDNPOptInGlue, SDNPOutGlue]>;

def target_call : SDNode<"processornamevalueISD::CALL", SDT_CPU_Call,
                           [SDNPHasChain, SDNPOptInGlue, SDNPOutGlue, SDNPVariadic]>;

def target_ret_flag : SDNode<"processornamevalueISD::RET_FLAG", SDTNone,
                           [SDNPHasChain, SDNPOptInGlue, SDNPVariadic]>;

/*
 * ADJCALLSTACKDOWN is a pseudo instruction used to represent the
 * 'CFSetupOpcode', which is needed for the call frame setup
 */
def ADJCALLSTACKDOWN : Instruction
{
    let InOperandList = (ins i64imm:$amt1, i64imm:$amt2); /* i32imm : Operand<i32> */
    let OutOperandList = (outs);
    let Pattern = [ (callseq_start timm:$amt1, timm:$amt2) ];
    let Namespace = "processornamevalue";
    let isPseudo = 1;
    let isCodeGenOnly = 1;
    let Defs = [ S31 ]; // stack pointer
    let Uses = [ S31 ]; // stack pointer
}

/*
 * ADJCALLSTACKUP is a pseudo instruction used to represent the
 * 'CFDestroyOpcode', which is needed for the call frame setup
 */
def ADJCALLSTACKUP : Instruction
{
    let InOperandList = (ins i64imm:$amt1, i64imm:$amt2);
    let OutOperandList = (outs);
    let Pattern = [ (callseq_end timm:$amt1, timm:$amt2) ];
    let Namespace = "processornamevalue";
    let isPseudo = 1;
    let isCodeGenOnly = 1;
    let Defs = [ S31 ]; // stack pointer
    let Uses = [ S31 ]; // stack pointer
}


def TEMP_RET : Instruction
{
    let Namespace = "processornamevalue";
    let InOperandList = (ins);
    let OutOperandList = (outs);
    let Pattern =  [ (target_ret_flag) ];
    let isTerminator  = 1;
    let isBranch      = 0;
    let isCall        = 0;
    let isReturn      = 1;
    let isPseudo      = 1;
    let isCodeGenOnly = 1;
    let mayLoad       = 0;
    let mayStore      = 0;
    let Defs = [];
    let Uses = [];
}




def SDT_processornamevalueSelectCC     : SDTypeProfile<1, 5, [SDTCisSameAs<1, 2>,
                                         SDTCisSameAs<0, 4>,
                                         SDTCisSameAs<4, 5>]>;


def target_selectcc_S : SDNode<"processornamevalueISD::SELECT_CC_S", SDT_processornamevalueSelectCC,
                     [SDNPInGlue]>;
def target_selectcc_R : SDNode<"processornamevalueISD::SELECT_CC_R", SDT_processornamevalueSelectCC,
                     [SDNPInGlue]>;
def target_selectcc_X : SDNode<"processornamevalueISD::SELECT_CC_X", SDT_processornamevalueSelectCC,
                     [SDNPInGlue]>;
def target_selectcc_W : SDNode<"processornamevalueISD::SELECT_CC_W", SDT_processornamevalueSelectCC,
                     [SDNPInGlue]>;


def vadl_imm32 : ImmLeaf<i32, [{ return true; }]>, Operand<i32>
{

}

def vadl_imm64 : ImmLeaf<i64, [{ return true; }]>, Operand<i64>
{

}

def vadl_lga : SDNode<"processornamevalueISD::LGA", SDTLoad, [SDNPHasChain, SDNPMayLoad, SDNPMemOperand]>;




def SelectCC_S: Instruction {
    field bits<64> Inst;
    // SoftFail is a field the disassembler can use to provide a way for
    // instructions to not match without killing the whole decode process. It is
    // mainly used for ARM, but Tablegen expects this field to exist or it fails
    // to build the decode table.
    field bits<32> SoftFail = 0;

    bits<7> Opcode = 0;

    let Inst{6-0} = Opcode;

    let Namespace = "processornamevalue";

    dag OutOperandList = (outs S:$dst);
    dag InOperandList = (ins S:$lhs, S:$rhs, vadl_imm64:$imm, S:$truev, S:$falsev);
    let Pattern = [ (set S:$dst, (target_selectcc_S S:$lhs, S:$rhs,
                (i64 vadl_imm64:$imm), S:$truev, S:$falsev)) ];

    let TSFlags{4-0} = 0;
    let usesCustomInserter = 1;
    let isPseudo = 1;
    let isCodeGenOnly = 1;
}


def SelectCC_R: Instruction {
    field bits<32> Inst;
    // SoftFail is a field the disassembler can use to provide a way for
    // instructions to not match without killing the whole decode process. It is
    // mainly used for ARM, but Tablegen expects this field to exist or it fails
    // to build the decode table.
    field bits<32> SoftFail = 0;

    bits<7> Opcode = 0;

    let Inst{6-0} = Opcode;

    let Namespace = "processornamevalue";

    dag OutOperandList = (outs R:$dst);
    dag InOperandList = (ins R:$lhs, R:$rhs, vadl_imm32:$imm, R:$truev, R:$falsev);
    let Pattern = [ (set R:$dst, (target_selectcc_R R:$lhs, R:$rhs,
                (i32 vadl_imm32:$imm), R:$truev, R:$falsev)) ];

    let TSFlags{4-0} = 0;
    let usesCustomInserter = 1;
    let isPseudo = 1;
    let isCodeGenOnly = 1;
}




def SelectCC_X: Instruction {
    field bits<64> Inst;
    // SoftFail is a field the disassembler can use to provide a way for
    // instructions to not match without killing the whole decode process. It is
    // mainly used for ARM, but Tablegen expects this field to exist or it fails
    // to build the decode table.
    field bits<32> SoftFail = 0;

    bits<7> Opcode = 0;

    let Inst{6-0} = Opcode;

    let Namespace = "processornamevalue";

    dag OutOperandList = (outs X:$dst);
    dag InOperandList = (ins X:$lhs, X:$rhs, vadl_imm64:$imm, X:$truev, X:$falsev);
    let Pattern = [ (set X:$dst, (target_selectcc_X X:$lhs, X:$rhs,
                (i64 vadl_imm64:$imm), X:$truev, X:$falsev)) ];

    let TSFlags{4-0} = 0;
    let usesCustomInserter = 1;
    let isPseudo = 1;
    let isCodeGenOnly = 1;
}


def SelectCC_W: Instruction {
    field bits<32> Inst;
    // SoftFail is a field the disassembler can use to provide a way for
    // instructions to not match without killing the whole decode process. It is
    // mainly used for ARM, but Tablegen expects this field to exist or it fails
    // to build the decode table.
    field bits<32> SoftFail = 0;

    bits<7> Opcode = 0;

    let Inst{6-0} = Opcode;

    let Namespace = "processornamevalue";

    dag OutOperandList = (outs W:$dst);
    dag InOperandList = (ins W:$lhs, W:$rhs, vadl_imm32:$imm, W:$truev, W:$falsev);
    let Pattern = [ (set W:$dst, (target_selectcc_W W:$lhs, W:$rhs,
                (i32 vadl_imm32:$imm), W:$truev, W:$falsev)) ];

    let TSFlags{4-0} = 0;
    let usesCustomInserter = 1;
    let isPseudo = 1;
    let isCodeGenOnly = 1;
}





class AArch64Base_ADDWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWASR_wrapper";
  let DecoderMethod = "AArch64Base_ADDWASR_imm6_decode_wrapper";
}

def AArch64Base_ADDWASR_imm6AsInt32
    : AArch64Base_ADDWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWASR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDWASR_imm6AsLabel : AArch64Base_ADDWASR_imm6<OtherVT>;

class AArch64Base_ADDWI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWI_wrapper";
  let DecoderMethod = "AArch64Base_ADDWI_imm12X_decode_wrapper";
}

def AArch64Base_ADDWI_imm12XAsInt64
    : AArch64Base_ADDWI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDWI_imm12X_predicate(Imm); }]>;

def AArch64Base_ADDWI_imm12XAsLabel : AArch64Base_ADDWI_imm12X<OtherVT>;

class AArch64Base_ADDWI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWI12_wrapper";
  let DecoderMethod = "AArch64Base_ADDWI12_imm12S_decode_wrapper";
}

def AArch64Base_ADDWI12_imm12SAsInt64
    : AArch64Base_ADDWI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDWI12_imm12S_predicate(Imm); }]>;

def AArch64Base_ADDWI12_imm12SAsLabel : AArch64Base_ADDWI12_imm12S<OtherVT>;

class AArch64Base_ADDWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWLSL_wrapper";
  let DecoderMethod = "AArch64Base_ADDWLSL_imm6_decode_wrapper";
}

def AArch64Base_ADDWLSL_imm6AsInt32
    : AArch64Base_ADDWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ADDWLSL_imm6AsLabel : AArch64Base_ADDWLSL_imm6<OtherVT>;

class AArch64Base_ADDWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWLSR_wrapper";
  let DecoderMethod = "AArch64Base_ADDWLSR_imm6_decode_wrapper";
}

def AArch64Base_ADDWLSR_imm6AsInt32
    : AArch64Base_ADDWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDWLSR_imm6AsLabel : AArch64Base_ADDWLSR_imm6<OtherVT>;

class AArch64Base_ADDWSASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSASR_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSASR_imm6_decode_wrapper";
}

def AArch64Base_ADDWSASR_imm6AsInt32
    : AArch64Base_ADDWSASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSASR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDWSASR_imm6AsLabel : AArch64Base_ADDWSASR_imm6<OtherVT>;

class AArch64Base_ADDWSI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSI_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSI_imm12X_decode_wrapper";
}

def AArch64Base_ADDWSI_imm12XAsInt64
    : AArch64Base_ADDWSI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDWSI_imm12X_predicate(Imm); }]>;

def AArch64Base_ADDWSI_imm12XAsLabel : AArch64Base_ADDWSI_imm12X<OtherVT>;

class AArch64Base_ADDWSI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSI12_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSI12_imm12S_decode_wrapper";
}

def AArch64Base_ADDWSI12_imm12SAsInt64
    : AArch64Base_ADDWSI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDWSI12_imm12S_predicate(Imm); }]>;

def AArch64Base_ADDWSI12_imm12SAsLabel : AArch64Base_ADDWSI12_imm12S<OtherVT>;

class AArch64Base_ADDWSLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSLSL_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSLSL_imm6_decode_wrapper";
}

def AArch64Base_ADDWSLSL_imm6AsInt32
    : AArch64Base_ADDWSLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ADDWSLSL_imm6AsLabel : AArch64Base_ADDWSLSL_imm6<OtherVT>;

class AArch64Base_ADDWSLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSLSR_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSLSR_imm6_decode_wrapper";
}

def AArch64Base_ADDWSLSR_imm6AsInt32
    : AArch64Base_ADDWSLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDWSLSR_imm6AsLabel : AArch64Base_ADDWSLSR_imm6<OtherVT>;

class AArch64Base_ADDWSSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSSXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSSXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDWSSXSB_imm3AsInt32
    : AArch64Base_ADDWSSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSSXSB_imm3AsLabel : AArch64Base_ADDWSSXSB_imm3<OtherVT>;

class AArch64Base_ADDWSSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSSXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSSXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDWSSXSH_imm3AsInt32
    : AArch64Base_ADDWSSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSSXSH_imm3AsLabel : AArch64Base_ADDWSSXSH_imm3<OtherVT>;

class AArch64Base_ADDWSSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSSXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSSXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDWSSXSW_imm3AsInt32
    : AArch64Base_ADDWSSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSSXSW_imm3AsLabel : AArch64Base_ADDWSSXSW_imm3<OtherVT>;

class AArch64Base_ADDWSSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSSXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSSXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDWSSXSX_imm3AsInt32
    : AArch64Base_ADDWSSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSSXSX_imm3AsLabel : AArch64Base_ADDWSSXSX_imm3<OtherVT>;

class AArch64Base_ADDWSUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSUXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSUXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDWSUXSB_imm3AsInt32
    : AArch64Base_ADDWSUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSUXSB_imm3AsLabel : AArch64Base_ADDWSUXSB_imm3<OtherVT>;

class AArch64Base_ADDWSUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSUXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSUXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDWSUXSH_imm3AsInt32
    : AArch64Base_ADDWSUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSUXSH_imm3AsLabel : AArch64Base_ADDWSUXSH_imm3<OtherVT>;

class AArch64Base_ADDWSUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSUXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSUXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDWSUXSW_imm3AsInt32
    : AArch64Base_ADDWSUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSUXSW_imm3AsLabel : AArch64Base_ADDWSUXSW_imm3<OtherVT>;

class AArch64Base_ADDWSUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSUXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSUXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDWSUXSX_imm3AsInt32
    : AArch64Base_ADDWSUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSUXSX_imm3AsLabel : AArch64Base_ADDWSUXSX_imm3<OtherVT>;

class AArch64Base_ADDWSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDWSXSB_imm3AsInt32
    : AArch64Base_ADDWSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSXSB_imm3AsLabel : AArch64Base_ADDWSXSB_imm3<OtherVT>;

class AArch64Base_ADDWSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDWSXSH_imm3AsInt32
    : AArch64Base_ADDWSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSXSH_imm3AsLabel : AArch64Base_ADDWSXSH_imm3<OtherVT>;

class AArch64Base_ADDWSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDWSXSW_imm3AsInt32
    : AArch64Base_ADDWSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSXSW_imm3AsLabel : AArch64Base_ADDWSXSW_imm3<OtherVT>;

class AArch64Base_ADDWSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWSXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDWSXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDWSXSX_imm3AsInt32
    : AArch64Base_ADDWSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWSXSX_imm3AsLabel : AArch64Base_ADDWSXSX_imm3<OtherVT>;

class AArch64Base_ADDWUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWUXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDWUXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDWUXSB_imm3AsInt32
    : AArch64Base_ADDWUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWUXSB_imm3AsLabel : AArch64Base_ADDWUXSB_imm3<OtherVT>;

class AArch64Base_ADDWUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWUXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDWUXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDWUXSH_imm3AsInt32
    : AArch64Base_ADDWUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWUXSH_imm3AsLabel : AArch64Base_ADDWUXSH_imm3<OtherVT>;

class AArch64Base_ADDWUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWUXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDWUXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDWUXSW_imm3AsInt32
    : AArch64Base_ADDWUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWUXSW_imm3AsLabel : AArch64Base_ADDWUXSW_imm3<OtherVT>;

class AArch64Base_ADDWUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDWUXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDWUXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDWUXSX_imm3AsInt32
    : AArch64Base_ADDWUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDWUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDWUXSX_imm3AsLabel : AArch64Base_ADDWUXSX_imm3<OtherVT>;

class AArch64Base_ADDXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXASR_wrapper";
  let DecoderMethod = "AArch64Base_ADDXASR_imm6_decode_wrapper";
}

def AArch64Base_ADDXASR_imm6AsInt32
    : AArch64Base_ADDXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXASR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDXASR_imm6AsLabel : AArch64Base_ADDXASR_imm6<OtherVT>;

class AArch64Base_ADDXI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXI_wrapper";
  let DecoderMethod = "AArch64Base_ADDXI_imm12X_decode_wrapper";
}

def AArch64Base_ADDXI_imm12XAsInt64
    : AArch64Base_ADDXI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDXI_imm12X_predicate(Imm); }]>;

def AArch64Base_ADDXI_imm12XAsLabel : AArch64Base_ADDXI_imm12X<OtherVT>;

class AArch64Base_ADDXI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXI12_wrapper";
  let DecoderMethod = "AArch64Base_ADDXI12_imm12S_decode_wrapper";
}

def AArch64Base_ADDXI12_imm12SAsInt64
    : AArch64Base_ADDXI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDXI12_imm12S_predicate(Imm); }]>;

def AArch64Base_ADDXI12_imm12SAsLabel : AArch64Base_ADDXI12_imm12S<OtherVT>;

class AArch64Base_ADDXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXLSL_wrapper";
  let DecoderMethod = "AArch64Base_ADDXLSL_imm6_decode_wrapper";
}

def AArch64Base_ADDXLSL_imm6AsInt32
    : AArch64Base_ADDXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ADDXLSL_imm6AsLabel : AArch64Base_ADDXLSL_imm6<OtherVT>;

class AArch64Base_ADDXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXLSR_wrapper";
  let DecoderMethod = "AArch64Base_ADDXLSR_imm6_decode_wrapper";
}

def AArch64Base_ADDXLSR_imm6AsInt32
    : AArch64Base_ADDXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDXLSR_imm6AsLabel : AArch64Base_ADDXLSR_imm6<OtherVT>;

class AArch64Base_ADDXSASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSASR_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSASR_imm6_decode_wrapper";
}

def AArch64Base_ADDXSASR_imm6AsInt32
    : AArch64Base_ADDXSASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSASR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDXSASR_imm6AsLabel : AArch64Base_ADDXSASR_imm6<OtherVT>;

class AArch64Base_ADDXSI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSI_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSI_imm12X_decode_wrapper";
}

def AArch64Base_ADDXSI_imm12XAsInt64
    : AArch64Base_ADDXSI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDXSI_imm12X_predicate(Imm); }]>;

def AArch64Base_ADDXSI_imm12XAsLabel : AArch64Base_ADDXSI_imm12X<OtherVT>;

class AArch64Base_ADDXSI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSI12_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSI12_imm12S_decode_wrapper";
}

def AArch64Base_ADDXSI12_imm12SAsInt64
    : AArch64Base_ADDXSI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADDXSI12_imm12S_predicate(Imm); }]>;

def AArch64Base_ADDXSI12_imm12SAsLabel : AArch64Base_ADDXSI12_imm12S<OtherVT>;

class AArch64Base_ADDXSLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSLSL_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSLSL_imm6_decode_wrapper";
}

def AArch64Base_ADDXSLSL_imm6AsInt32
    : AArch64Base_ADDXSLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ADDXSLSL_imm6AsLabel : AArch64Base_ADDXSLSL_imm6<OtherVT>;

class AArch64Base_ADDXSLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSLSR_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSLSR_imm6_decode_wrapper";
}

def AArch64Base_ADDXSLSR_imm6AsInt32
    : AArch64Base_ADDXSLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ADDXSLSR_imm6AsLabel : AArch64Base_ADDXSLSR_imm6<OtherVT>;

class AArch64Base_ADDXSSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSSXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSSXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDXSSXSB_imm3AsInt32
    : AArch64Base_ADDXSSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSSXSB_imm3AsLabel : AArch64Base_ADDXSSXSB_imm3<OtherVT>;

class AArch64Base_ADDXSSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSSXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSSXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDXSSXSH_imm3AsInt32
    : AArch64Base_ADDXSSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSSXSH_imm3AsLabel : AArch64Base_ADDXSSXSH_imm3<OtherVT>;

class AArch64Base_ADDXSSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSSXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSSXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDXSSXSW_imm3AsInt32
    : AArch64Base_ADDXSSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSSXSW_imm3AsLabel : AArch64Base_ADDXSSXSW_imm3<OtherVT>;

class AArch64Base_ADDXSSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSSXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSSXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDXSSXSX_imm3AsInt32
    : AArch64Base_ADDXSSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSSXSX_imm3AsLabel : AArch64Base_ADDXSSXSX_imm3<OtherVT>;

class AArch64Base_ADDXSUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSUXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSUXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDXSUXSB_imm3AsInt32
    : AArch64Base_ADDXSUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSUXSB_imm3AsLabel : AArch64Base_ADDXSUXSB_imm3<OtherVT>;

class AArch64Base_ADDXSUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSUXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSUXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDXSUXSH_imm3AsInt32
    : AArch64Base_ADDXSUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSUXSH_imm3AsLabel : AArch64Base_ADDXSUXSH_imm3<OtherVT>;

class AArch64Base_ADDXSUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSUXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSUXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDXSUXSW_imm3AsInt32
    : AArch64Base_ADDXSUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSUXSW_imm3AsLabel : AArch64Base_ADDXSUXSW_imm3<OtherVT>;

class AArch64Base_ADDXSUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSUXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSUXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDXSUXSX_imm3AsInt32
    : AArch64Base_ADDXSUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSUXSX_imm3AsLabel : AArch64Base_ADDXSUXSX_imm3<OtherVT>;

class AArch64Base_ADDXSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDXSXSB_imm3AsInt32
    : AArch64Base_ADDXSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSXSB_imm3AsLabel : AArch64Base_ADDXSXSB_imm3<OtherVT>;

class AArch64Base_ADDXSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDXSXSH_imm3AsInt32
    : AArch64Base_ADDXSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSXSH_imm3AsLabel : AArch64Base_ADDXSXSH_imm3<OtherVT>;

class AArch64Base_ADDXSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDXSXSW_imm3AsInt32
    : AArch64Base_ADDXSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSXSW_imm3AsLabel : AArch64Base_ADDXSXSW_imm3<OtherVT>;

class AArch64Base_ADDXSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXSXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDXSXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDXSXSX_imm3AsInt32
    : AArch64Base_ADDXSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXSXSX_imm3AsLabel : AArch64Base_ADDXSXSX_imm3<OtherVT>;

class AArch64Base_ADDXUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXUXSB_wrapper";
  let DecoderMethod = "AArch64Base_ADDXUXSB_imm3_decode_wrapper";
}

def AArch64Base_ADDXUXSB_imm3AsInt32
    : AArch64Base_ADDXUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXUXSB_imm3AsLabel : AArch64Base_ADDXUXSB_imm3<OtherVT>;

class AArch64Base_ADDXUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXUXSH_wrapper";
  let DecoderMethod = "AArch64Base_ADDXUXSH_imm3_decode_wrapper";
}

def AArch64Base_ADDXUXSH_imm3AsInt32
    : AArch64Base_ADDXUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXUXSH_imm3AsLabel : AArch64Base_ADDXUXSH_imm3<OtherVT>;

class AArch64Base_ADDXUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXUXSW_wrapper";
  let DecoderMethod = "AArch64Base_ADDXUXSW_imm3_decode_wrapper";
}

def AArch64Base_ADDXUXSW_imm3AsInt32
    : AArch64Base_ADDXUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXUXSW_imm3AsLabel : AArch64Base_ADDXUXSW_imm3<OtherVT>;

class AArch64Base_ADDXUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADDXUXSX_wrapper";
  let DecoderMethod = "AArch64Base_ADDXUXSX_imm3_decode_wrapper";
}

def AArch64Base_ADDXUXSX_imm3AsInt32
    : AArch64Base_ADDXUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ADDXUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_ADDXUXSX_imm3AsLabel : AArch64Base_ADDXUXSX_imm3<OtherVT>;

class AArch64Base_ADR_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADR_wrapper";
  let DecoderMethod = "AArch64Base_ADR_offset_decode_wrapper";
}

def AArch64Base_ADR_offsetAsInt64
    : AArch64Base_ADR_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADR_offset_predicate(Imm); }]>;

def AArch64Base_ADR_offsetAsLabel : AArch64Base_ADR_offset<OtherVT>;

class AArch64Base_ADRP_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ADRP_wrapper";
  let DecoderMethod = "AArch64Base_ADRP_offset_decode_wrapper";
}

def AArch64Base_ADRP_offsetAsInt64
    : AArch64Base_ADRP_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ADRP_offset_predicate(Imm); }]>;

def AArch64Base_ADRP_offsetAsLabel : AArch64Base_ADRP_offset<OtherVT>;

class AArch64Base_ANDIW_immWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDIW_wrapper";
  let DecoderMethod = "AArch64Base_ANDIW_immWSize_decode_wrapper";
}

def AArch64Base_ANDIW_immWSizeAsInt32
    : AArch64Base_ANDIW_immWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDIW_immWSize_predicate(Imm); }]>;

def AArch64Base_ANDIW_immWSizeAsLabel : AArch64Base_ANDIW_immWSize<OtherVT>;

class AArch64Base_ANDIX_immXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDIX_wrapper";
  let DecoderMethod = "AArch64Base_ANDIX_immXSize_decode_wrapper";
}

def AArch64Base_ANDIX_immXSizeAsInt64
    : AArch64Base_ANDIX_immXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ANDIX_immXSize_predicate(Imm); }]>;

def AArch64Base_ANDIX_immXSizeAsLabel : AArch64Base_ANDIX_immXSize<OtherVT>;

class AArch64Base_ANDSIW_immWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSIW_wrapper";
  let DecoderMethod = "AArch64Base_ANDSIW_immWSize_decode_wrapper";
}

def AArch64Base_ANDSIW_immWSizeAsInt32
    : AArch64Base_ANDSIW_immWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSIW_immWSize_predicate(Imm); }]>;

def AArch64Base_ANDSIW_immWSizeAsLabel : AArch64Base_ANDSIW_immWSize<OtherVT>;

class AArch64Base_ANDSIX_immXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSIX_wrapper";
  let DecoderMethod = "AArch64Base_ANDSIX_immXSize_decode_wrapper";
}

def AArch64Base_ANDSIX_immXSizeAsInt64
    : AArch64Base_ANDSIX_immXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ANDSIX_immXSize_predicate(Imm); }]>;

def AArch64Base_ANDSIX_immXSizeAsLabel : AArch64Base_ANDSIX_immXSize<OtherVT>;

class AArch64Base_ANDSWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSWASR_wrapper";
  let DecoderMethod = "AArch64Base_ANDSWASR_imm6_decode_wrapper";
}

def AArch64Base_ANDSWASR_imm6AsInt32
    : AArch64Base_ANDSWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSWASR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSWASR_imm6AsLabel : AArch64Base_ANDSWASR_imm6<OtherVT>;

class AArch64Base_ANDSWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSWLSL_wrapper";
  let DecoderMethod = "AArch64Base_ANDSWLSL_imm6_decode_wrapper";
}

def AArch64Base_ANDSWLSL_imm6AsInt32
    : AArch64Base_ANDSWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSWLSL_imm6AsLabel : AArch64Base_ANDSWLSL_imm6<OtherVT>;

class AArch64Base_ANDSWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSWLSR_wrapper";
  let DecoderMethod = "AArch64Base_ANDSWLSR_imm6_decode_wrapper";
}

def AArch64Base_ANDSWLSR_imm6AsInt32
    : AArch64Base_ANDSWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSWLSR_imm6AsLabel : AArch64Base_ANDSWLSR_imm6<OtherVT>;

class AArch64Base_ANDSWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSWROR_wrapper";
  let DecoderMethod = "AArch64Base_ANDSWROR_imm6_decode_wrapper";
}

def AArch64Base_ANDSWROR_imm6AsInt32
    : AArch64Base_ANDSWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSWROR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSWROR_imm6AsLabel : AArch64Base_ANDSWROR_imm6<OtherVT>;

class AArch64Base_ANDSXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSXASR_wrapper";
  let DecoderMethod = "AArch64Base_ANDSXASR_imm6_decode_wrapper";
}

def AArch64Base_ANDSXASR_imm6AsInt32
    : AArch64Base_ANDSXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSXASR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSXASR_imm6AsLabel : AArch64Base_ANDSXASR_imm6<OtherVT>;

class AArch64Base_ANDSXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSXLSL_wrapper";
  let DecoderMethod = "AArch64Base_ANDSXLSL_imm6_decode_wrapper";
}

def AArch64Base_ANDSXLSL_imm6AsInt32
    : AArch64Base_ANDSXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSXLSL_imm6AsLabel : AArch64Base_ANDSXLSL_imm6<OtherVT>;

class AArch64Base_ANDSXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSXLSR_wrapper";
  let DecoderMethod = "AArch64Base_ANDSXLSR_imm6_decode_wrapper";
}

def AArch64Base_ANDSXLSR_imm6AsInt32
    : AArch64Base_ANDSXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSXLSR_imm6AsLabel : AArch64Base_ANDSXLSR_imm6<OtherVT>;

class AArch64Base_ANDSXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDSXROR_wrapper";
  let DecoderMethod = "AArch64Base_ANDSXROR_imm6_decode_wrapper";
}

def AArch64Base_ANDSXROR_imm6AsInt32
    : AArch64Base_ANDSXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDSXROR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDSXROR_imm6AsLabel : AArch64Base_ANDSXROR_imm6<OtherVT>;

class AArch64Base_ANDWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDWASR_wrapper";
  let DecoderMethod = "AArch64Base_ANDWASR_imm6_decode_wrapper";
}

def AArch64Base_ANDWASR_imm6AsInt32
    : AArch64Base_ANDWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDWASR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDWASR_imm6AsLabel : AArch64Base_ANDWASR_imm6<OtherVT>;

class AArch64Base_ANDWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDWLSL_wrapper";
  let DecoderMethod = "AArch64Base_ANDWLSL_imm6_decode_wrapper";
}

def AArch64Base_ANDWLSL_imm6AsInt32
    : AArch64Base_ANDWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ANDWLSL_imm6AsLabel : AArch64Base_ANDWLSL_imm6<OtherVT>;

class AArch64Base_ANDWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDWLSR_wrapper";
  let DecoderMethod = "AArch64Base_ANDWLSR_imm6_decode_wrapper";
}

def AArch64Base_ANDWLSR_imm6AsInt32
    : AArch64Base_ANDWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDWLSR_imm6AsLabel : AArch64Base_ANDWLSR_imm6<OtherVT>;

class AArch64Base_ANDWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDWROR_wrapper";
  let DecoderMethod = "AArch64Base_ANDWROR_imm6_decode_wrapper";
}

def AArch64Base_ANDWROR_imm6AsInt32
    : AArch64Base_ANDWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDWROR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDWROR_imm6AsLabel : AArch64Base_ANDWROR_imm6<OtherVT>;

class AArch64Base_ANDXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDXASR_wrapper";
  let DecoderMethod = "AArch64Base_ANDXASR_imm6_decode_wrapper";
}

def AArch64Base_ANDXASR_imm6AsInt32
    : AArch64Base_ANDXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDXASR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDXASR_imm6AsLabel : AArch64Base_ANDXASR_imm6<OtherVT>;

class AArch64Base_ANDXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDXLSL_wrapper";
  let DecoderMethod = "AArch64Base_ANDXLSL_imm6_decode_wrapper";
}

def AArch64Base_ANDXLSL_imm6AsInt32
    : AArch64Base_ANDXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ANDXLSL_imm6AsLabel : AArch64Base_ANDXLSL_imm6<OtherVT>;

class AArch64Base_ANDXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDXLSR_wrapper";
  let DecoderMethod = "AArch64Base_ANDXLSR_imm6_decode_wrapper";
}

def AArch64Base_ANDXLSR_imm6AsInt32
    : AArch64Base_ANDXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDXLSR_imm6AsLabel : AArch64Base_ANDXLSR_imm6<OtherVT>;

class AArch64Base_ANDXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ANDXROR_wrapper";
  let DecoderMethod = "AArch64Base_ANDXROR_imm6_decode_wrapper";
}

def AArch64Base_ANDXROR_imm6AsInt32
    : AArch64Base_ANDXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ANDXROR_imm6_predicate(Imm); }]>;

def AArch64Base_ANDXROR_imm6AsLabel : AArch64Base_ANDXROR_imm6<OtherVT>;

class AArch64Base_B_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_wrapper";
  let DecoderMethod = "AArch64Base_B_offset_decode_wrapper";
}

def AArch64Base_B_offsetAsInt64
    : AArch64Base_B_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_offset_predicate(Imm); }]>;

def AArch64Base_B_offsetAsLabel : AArch64Base_B_offset<OtherVT>;

class AArch64Base_BFMW_leftWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BFMW_wrapper";
  let DecoderMethod = "AArch64Base_BFMW_leftWSize_decode_wrapper";
}

def AArch64Base_BFMW_leftWSizeAsInt32
    : AArch64Base_BFMW_leftWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BFMW_leftWSize_predicate(Imm); }]>;

def AArch64Base_BFMW_leftWSizeAsLabel : AArch64Base_BFMW_leftWSize<OtherVT>;

class AArch64Base_BFMW_rightWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BFMW_wrapper";
  let DecoderMethod = "AArch64Base_BFMW_rightWSize_decode_wrapper";
}

def AArch64Base_BFMW_rightWSizeAsInt32
    : AArch64Base_BFMW_rightWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BFMW_rightWSize_predicate(Imm); }]>;

def AArch64Base_BFMW_rightWSizeAsLabel : AArch64Base_BFMW_rightWSize<OtherVT>;

class AArch64Base_BFMX_leftXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BFMX_wrapper";
  let DecoderMethod = "AArch64Base_BFMX_leftXSize_decode_wrapper";
}

def AArch64Base_BFMX_leftXSizeAsInt32
    : AArch64Base_BFMX_leftXSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BFMX_leftXSize_predicate(Imm); }]>;

def AArch64Base_BFMX_leftXSizeAsLabel : AArch64Base_BFMX_leftXSize<OtherVT>;

class AArch64Base_BFMX_rightXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BFMX_wrapper";
  let DecoderMethod = "AArch64Base_BFMX_rightXSize_decode_wrapper";
}

def AArch64Base_BFMX_rightXSizeAsInt32
    : AArch64Base_BFMX_rightXSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BFMX_rightXSize_predicate(Imm); }]>;

def AArch64Base_BFMX_rightXSizeAsLabel : AArch64Base_BFMX_rightXSize<OtherVT>;

class AArch64Base_BICSWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSWASR_wrapper";
  let DecoderMethod = "AArch64Base_BICSWASR_imm6_decode_wrapper";
}

def AArch64Base_BICSWASR_imm6AsInt32
    : AArch64Base_BICSWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSWASR_imm6_predicate(Imm); }]>;

def AArch64Base_BICSWASR_imm6AsLabel : AArch64Base_BICSWASR_imm6<OtherVT>;

class AArch64Base_BICSWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSWLSL_wrapper";
  let DecoderMethod = "AArch64Base_BICSWLSL_imm6_decode_wrapper";
}

def AArch64Base_BICSWLSL_imm6AsInt32
    : AArch64Base_BICSWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_BICSWLSL_imm6AsLabel : AArch64Base_BICSWLSL_imm6<OtherVT>;

class AArch64Base_BICSWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSWLSR_wrapper";
  let DecoderMethod = "AArch64Base_BICSWLSR_imm6_decode_wrapper";
}

def AArch64Base_BICSWLSR_imm6AsInt32
    : AArch64Base_BICSWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_BICSWLSR_imm6AsLabel : AArch64Base_BICSWLSR_imm6<OtherVT>;

class AArch64Base_BICSWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSWROR_wrapper";
  let DecoderMethod = "AArch64Base_BICSWROR_imm6_decode_wrapper";
}

def AArch64Base_BICSWROR_imm6AsInt32
    : AArch64Base_BICSWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSWROR_imm6_predicate(Imm); }]>;

def AArch64Base_BICSWROR_imm6AsLabel : AArch64Base_BICSWROR_imm6<OtherVT>;

class AArch64Base_BICSXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSXASR_wrapper";
  let DecoderMethod = "AArch64Base_BICSXASR_imm6_decode_wrapper";
}

def AArch64Base_BICSXASR_imm6AsInt32
    : AArch64Base_BICSXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSXASR_imm6_predicate(Imm); }]>;

def AArch64Base_BICSXASR_imm6AsLabel : AArch64Base_BICSXASR_imm6<OtherVT>;

class AArch64Base_BICSXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSXLSL_wrapper";
  let DecoderMethod = "AArch64Base_BICSXLSL_imm6_decode_wrapper";
}

def AArch64Base_BICSXLSL_imm6AsInt32
    : AArch64Base_BICSXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_BICSXLSL_imm6AsLabel : AArch64Base_BICSXLSL_imm6<OtherVT>;

class AArch64Base_BICSXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSXLSR_wrapper";
  let DecoderMethod = "AArch64Base_BICSXLSR_imm6_decode_wrapper";
}

def AArch64Base_BICSXLSR_imm6AsInt32
    : AArch64Base_BICSXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_BICSXLSR_imm6AsLabel : AArch64Base_BICSXLSR_imm6<OtherVT>;

class AArch64Base_BICSXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICSXROR_wrapper";
  let DecoderMethod = "AArch64Base_BICSXROR_imm6_decode_wrapper";
}

def AArch64Base_BICSXROR_imm6AsInt32
    : AArch64Base_BICSXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICSXROR_imm6_predicate(Imm); }]>;

def AArch64Base_BICSXROR_imm6AsLabel : AArch64Base_BICSXROR_imm6<OtherVT>;

class AArch64Base_BICWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICWASR_wrapper";
  let DecoderMethod = "AArch64Base_BICWASR_imm6_decode_wrapper";
}

def AArch64Base_BICWASR_imm6AsInt32
    : AArch64Base_BICWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICWASR_imm6_predicate(Imm); }]>;

def AArch64Base_BICWASR_imm6AsLabel : AArch64Base_BICWASR_imm6<OtherVT>;

class AArch64Base_BICWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICWLSL_wrapper";
  let DecoderMethod = "AArch64Base_BICWLSL_imm6_decode_wrapper";
}

def AArch64Base_BICWLSL_imm6AsInt32
    : AArch64Base_BICWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_BICWLSL_imm6AsLabel : AArch64Base_BICWLSL_imm6<OtherVT>;

class AArch64Base_BICWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICWLSR_wrapper";
  let DecoderMethod = "AArch64Base_BICWLSR_imm6_decode_wrapper";
}

def AArch64Base_BICWLSR_imm6AsInt32
    : AArch64Base_BICWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_BICWLSR_imm6AsLabel : AArch64Base_BICWLSR_imm6<OtherVT>;

class AArch64Base_BICWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICWROR_wrapper";
  let DecoderMethod = "AArch64Base_BICWROR_imm6_decode_wrapper";
}

def AArch64Base_BICWROR_imm6AsInt32
    : AArch64Base_BICWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICWROR_imm6_predicate(Imm); }]>;

def AArch64Base_BICWROR_imm6AsLabel : AArch64Base_BICWROR_imm6<OtherVT>;

class AArch64Base_BICXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICXASR_wrapper";
  let DecoderMethod = "AArch64Base_BICXASR_imm6_decode_wrapper";
}

def AArch64Base_BICXASR_imm6AsInt32
    : AArch64Base_BICXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICXASR_imm6_predicate(Imm); }]>;

def AArch64Base_BICXASR_imm6AsLabel : AArch64Base_BICXASR_imm6<OtherVT>;

class AArch64Base_BICXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICXLSL_wrapper";
  let DecoderMethod = "AArch64Base_BICXLSL_imm6_decode_wrapper";
}

def AArch64Base_BICXLSL_imm6AsInt32
    : AArch64Base_BICXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_BICXLSL_imm6AsLabel : AArch64Base_BICXLSL_imm6<OtherVT>;

class AArch64Base_BICXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICXLSR_wrapper";
  let DecoderMethod = "AArch64Base_BICXLSR_imm6_decode_wrapper";
}

def AArch64Base_BICXLSR_imm6AsInt32
    : AArch64Base_BICXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_BICXLSR_imm6AsLabel : AArch64Base_BICXLSR_imm6<OtherVT>;

class AArch64Base_BICXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BICXROR_wrapper";
  let DecoderMethod = "AArch64Base_BICXROR_imm6_decode_wrapper";
}

def AArch64Base_BICXROR_imm6AsInt32
    : AArch64Base_BICXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BICXROR_imm6_predicate(Imm); }]>;

def AArch64Base_BICXROR_imm6AsLabel : AArch64Base_BICXROR_imm6<OtherVT>;

class AArch64Base_BL_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BL_wrapper";
  let DecoderMethod = "AArch64Base_BL_offset_decode_wrapper";
}

def AArch64Base_BL_offsetAsInt64
    : AArch64Base_BL_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_BL_offset_predicate(Imm); }]>;

def AArch64Base_BL_offsetAsLabel : AArch64Base_BL_offset<OtherVT>;

class AArch64Base_BRK_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BRK_wrapper";
  let DecoderMethod = "AArch64Base_BRK_imm16_decode_wrapper";
}

def AArch64Base_BRK_imm16AsInt32
    : AArch64Base_BRK_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BRK_imm16_predicate(Imm); }]>;

def AArch64Base_BRK_imm16AsLabel : AArch64Base_BRK_imm16<OtherVT>;

class AArch64Base_BTI_bti<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_BTI_wrapper";
  let DecoderMethod = "AArch64Base_BTI_bti_decode_wrapper";
}

def AArch64Base_BTI_btiAsInt32
    : AArch64Base_BTI_bti<i32>
    , ImmLeaf<i32, [{ return AArch64Base_BTI_bti_predicate(Imm); }]>;

def AArch64Base_BTI_btiAsLabel : AArch64Base_BTI_bti<OtherVT>;

class AArch64Base_B_AL_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_AL_wrapper";
  let DecoderMethod = "AArch64Base_B_AL_offset_decode_wrapper";
}

def AArch64Base_B_AL_offsetAsInt64
    : AArch64Base_B_AL_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_AL_offset_predicate(Imm); }]>;

def AArch64Base_B_AL_offsetAsLabel : AArch64Base_B_AL_offset<OtherVT>;

class AArch64Base_B_CC_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_CC_wrapper";
  let DecoderMethod = "AArch64Base_B_CC_offset_decode_wrapper";
}

def AArch64Base_B_CC_offsetAsInt64
    : AArch64Base_B_CC_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_CC_offset_predicate(Imm); }]>;

def AArch64Base_B_CC_offsetAsLabel : AArch64Base_B_CC_offset<OtherVT>;

class AArch64Base_B_CS_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_CS_wrapper";
  let DecoderMethod = "AArch64Base_B_CS_offset_decode_wrapper";
}

def AArch64Base_B_CS_offsetAsInt64
    : AArch64Base_B_CS_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_CS_offset_predicate(Imm); }]>;

def AArch64Base_B_CS_offsetAsLabel : AArch64Base_B_CS_offset<OtherVT>;

class AArch64Base_B_EQ_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_EQ_wrapper";
  let DecoderMethod = "AArch64Base_B_EQ_offset_decode_wrapper";
}

def AArch64Base_B_EQ_offsetAsInt64
    : AArch64Base_B_EQ_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_EQ_offset_predicate(Imm); }]>;

def AArch64Base_B_EQ_offsetAsLabel : AArch64Base_B_EQ_offset<OtherVT>;

class AArch64Base_B_GE_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_GE_wrapper";
  let DecoderMethod = "AArch64Base_B_GE_offset_decode_wrapper";
}

def AArch64Base_B_GE_offsetAsInt64
    : AArch64Base_B_GE_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_GE_offset_predicate(Imm); }]>;

def AArch64Base_B_GE_offsetAsLabel : AArch64Base_B_GE_offset<OtherVT>;

class AArch64Base_B_GT_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_GT_wrapper";
  let DecoderMethod = "AArch64Base_B_GT_offset_decode_wrapper";
}

def AArch64Base_B_GT_offsetAsInt64
    : AArch64Base_B_GT_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_GT_offset_predicate(Imm); }]>;

def AArch64Base_B_GT_offsetAsLabel : AArch64Base_B_GT_offset<OtherVT>;

class AArch64Base_B_HI_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_HI_wrapper";
  let DecoderMethod = "AArch64Base_B_HI_offset_decode_wrapper";
}

def AArch64Base_B_HI_offsetAsInt64
    : AArch64Base_B_HI_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_HI_offset_predicate(Imm); }]>;

def AArch64Base_B_HI_offsetAsLabel : AArch64Base_B_HI_offset<OtherVT>;

class AArch64Base_B_LE_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_LE_wrapper";
  let DecoderMethod = "AArch64Base_B_LE_offset_decode_wrapper";
}

def AArch64Base_B_LE_offsetAsInt64
    : AArch64Base_B_LE_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_LE_offset_predicate(Imm); }]>;

def AArch64Base_B_LE_offsetAsLabel : AArch64Base_B_LE_offset<OtherVT>;

class AArch64Base_B_LS_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_LS_wrapper";
  let DecoderMethod = "AArch64Base_B_LS_offset_decode_wrapper";
}

def AArch64Base_B_LS_offsetAsInt64
    : AArch64Base_B_LS_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_LS_offset_predicate(Imm); }]>;

def AArch64Base_B_LS_offsetAsLabel : AArch64Base_B_LS_offset<OtherVT>;

class AArch64Base_B_LT_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_LT_wrapper";
  let DecoderMethod = "AArch64Base_B_LT_offset_decode_wrapper";
}

def AArch64Base_B_LT_offsetAsInt64
    : AArch64Base_B_LT_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_LT_offset_predicate(Imm); }]>;

def AArch64Base_B_LT_offsetAsLabel : AArch64Base_B_LT_offset<OtherVT>;

class AArch64Base_B_MI_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_MI_wrapper";
  let DecoderMethod = "AArch64Base_B_MI_offset_decode_wrapper";
}

def AArch64Base_B_MI_offsetAsInt64
    : AArch64Base_B_MI_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_MI_offset_predicate(Imm); }]>;

def AArch64Base_B_MI_offsetAsLabel : AArch64Base_B_MI_offset<OtherVT>;

class AArch64Base_B_NE_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_NE_wrapper";
  let DecoderMethod = "AArch64Base_B_NE_offset_decode_wrapper";
}

def AArch64Base_B_NE_offsetAsInt64
    : AArch64Base_B_NE_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_NE_offset_predicate(Imm); }]>;

def AArch64Base_B_NE_offsetAsLabel : AArch64Base_B_NE_offset<OtherVT>;

class AArch64Base_B_NV_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_NV_wrapper";
  let DecoderMethod = "AArch64Base_B_NV_offset_decode_wrapper";
}

def AArch64Base_B_NV_offsetAsInt64
    : AArch64Base_B_NV_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_NV_offset_predicate(Imm); }]>;

def AArch64Base_B_NV_offsetAsLabel : AArch64Base_B_NV_offset<OtherVT>;

class AArch64Base_B_PL_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_PL_wrapper";
  let DecoderMethod = "AArch64Base_B_PL_offset_decode_wrapper";
}

def AArch64Base_B_PL_offsetAsInt64
    : AArch64Base_B_PL_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_PL_offset_predicate(Imm); }]>;

def AArch64Base_B_PL_offsetAsLabel : AArch64Base_B_PL_offset<OtherVT>;

class AArch64Base_B_VC_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_VC_wrapper";
  let DecoderMethod = "AArch64Base_B_VC_offset_decode_wrapper";
}

def AArch64Base_B_VC_offsetAsInt64
    : AArch64Base_B_VC_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_VC_offset_predicate(Imm); }]>;

def AArch64Base_B_VC_offsetAsLabel : AArch64Base_B_VC_offset<OtherVT>;

class AArch64Base_B_VS_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_B_VS_wrapper";
  let DecoderMethod = "AArch64Base_B_VS_offset_decode_wrapper";
}

def AArch64Base_B_VS_offsetAsInt64
    : AArch64Base_B_VS_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_B_VS_offset_predicate(Imm); }]>;

def AArch64Base_B_VS_offsetAsLabel : AArch64Base_B_VS_offset<OtherVT>;

class AArch64Base_CBNZW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CBNZW_wrapper";
  let DecoderMethod = "AArch64Base_CBNZW_offset_decode_wrapper";
}

def AArch64Base_CBNZW_offsetAsInt64
    : AArch64Base_CBNZW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CBNZW_offset_predicate(Imm); }]>;

def AArch64Base_CBNZW_offsetAsLabel : AArch64Base_CBNZW_offset<OtherVT>;

class AArch64Base_CBNZX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CBNZX_wrapper";
  let DecoderMethod = "AArch64Base_CBNZX_offset_decode_wrapper";
}

def AArch64Base_CBNZX_offsetAsInt64
    : AArch64Base_CBNZX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CBNZX_offset_predicate(Imm); }]>;

def AArch64Base_CBNZX_offsetAsLabel : AArch64Base_CBNZX_offset<OtherVT>;

class AArch64Base_CBZW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CBZW_wrapper";
  let DecoderMethod = "AArch64Base_CBZW_offset_decode_wrapper";
}

def AArch64Base_CBZW_offsetAsInt64
    : AArch64Base_CBZW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CBZW_offset_predicate(Imm); }]>;

def AArch64Base_CBZW_offsetAsLabel : AArch64Base_CBZW_offset<OtherVT>;

class AArch64Base_CBZX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CBZX_wrapper";
  let DecoderMethod = "AArch64Base_CBZX_offset_decode_wrapper";
}

def AArch64Base_CBZX_offsetAsInt64
    : AArch64Base_CBZX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CBZX_offset_predicate(Imm); }]>;

def AArch64Base_CBZX_offsetAsLabel : AArch64Base_CBZX_offset<OtherVT>;

class AArch64Base_CCMNALW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNALW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNALW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNALW_nzcvAsInt32
    : AArch64Base_CCMNALW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNALW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNALW_nzcvAsLabel : AArch64Base_CCMNALW_nzcv<OtherVT>;

class AArch64Base_CCMNALX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNALX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNALX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNALX_nzcvAsInt32
    : AArch64Base_CCMNALX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNALX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNALX_nzcvAsLabel : AArch64Base_CCMNALX_nzcv<OtherVT>;

class AArch64Base_CCMNCCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNCCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNCCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNCCW_nzcvAsInt32
    : AArch64Base_CCMNCCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNCCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNCCW_nzcvAsLabel : AArch64Base_CCMNCCW_nzcv<OtherVT>;

class AArch64Base_CCMNCCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNCCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNCCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNCCX_nzcvAsInt32
    : AArch64Base_CCMNCCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNCCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNCCX_nzcvAsLabel : AArch64Base_CCMNCCX_nzcv<OtherVT>;

class AArch64Base_CCMNCSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNCSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNCSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNCSW_nzcvAsInt32
    : AArch64Base_CCMNCSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNCSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNCSW_nzcvAsLabel : AArch64Base_CCMNCSW_nzcv<OtherVT>;

class AArch64Base_CCMNCSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNCSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNCSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNCSX_nzcvAsInt32
    : AArch64Base_CCMNCSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNCSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNCSX_nzcvAsLabel : AArch64Base_CCMNCSX_nzcv<OtherVT>;

class AArch64Base_CCMNEQW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNEQW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNEQW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNEQW_nzcvAsInt32
    : AArch64Base_CCMNEQW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNEQW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNEQW_nzcvAsLabel : AArch64Base_CCMNEQW_nzcv<OtherVT>;

class AArch64Base_CCMNEQX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNEQX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNEQX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNEQX_nzcvAsInt32
    : AArch64Base_CCMNEQX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNEQX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNEQX_nzcvAsLabel : AArch64Base_CCMNEQX_nzcv<OtherVT>;

class AArch64Base_CCMNGEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNGEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNGEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNGEW_nzcvAsInt32
    : AArch64Base_CCMNGEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNGEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNGEW_nzcvAsLabel : AArch64Base_CCMNGEW_nzcv<OtherVT>;

class AArch64Base_CCMNGEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNGEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNGEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNGEX_nzcvAsInt32
    : AArch64Base_CCMNGEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNGEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNGEX_nzcvAsLabel : AArch64Base_CCMNGEX_nzcv<OtherVT>;

class AArch64Base_CCMNGTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNGTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNGTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNGTW_nzcvAsInt32
    : AArch64Base_CCMNGTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNGTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNGTW_nzcvAsLabel : AArch64Base_CCMNGTW_nzcv<OtherVT>;

class AArch64Base_CCMNGTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNGTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNGTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNGTX_nzcvAsInt32
    : AArch64Base_CCMNGTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNGTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNGTX_nzcvAsLabel : AArch64Base_CCMNGTX_nzcv<OtherVT>;

class AArch64Base_CCMNHIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNHIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNHIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNHIW_nzcvAsInt32
    : AArch64Base_CCMNHIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNHIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNHIW_nzcvAsLabel : AArch64Base_CCMNHIW_nzcv<OtherVT>;

class AArch64Base_CCMNHIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNHIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNHIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNHIX_nzcvAsInt32
    : AArch64Base_CCMNHIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNHIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNHIX_nzcvAsLabel : AArch64Base_CCMNHIX_nzcv<OtherVT>;

class AArch64Base_CCMNIALW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIALW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIALW_immX_decode_wrapper";
}

def AArch64Base_CCMNIALW_immXAsInt64
    : AArch64Base_CCMNIALW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIALW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIALW_immXAsLabel : AArch64Base_CCMNIALW_immX<OtherVT>;

class AArch64Base_CCMNIALW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIALW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIALW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIALW_nzcvAsInt32
    : AArch64Base_CCMNIALW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIALW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIALW_nzcvAsLabel : AArch64Base_CCMNIALW_nzcv<OtherVT>;

class AArch64Base_CCMNIALX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIALX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIALX_immX_decode_wrapper";
}

def AArch64Base_CCMNIALX_immXAsInt64
    : AArch64Base_CCMNIALX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIALX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIALX_immXAsLabel : AArch64Base_CCMNIALX_immX<OtherVT>;

class AArch64Base_CCMNIALX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIALX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIALX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIALX_nzcvAsInt32
    : AArch64Base_CCMNIALX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIALX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIALX_nzcvAsLabel : AArch64Base_CCMNIALX_nzcv<OtherVT>;

class AArch64Base_CCMNICCW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICCW_immX_decode_wrapper";
}

def AArch64Base_CCMNICCW_immXAsInt64
    : AArch64Base_CCMNICCW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNICCW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNICCW_immXAsLabel : AArch64Base_CCMNICCW_immX<OtherVT>;

class AArch64Base_CCMNICCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNICCW_nzcvAsInt32
    : AArch64Base_CCMNICCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNICCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNICCW_nzcvAsLabel : AArch64Base_CCMNICCW_nzcv<OtherVT>;

class AArch64Base_CCMNICCX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICCX_immX_decode_wrapper";
}

def AArch64Base_CCMNICCX_immXAsInt64
    : AArch64Base_CCMNICCX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNICCX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNICCX_immXAsLabel : AArch64Base_CCMNICCX_immX<OtherVT>;

class AArch64Base_CCMNICCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNICCX_nzcvAsInt32
    : AArch64Base_CCMNICCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNICCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNICCX_nzcvAsLabel : AArch64Base_CCMNICCX_nzcv<OtherVT>;

class AArch64Base_CCMNICSW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICSW_immX_decode_wrapper";
}

def AArch64Base_CCMNICSW_immXAsInt64
    : AArch64Base_CCMNICSW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNICSW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNICSW_immXAsLabel : AArch64Base_CCMNICSW_immX<OtherVT>;

class AArch64Base_CCMNICSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNICSW_nzcvAsInt32
    : AArch64Base_CCMNICSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNICSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNICSW_nzcvAsLabel : AArch64Base_CCMNICSW_nzcv<OtherVT>;

class AArch64Base_CCMNICSX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICSX_immX_decode_wrapper";
}

def AArch64Base_CCMNICSX_immXAsInt64
    : AArch64Base_CCMNICSX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNICSX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNICSX_immXAsLabel : AArch64Base_CCMNICSX_immX<OtherVT>;

class AArch64Base_CCMNICSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNICSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNICSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNICSX_nzcvAsInt32
    : AArch64Base_CCMNICSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNICSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNICSX_nzcvAsLabel : AArch64Base_CCMNICSX_nzcv<OtherVT>;

class AArch64Base_CCMNIEQW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIEQW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIEQW_immX_decode_wrapper";
}

def AArch64Base_CCMNIEQW_immXAsInt64
    : AArch64Base_CCMNIEQW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIEQW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIEQW_immXAsLabel : AArch64Base_CCMNIEQW_immX<OtherVT>;

class AArch64Base_CCMNIEQW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIEQW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIEQW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIEQW_nzcvAsInt32
    : AArch64Base_CCMNIEQW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIEQW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIEQW_nzcvAsLabel : AArch64Base_CCMNIEQW_nzcv<OtherVT>;

class AArch64Base_CCMNIEQX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIEQX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIEQX_immX_decode_wrapper";
}

def AArch64Base_CCMNIEQX_immXAsInt64
    : AArch64Base_CCMNIEQX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIEQX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIEQX_immXAsLabel : AArch64Base_CCMNIEQX_immX<OtherVT>;

class AArch64Base_CCMNIEQX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIEQX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIEQX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIEQX_nzcvAsInt32
    : AArch64Base_CCMNIEQX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIEQX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIEQX_nzcvAsLabel : AArch64Base_CCMNIEQX_nzcv<OtherVT>;

class AArch64Base_CCMNIGEW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGEW_immX_decode_wrapper";
}

def AArch64Base_CCMNIGEW_immXAsInt64
    : AArch64Base_CCMNIGEW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIGEW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIGEW_immXAsLabel : AArch64Base_CCMNIGEW_immX<OtherVT>;

class AArch64Base_CCMNIGEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIGEW_nzcvAsInt32
    : AArch64Base_CCMNIGEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIGEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIGEW_nzcvAsLabel : AArch64Base_CCMNIGEW_nzcv<OtherVT>;

class AArch64Base_CCMNIGEX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGEX_immX_decode_wrapper";
}

def AArch64Base_CCMNIGEX_immXAsInt64
    : AArch64Base_CCMNIGEX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIGEX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIGEX_immXAsLabel : AArch64Base_CCMNIGEX_immX<OtherVT>;

class AArch64Base_CCMNIGEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIGEX_nzcvAsInt32
    : AArch64Base_CCMNIGEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIGEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIGEX_nzcvAsLabel : AArch64Base_CCMNIGEX_nzcv<OtherVT>;

class AArch64Base_CCMNIGTW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGTW_immX_decode_wrapper";
}

def AArch64Base_CCMNIGTW_immXAsInt64
    : AArch64Base_CCMNIGTW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIGTW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIGTW_immXAsLabel : AArch64Base_CCMNIGTW_immX<OtherVT>;

class AArch64Base_CCMNIGTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIGTW_nzcvAsInt32
    : AArch64Base_CCMNIGTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIGTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIGTW_nzcvAsLabel : AArch64Base_CCMNIGTW_nzcv<OtherVT>;

class AArch64Base_CCMNIGTX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGTX_immX_decode_wrapper";
}

def AArch64Base_CCMNIGTX_immXAsInt64
    : AArch64Base_CCMNIGTX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIGTX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIGTX_immXAsLabel : AArch64Base_CCMNIGTX_immX<OtherVT>;

class AArch64Base_CCMNIGTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIGTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIGTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIGTX_nzcvAsInt32
    : AArch64Base_CCMNIGTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIGTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIGTX_nzcvAsLabel : AArch64Base_CCMNIGTX_nzcv<OtherVT>;

class AArch64Base_CCMNIHIW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIHIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIHIW_immX_decode_wrapper";
}

def AArch64Base_CCMNIHIW_immXAsInt64
    : AArch64Base_CCMNIHIW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIHIW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIHIW_immXAsLabel : AArch64Base_CCMNIHIW_immX<OtherVT>;

class AArch64Base_CCMNIHIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIHIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIHIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIHIW_nzcvAsInt32
    : AArch64Base_CCMNIHIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIHIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIHIW_nzcvAsLabel : AArch64Base_CCMNIHIW_nzcv<OtherVT>;

class AArch64Base_CCMNIHIX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIHIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIHIX_immX_decode_wrapper";
}

def AArch64Base_CCMNIHIX_immXAsInt64
    : AArch64Base_CCMNIHIX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIHIX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIHIX_immXAsLabel : AArch64Base_CCMNIHIX_immX<OtherVT>;

class AArch64Base_CCMNIHIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIHIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIHIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIHIX_nzcvAsInt32
    : AArch64Base_CCMNIHIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIHIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIHIX_nzcvAsLabel : AArch64Base_CCMNIHIX_nzcv<OtherVT>;

class AArch64Base_CCMNILEW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILEW_immX_decode_wrapper";
}

def AArch64Base_CCMNILEW_immXAsInt64
    : AArch64Base_CCMNILEW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNILEW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNILEW_immXAsLabel : AArch64Base_CCMNILEW_immX<OtherVT>;

class AArch64Base_CCMNILEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNILEW_nzcvAsInt32
    : AArch64Base_CCMNILEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNILEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNILEW_nzcvAsLabel : AArch64Base_CCMNILEW_nzcv<OtherVT>;

class AArch64Base_CCMNILEX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILEX_immX_decode_wrapper";
}

def AArch64Base_CCMNILEX_immXAsInt64
    : AArch64Base_CCMNILEX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNILEX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNILEX_immXAsLabel : AArch64Base_CCMNILEX_immX<OtherVT>;

class AArch64Base_CCMNILEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNILEX_nzcvAsInt32
    : AArch64Base_CCMNILEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNILEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNILEX_nzcvAsLabel : AArch64Base_CCMNILEX_nzcv<OtherVT>;

class AArch64Base_CCMNILSW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILSW_immX_decode_wrapper";
}

def AArch64Base_CCMNILSW_immXAsInt64
    : AArch64Base_CCMNILSW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNILSW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNILSW_immXAsLabel : AArch64Base_CCMNILSW_immX<OtherVT>;

class AArch64Base_CCMNILSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNILSW_nzcvAsInt32
    : AArch64Base_CCMNILSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNILSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNILSW_nzcvAsLabel : AArch64Base_CCMNILSW_nzcv<OtherVT>;

class AArch64Base_CCMNILSX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILSX_immX_decode_wrapper";
}

def AArch64Base_CCMNILSX_immXAsInt64
    : AArch64Base_CCMNILSX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNILSX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNILSX_immXAsLabel : AArch64Base_CCMNILSX_immX<OtherVT>;

class AArch64Base_CCMNILSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNILSX_nzcvAsInt32
    : AArch64Base_CCMNILSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNILSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNILSX_nzcvAsLabel : AArch64Base_CCMNILSX_nzcv<OtherVT>;

class AArch64Base_CCMNILTW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILTW_immX_decode_wrapper";
}

def AArch64Base_CCMNILTW_immXAsInt64
    : AArch64Base_CCMNILTW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNILTW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNILTW_immXAsLabel : AArch64Base_CCMNILTW_immX<OtherVT>;

class AArch64Base_CCMNILTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNILTW_nzcvAsInt32
    : AArch64Base_CCMNILTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNILTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNILTW_nzcvAsLabel : AArch64Base_CCMNILTW_nzcv<OtherVT>;

class AArch64Base_CCMNILTX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILTX_immX_decode_wrapper";
}

def AArch64Base_CCMNILTX_immXAsInt64
    : AArch64Base_CCMNILTX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNILTX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNILTX_immXAsLabel : AArch64Base_CCMNILTX_immX<OtherVT>;

class AArch64Base_CCMNILTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNILTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNILTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNILTX_nzcvAsInt32
    : AArch64Base_CCMNILTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNILTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNILTX_nzcvAsLabel : AArch64Base_CCMNILTX_nzcv<OtherVT>;

class AArch64Base_CCMNIMIW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIMIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIMIW_immX_decode_wrapper";
}

def AArch64Base_CCMNIMIW_immXAsInt64
    : AArch64Base_CCMNIMIW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIMIW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIMIW_immXAsLabel : AArch64Base_CCMNIMIW_immX<OtherVT>;

class AArch64Base_CCMNIMIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIMIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIMIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIMIW_nzcvAsInt32
    : AArch64Base_CCMNIMIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIMIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIMIW_nzcvAsLabel : AArch64Base_CCMNIMIW_nzcv<OtherVT>;

class AArch64Base_CCMNIMIX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIMIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIMIX_immX_decode_wrapper";
}

def AArch64Base_CCMNIMIX_immXAsInt64
    : AArch64Base_CCMNIMIX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIMIX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIMIX_immXAsLabel : AArch64Base_CCMNIMIX_immX<OtherVT>;

class AArch64Base_CCMNIMIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIMIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIMIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIMIX_nzcvAsInt32
    : AArch64Base_CCMNIMIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIMIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIMIX_nzcvAsLabel : AArch64Base_CCMNIMIX_nzcv<OtherVT>;

class AArch64Base_CCMNINEW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINEW_immX_decode_wrapper";
}

def AArch64Base_CCMNINEW_immXAsInt64
    : AArch64Base_CCMNINEW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNINEW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNINEW_immXAsLabel : AArch64Base_CCMNINEW_immX<OtherVT>;

class AArch64Base_CCMNINEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNINEW_nzcvAsInt32
    : AArch64Base_CCMNINEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNINEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNINEW_nzcvAsLabel : AArch64Base_CCMNINEW_nzcv<OtherVT>;

class AArch64Base_CCMNINEX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINEX_immX_decode_wrapper";
}

def AArch64Base_CCMNINEX_immXAsInt64
    : AArch64Base_CCMNINEX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNINEX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNINEX_immXAsLabel : AArch64Base_CCMNINEX_immX<OtherVT>;

class AArch64Base_CCMNINEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNINEX_nzcvAsInt32
    : AArch64Base_CCMNINEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNINEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNINEX_nzcvAsLabel : AArch64Base_CCMNINEX_nzcv<OtherVT>;

class AArch64Base_CCMNINVW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINVW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINVW_immX_decode_wrapper";
}

def AArch64Base_CCMNINVW_immXAsInt64
    : AArch64Base_CCMNINVW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNINVW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNINVW_immXAsLabel : AArch64Base_CCMNINVW_immX<OtherVT>;

class AArch64Base_CCMNINVW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINVW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINVW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNINVW_nzcvAsInt32
    : AArch64Base_CCMNINVW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNINVW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNINVW_nzcvAsLabel : AArch64Base_CCMNINVW_nzcv<OtherVT>;

class AArch64Base_CCMNINVX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINVX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINVX_immX_decode_wrapper";
}

def AArch64Base_CCMNINVX_immXAsInt64
    : AArch64Base_CCMNINVX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNINVX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNINVX_immXAsLabel : AArch64Base_CCMNINVX_immX<OtherVT>;

class AArch64Base_CCMNINVX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNINVX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNINVX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNINVX_nzcvAsInt32
    : AArch64Base_CCMNINVX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNINVX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNINVX_nzcvAsLabel : AArch64Base_CCMNINVX_nzcv<OtherVT>;

class AArch64Base_CCMNIPLW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIPLW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIPLW_immX_decode_wrapper";
}

def AArch64Base_CCMNIPLW_immXAsInt64
    : AArch64Base_CCMNIPLW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIPLW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIPLW_immXAsLabel : AArch64Base_CCMNIPLW_immX<OtherVT>;

class AArch64Base_CCMNIPLW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIPLW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIPLW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIPLW_nzcvAsInt32
    : AArch64Base_CCMNIPLW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIPLW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIPLW_nzcvAsLabel : AArch64Base_CCMNIPLW_nzcv<OtherVT>;

class AArch64Base_CCMNIPLX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIPLX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIPLX_immX_decode_wrapper";
}

def AArch64Base_CCMNIPLX_immXAsInt64
    : AArch64Base_CCMNIPLX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIPLX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIPLX_immXAsLabel : AArch64Base_CCMNIPLX_immX<OtherVT>;

class AArch64Base_CCMNIPLX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIPLX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIPLX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIPLX_nzcvAsInt32
    : AArch64Base_CCMNIPLX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIPLX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIPLX_nzcvAsLabel : AArch64Base_CCMNIPLX_nzcv<OtherVT>;

class AArch64Base_CCMNIVCW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVCW_immX_decode_wrapper";
}

def AArch64Base_CCMNIVCW_immXAsInt64
    : AArch64Base_CCMNIVCW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIVCW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIVCW_immXAsLabel : AArch64Base_CCMNIVCW_immX<OtherVT>;

class AArch64Base_CCMNIVCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIVCW_nzcvAsInt32
    : AArch64Base_CCMNIVCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIVCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIVCW_nzcvAsLabel : AArch64Base_CCMNIVCW_nzcv<OtherVT>;

class AArch64Base_CCMNIVCX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVCX_immX_decode_wrapper";
}

def AArch64Base_CCMNIVCX_immXAsInt64
    : AArch64Base_CCMNIVCX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIVCX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIVCX_immXAsLabel : AArch64Base_CCMNIVCX_immX<OtherVT>;

class AArch64Base_CCMNIVCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIVCX_nzcvAsInt32
    : AArch64Base_CCMNIVCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIVCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIVCX_nzcvAsLabel : AArch64Base_CCMNIVCX_nzcv<OtherVT>;

class AArch64Base_CCMNIVSW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVSW_immX_decode_wrapper";
}

def AArch64Base_CCMNIVSW_immXAsInt64
    : AArch64Base_CCMNIVSW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIVSW_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIVSW_immXAsLabel : AArch64Base_CCMNIVSW_immX<OtherVT>;

class AArch64Base_CCMNIVSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIVSW_nzcvAsInt32
    : AArch64Base_CCMNIVSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIVSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIVSW_nzcvAsLabel : AArch64Base_CCMNIVSW_nzcv<OtherVT>;

class AArch64Base_CCMNIVSX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVSX_immX_decode_wrapper";
}

def AArch64Base_CCMNIVSX_immXAsInt64
    : AArch64Base_CCMNIVSX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMNIVSX_immX_predicate(Imm); }]>;

def AArch64Base_CCMNIVSX_immXAsLabel : AArch64Base_CCMNIVSX_immX<OtherVT>;

class AArch64Base_CCMNIVSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNIVSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNIVSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNIVSX_nzcvAsInt32
    : AArch64Base_CCMNIVSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNIVSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNIVSX_nzcvAsLabel : AArch64Base_CCMNIVSX_nzcv<OtherVT>;

class AArch64Base_CCMNLEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNLEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNLEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNLEW_nzcvAsInt32
    : AArch64Base_CCMNLEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNLEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNLEW_nzcvAsLabel : AArch64Base_CCMNLEW_nzcv<OtherVT>;

class AArch64Base_CCMNLEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNLEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNLEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNLEX_nzcvAsInt32
    : AArch64Base_CCMNLEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNLEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNLEX_nzcvAsLabel : AArch64Base_CCMNLEX_nzcv<OtherVT>;

class AArch64Base_CCMNLSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNLSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNLSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNLSW_nzcvAsInt32
    : AArch64Base_CCMNLSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNLSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNLSW_nzcvAsLabel : AArch64Base_CCMNLSW_nzcv<OtherVT>;

class AArch64Base_CCMNLSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNLSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNLSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNLSX_nzcvAsInt32
    : AArch64Base_CCMNLSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNLSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNLSX_nzcvAsLabel : AArch64Base_CCMNLSX_nzcv<OtherVT>;

class AArch64Base_CCMNLTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNLTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNLTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNLTW_nzcvAsInt32
    : AArch64Base_CCMNLTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNLTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNLTW_nzcvAsLabel : AArch64Base_CCMNLTW_nzcv<OtherVT>;

class AArch64Base_CCMNLTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNLTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNLTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNLTX_nzcvAsInt32
    : AArch64Base_CCMNLTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNLTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNLTX_nzcvAsLabel : AArch64Base_CCMNLTX_nzcv<OtherVT>;

class AArch64Base_CCMNMIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNMIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNMIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNMIW_nzcvAsInt32
    : AArch64Base_CCMNMIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNMIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNMIW_nzcvAsLabel : AArch64Base_CCMNMIW_nzcv<OtherVT>;

class AArch64Base_CCMNMIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNMIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNMIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNMIX_nzcvAsInt32
    : AArch64Base_CCMNMIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNMIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNMIX_nzcvAsLabel : AArch64Base_CCMNMIX_nzcv<OtherVT>;

class AArch64Base_CCMNNEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNNEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNNEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNNEW_nzcvAsInt32
    : AArch64Base_CCMNNEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNNEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNNEW_nzcvAsLabel : AArch64Base_CCMNNEW_nzcv<OtherVT>;

class AArch64Base_CCMNNEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNNEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNNEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNNEX_nzcvAsInt32
    : AArch64Base_CCMNNEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNNEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNNEX_nzcvAsLabel : AArch64Base_CCMNNEX_nzcv<OtherVT>;

class AArch64Base_CCMNNVW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNNVW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNNVW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNNVW_nzcvAsInt32
    : AArch64Base_CCMNNVW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNNVW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNNVW_nzcvAsLabel : AArch64Base_CCMNNVW_nzcv<OtherVT>;

class AArch64Base_CCMNNVX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNNVX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNNVX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNNVX_nzcvAsInt32
    : AArch64Base_CCMNNVX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNNVX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNNVX_nzcvAsLabel : AArch64Base_CCMNNVX_nzcv<OtherVT>;

class AArch64Base_CCMNPLW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNPLW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNPLW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNPLW_nzcvAsInt32
    : AArch64Base_CCMNPLW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNPLW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNPLW_nzcvAsLabel : AArch64Base_CCMNPLW_nzcv<OtherVT>;

class AArch64Base_CCMNPLX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNPLX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNPLX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNPLX_nzcvAsInt32
    : AArch64Base_CCMNPLX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNPLX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNPLX_nzcvAsLabel : AArch64Base_CCMNPLX_nzcv<OtherVT>;

class AArch64Base_CCMNVCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNVCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNVCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNVCW_nzcvAsInt32
    : AArch64Base_CCMNVCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNVCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNVCW_nzcvAsLabel : AArch64Base_CCMNVCW_nzcv<OtherVT>;

class AArch64Base_CCMNVCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNVCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNVCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNVCX_nzcvAsInt32
    : AArch64Base_CCMNVCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNVCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNVCX_nzcvAsLabel : AArch64Base_CCMNVCX_nzcv<OtherVT>;

class AArch64Base_CCMNVSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNVSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMNVSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMNVSW_nzcvAsInt32
    : AArch64Base_CCMNVSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNVSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNVSW_nzcvAsLabel : AArch64Base_CCMNVSW_nzcv<OtherVT>;

class AArch64Base_CCMNVSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMNVSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMNVSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMNVSX_nzcvAsInt32
    : AArch64Base_CCMNVSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMNVSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMNVSX_nzcvAsLabel : AArch64Base_CCMNVSX_nzcv<OtherVT>;

class AArch64Base_CCMPALW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPALW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPALW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPALW_nzcvAsInt32
    : AArch64Base_CCMPALW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPALW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPALW_nzcvAsLabel : AArch64Base_CCMPALW_nzcv<OtherVT>;

class AArch64Base_CCMPALX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPALX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPALX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPALX_nzcvAsInt32
    : AArch64Base_CCMPALX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPALX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPALX_nzcvAsLabel : AArch64Base_CCMPALX_nzcv<OtherVT>;

class AArch64Base_CCMPCCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPCCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPCCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPCCW_nzcvAsInt32
    : AArch64Base_CCMPCCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPCCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPCCW_nzcvAsLabel : AArch64Base_CCMPCCW_nzcv<OtherVT>;

class AArch64Base_CCMPCCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPCCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPCCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPCCX_nzcvAsInt32
    : AArch64Base_CCMPCCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPCCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPCCX_nzcvAsLabel : AArch64Base_CCMPCCX_nzcv<OtherVT>;

class AArch64Base_CCMPCSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPCSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPCSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPCSW_nzcvAsInt32
    : AArch64Base_CCMPCSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPCSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPCSW_nzcvAsLabel : AArch64Base_CCMPCSW_nzcv<OtherVT>;

class AArch64Base_CCMPCSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPCSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPCSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPCSX_nzcvAsInt32
    : AArch64Base_CCMPCSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPCSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPCSX_nzcvAsLabel : AArch64Base_CCMPCSX_nzcv<OtherVT>;

class AArch64Base_CCMPEQW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPEQW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPEQW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPEQW_nzcvAsInt32
    : AArch64Base_CCMPEQW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPEQW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPEQW_nzcvAsLabel : AArch64Base_CCMPEQW_nzcv<OtherVT>;

class AArch64Base_CCMPEQX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPEQX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPEQX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPEQX_nzcvAsInt32
    : AArch64Base_CCMPEQX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPEQX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPEQX_nzcvAsLabel : AArch64Base_CCMPEQX_nzcv<OtherVT>;

class AArch64Base_CCMPGEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPGEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPGEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPGEW_nzcvAsInt32
    : AArch64Base_CCMPGEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPGEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPGEW_nzcvAsLabel : AArch64Base_CCMPGEW_nzcv<OtherVT>;

class AArch64Base_CCMPGEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPGEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPGEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPGEX_nzcvAsInt32
    : AArch64Base_CCMPGEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPGEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPGEX_nzcvAsLabel : AArch64Base_CCMPGEX_nzcv<OtherVT>;

class AArch64Base_CCMPGTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPGTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPGTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPGTW_nzcvAsInt32
    : AArch64Base_CCMPGTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPGTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPGTW_nzcvAsLabel : AArch64Base_CCMPGTW_nzcv<OtherVT>;

class AArch64Base_CCMPGTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPGTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPGTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPGTX_nzcvAsInt32
    : AArch64Base_CCMPGTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPGTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPGTX_nzcvAsLabel : AArch64Base_CCMPGTX_nzcv<OtherVT>;

class AArch64Base_CCMPHIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPHIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPHIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPHIW_nzcvAsInt32
    : AArch64Base_CCMPHIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPHIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPHIW_nzcvAsLabel : AArch64Base_CCMPHIW_nzcv<OtherVT>;

class AArch64Base_CCMPHIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPHIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPHIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPHIX_nzcvAsInt32
    : AArch64Base_CCMPHIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPHIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPHIX_nzcvAsLabel : AArch64Base_CCMPHIX_nzcv<OtherVT>;

class AArch64Base_CCMPIALW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIALW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIALW_immX_decode_wrapper";
}

def AArch64Base_CCMPIALW_immXAsInt64
    : AArch64Base_CCMPIALW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIALW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIALW_immXAsLabel : AArch64Base_CCMPIALW_immX<OtherVT>;

class AArch64Base_CCMPIALW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIALW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIALW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIALW_nzcvAsInt32
    : AArch64Base_CCMPIALW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIALW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIALW_nzcvAsLabel : AArch64Base_CCMPIALW_nzcv<OtherVT>;

class AArch64Base_CCMPIALX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIALX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIALX_immX_decode_wrapper";
}

def AArch64Base_CCMPIALX_immXAsInt64
    : AArch64Base_CCMPIALX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIALX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIALX_immXAsLabel : AArch64Base_CCMPIALX_immX<OtherVT>;

class AArch64Base_CCMPIALX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIALX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIALX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIALX_nzcvAsInt32
    : AArch64Base_CCMPIALX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIALX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIALX_nzcvAsLabel : AArch64Base_CCMPIALX_nzcv<OtherVT>;

class AArch64Base_CCMPICCW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICCW_immX_decode_wrapper";
}

def AArch64Base_CCMPICCW_immXAsInt64
    : AArch64Base_CCMPICCW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPICCW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPICCW_immXAsLabel : AArch64Base_CCMPICCW_immX<OtherVT>;

class AArch64Base_CCMPICCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPICCW_nzcvAsInt32
    : AArch64Base_CCMPICCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPICCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPICCW_nzcvAsLabel : AArch64Base_CCMPICCW_nzcv<OtherVT>;

class AArch64Base_CCMPICCX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICCX_immX_decode_wrapper";
}

def AArch64Base_CCMPICCX_immXAsInt64
    : AArch64Base_CCMPICCX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPICCX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPICCX_immXAsLabel : AArch64Base_CCMPICCX_immX<OtherVT>;

class AArch64Base_CCMPICCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPICCX_nzcvAsInt32
    : AArch64Base_CCMPICCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPICCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPICCX_nzcvAsLabel : AArch64Base_CCMPICCX_nzcv<OtherVT>;

class AArch64Base_CCMPICSW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICSW_immX_decode_wrapper";
}

def AArch64Base_CCMPICSW_immXAsInt64
    : AArch64Base_CCMPICSW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPICSW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPICSW_immXAsLabel : AArch64Base_CCMPICSW_immX<OtherVT>;

class AArch64Base_CCMPICSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPICSW_nzcvAsInt32
    : AArch64Base_CCMPICSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPICSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPICSW_nzcvAsLabel : AArch64Base_CCMPICSW_nzcv<OtherVT>;

class AArch64Base_CCMPICSX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICSX_immX_decode_wrapper";
}

def AArch64Base_CCMPICSX_immXAsInt64
    : AArch64Base_CCMPICSX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPICSX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPICSX_immXAsLabel : AArch64Base_CCMPICSX_immX<OtherVT>;

class AArch64Base_CCMPICSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPICSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPICSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPICSX_nzcvAsInt32
    : AArch64Base_CCMPICSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPICSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPICSX_nzcvAsLabel : AArch64Base_CCMPICSX_nzcv<OtherVT>;

class AArch64Base_CCMPIEQW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIEQW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIEQW_immX_decode_wrapper";
}

def AArch64Base_CCMPIEQW_immXAsInt64
    : AArch64Base_CCMPIEQW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIEQW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIEQW_immXAsLabel : AArch64Base_CCMPIEQW_immX<OtherVT>;

class AArch64Base_CCMPIEQW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIEQW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIEQW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIEQW_nzcvAsInt32
    : AArch64Base_CCMPIEQW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIEQW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIEQW_nzcvAsLabel : AArch64Base_CCMPIEQW_nzcv<OtherVT>;

class AArch64Base_CCMPIEQX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIEQX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIEQX_immX_decode_wrapper";
}

def AArch64Base_CCMPIEQX_immXAsInt64
    : AArch64Base_CCMPIEQX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIEQX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIEQX_immXAsLabel : AArch64Base_CCMPIEQX_immX<OtherVT>;

class AArch64Base_CCMPIEQX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIEQX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIEQX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIEQX_nzcvAsInt32
    : AArch64Base_CCMPIEQX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIEQX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIEQX_nzcvAsLabel : AArch64Base_CCMPIEQX_nzcv<OtherVT>;

class AArch64Base_CCMPIGEW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGEW_immX_decode_wrapper";
}

def AArch64Base_CCMPIGEW_immXAsInt64
    : AArch64Base_CCMPIGEW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIGEW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIGEW_immXAsLabel : AArch64Base_CCMPIGEW_immX<OtherVT>;

class AArch64Base_CCMPIGEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIGEW_nzcvAsInt32
    : AArch64Base_CCMPIGEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIGEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIGEW_nzcvAsLabel : AArch64Base_CCMPIGEW_nzcv<OtherVT>;

class AArch64Base_CCMPIGEX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGEX_immX_decode_wrapper";
}

def AArch64Base_CCMPIGEX_immXAsInt64
    : AArch64Base_CCMPIGEX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIGEX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIGEX_immXAsLabel : AArch64Base_CCMPIGEX_immX<OtherVT>;

class AArch64Base_CCMPIGEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIGEX_nzcvAsInt32
    : AArch64Base_CCMPIGEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIGEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIGEX_nzcvAsLabel : AArch64Base_CCMPIGEX_nzcv<OtherVT>;

class AArch64Base_CCMPIGTW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGTW_immX_decode_wrapper";
}

def AArch64Base_CCMPIGTW_immXAsInt64
    : AArch64Base_CCMPIGTW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIGTW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIGTW_immXAsLabel : AArch64Base_CCMPIGTW_immX<OtherVT>;

class AArch64Base_CCMPIGTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIGTW_nzcvAsInt32
    : AArch64Base_CCMPIGTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIGTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIGTW_nzcvAsLabel : AArch64Base_CCMPIGTW_nzcv<OtherVT>;

class AArch64Base_CCMPIGTX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGTX_immX_decode_wrapper";
}

def AArch64Base_CCMPIGTX_immXAsInt64
    : AArch64Base_CCMPIGTX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIGTX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIGTX_immXAsLabel : AArch64Base_CCMPIGTX_immX<OtherVT>;

class AArch64Base_CCMPIGTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIGTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIGTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIGTX_nzcvAsInt32
    : AArch64Base_CCMPIGTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIGTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIGTX_nzcvAsLabel : AArch64Base_CCMPIGTX_nzcv<OtherVT>;

class AArch64Base_CCMPIHIW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIHIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIHIW_immX_decode_wrapper";
}

def AArch64Base_CCMPIHIW_immXAsInt64
    : AArch64Base_CCMPIHIW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIHIW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIHIW_immXAsLabel : AArch64Base_CCMPIHIW_immX<OtherVT>;

class AArch64Base_CCMPIHIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIHIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIHIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIHIW_nzcvAsInt32
    : AArch64Base_CCMPIHIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIHIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIHIW_nzcvAsLabel : AArch64Base_CCMPIHIW_nzcv<OtherVT>;

class AArch64Base_CCMPIHIX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIHIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIHIX_immX_decode_wrapper";
}

def AArch64Base_CCMPIHIX_immXAsInt64
    : AArch64Base_CCMPIHIX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIHIX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIHIX_immXAsLabel : AArch64Base_CCMPIHIX_immX<OtherVT>;

class AArch64Base_CCMPIHIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIHIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIHIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIHIX_nzcvAsInt32
    : AArch64Base_CCMPIHIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIHIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIHIX_nzcvAsLabel : AArch64Base_CCMPIHIX_nzcv<OtherVT>;

class AArch64Base_CCMPILEW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILEW_immX_decode_wrapper";
}

def AArch64Base_CCMPILEW_immXAsInt64
    : AArch64Base_CCMPILEW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPILEW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPILEW_immXAsLabel : AArch64Base_CCMPILEW_immX<OtherVT>;

class AArch64Base_CCMPILEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPILEW_nzcvAsInt32
    : AArch64Base_CCMPILEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPILEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPILEW_nzcvAsLabel : AArch64Base_CCMPILEW_nzcv<OtherVT>;

class AArch64Base_CCMPILEX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILEX_immX_decode_wrapper";
}

def AArch64Base_CCMPILEX_immXAsInt64
    : AArch64Base_CCMPILEX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPILEX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPILEX_immXAsLabel : AArch64Base_CCMPILEX_immX<OtherVT>;

class AArch64Base_CCMPILEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPILEX_nzcvAsInt32
    : AArch64Base_CCMPILEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPILEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPILEX_nzcvAsLabel : AArch64Base_CCMPILEX_nzcv<OtherVT>;

class AArch64Base_CCMPILSW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILSW_immX_decode_wrapper";
}

def AArch64Base_CCMPILSW_immXAsInt64
    : AArch64Base_CCMPILSW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPILSW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPILSW_immXAsLabel : AArch64Base_CCMPILSW_immX<OtherVT>;

class AArch64Base_CCMPILSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPILSW_nzcvAsInt32
    : AArch64Base_CCMPILSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPILSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPILSW_nzcvAsLabel : AArch64Base_CCMPILSW_nzcv<OtherVT>;

class AArch64Base_CCMPILSX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILSX_immX_decode_wrapper";
}

def AArch64Base_CCMPILSX_immXAsInt64
    : AArch64Base_CCMPILSX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPILSX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPILSX_immXAsLabel : AArch64Base_CCMPILSX_immX<OtherVT>;

class AArch64Base_CCMPILSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPILSX_nzcvAsInt32
    : AArch64Base_CCMPILSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPILSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPILSX_nzcvAsLabel : AArch64Base_CCMPILSX_nzcv<OtherVT>;

class AArch64Base_CCMPILTW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILTW_immX_decode_wrapper";
}

def AArch64Base_CCMPILTW_immXAsInt64
    : AArch64Base_CCMPILTW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPILTW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPILTW_immXAsLabel : AArch64Base_CCMPILTW_immX<OtherVT>;

class AArch64Base_CCMPILTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPILTW_nzcvAsInt32
    : AArch64Base_CCMPILTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPILTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPILTW_nzcvAsLabel : AArch64Base_CCMPILTW_nzcv<OtherVT>;

class AArch64Base_CCMPILTX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILTX_immX_decode_wrapper";
}

def AArch64Base_CCMPILTX_immXAsInt64
    : AArch64Base_CCMPILTX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPILTX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPILTX_immXAsLabel : AArch64Base_CCMPILTX_immX<OtherVT>;

class AArch64Base_CCMPILTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPILTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPILTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPILTX_nzcvAsInt32
    : AArch64Base_CCMPILTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPILTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPILTX_nzcvAsLabel : AArch64Base_CCMPILTX_nzcv<OtherVT>;

class AArch64Base_CCMPIMIW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIMIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIMIW_immX_decode_wrapper";
}

def AArch64Base_CCMPIMIW_immXAsInt64
    : AArch64Base_CCMPIMIW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIMIW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIMIW_immXAsLabel : AArch64Base_CCMPIMIW_immX<OtherVT>;

class AArch64Base_CCMPIMIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIMIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIMIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIMIW_nzcvAsInt32
    : AArch64Base_CCMPIMIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIMIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIMIW_nzcvAsLabel : AArch64Base_CCMPIMIW_nzcv<OtherVT>;

class AArch64Base_CCMPIMIX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIMIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIMIX_immX_decode_wrapper";
}

def AArch64Base_CCMPIMIX_immXAsInt64
    : AArch64Base_CCMPIMIX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIMIX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIMIX_immXAsLabel : AArch64Base_CCMPIMIX_immX<OtherVT>;

class AArch64Base_CCMPIMIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIMIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIMIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIMIX_nzcvAsInt32
    : AArch64Base_CCMPIMIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIMIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIMIX_nzcvAsLabel : AArch64Base_CCMPIMIX_nzcv<OtherVT>;

class AArch64Base_CCMPINEW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINEW_immX_decode_wrapper";
}

def AArch64Base_CCMPINEW_immXAsInt64
    : AArch64Base_CCMPINEW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPINEW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPINEW_immXAsLabel : AArch64Base_CCMPINEW_immX<OtherVT>;

class AArch64Base_CCMPINEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPINEW_nzcvAsInt32
    : AArch64Base_CCMPINEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPINEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPINEW_nzcvAsLabel : AArch64Base_CCMPINEW_nzcv<OtherVT>;

class AArch64Base_CCMPINEX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINEX_immX_decode_wrapper";
}

def AArch64Base_CCMPINEX_immXAsInt64
    : AArch64Base_CCMPINEX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPINEX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPINEX_immXAsLabel : AArch64Base_CCMPINEX_immX<OtherVT>;

class AArch64Base_CCMPINEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPINEX_nzcvAsInt32
    : AArch64Base_CCMPINEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPINEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPINEX_nzcvAsLabel : AArch64Base_CCMPINEX_nzcv<OtherVT>;

class AArch64Base_CCMPINVW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINVW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINVW_immX_decode_wrapper";
}

def AArch64Base_CCMPINVW_immXAsInt64
    : AArch64Base_CCMPINVW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPINVW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPINVW_immXAsLabel : AArch64Base_CCMPINVW_immX<OtherVT>;

class AArch64Base_CCMPINVW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINVW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINVW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPINVW_nzcvAsInt32
    : AArch64Base_CCMPINVW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPINVW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPINVW_nzcvAsLabel : AArch64Base_CCMPINVW_nzcv<OtherVT>;

class AArch64Base_CCMPINVX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINVX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINVX_immX_decode_wrapper";
}

def AArch64Base_CCMPINVX_immXAsInt64
    : AArch64Base_CCMPINVX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPINVX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPINVX_immXAsLabel : AArch64Base_CCMPINVX_immX<OtherVT>;

class AArch64Base_CCMPINVX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPINVX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPINVX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPINVX_nzcvAsInt32
    : AArch64Base_CCMPINVX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPINVX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPINVX_nzcvAsLabel : AArch64Base_CCMPINVX_nzcv<OtherVT>;

class AArch64Base_CCMPIPLW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIPLW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIPLW_immX_decode_wrapper";
}

def AArch64Base_CCMPIPLW_immXAsInt64
    : AArch64Base_CCMPIPLW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIPLW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIPLW_immXAsLabel : AArch64Base_CCMPIPLW_immX<OtherVT>;

class AArch64Base_CCMPIPLW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIPLW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIPLW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIPLW_nzcvAsInt32
    : AArch64Base_CCMPIPLW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIPLW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIPLW_nzcvAsLabel : AArch64Base_CCMPIPLW_nzcv<OtherVT>;

class AArch64Base_CCMPIPLX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIPLX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIPLX_immX_decode_wrapper";
}

def AArch64Base_CCMPIPLX_immXAsInt64
    : AArch64Base_CCMPIPLX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIPLX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIPLX_immXAsLabel : AArch64Base_CCMPIPLX_immX<OtherVT>;

class AArch64Base_CCMPIPLX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIPLX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIPLX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIPLX_nzcvAsInt32
    : AArch64Base_CCMPIPLX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIPLX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIPLX_nzcvAsLabel : AArch64Base_CCMPIPLX_nzcv<OtherVT>;

class AArch64Base_CCMPIVCW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVCW_immX_decode_wrapper";
}

def AArch64Base_CCMPIVCW_immXAsInt64
    : AArch64Base_CCMPIVCW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIVCW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIVCW_immXAsLabel : AArch64Base_CCMPIVCW_immX<OtherVT>;

class AArch64Base_CCMPIVCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIVCW_nzcvAsInt32
    : AArch64Base_CCMPIVCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIVCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIVCW_nzcvAsLabel : AArch64Base_CCMPIVCW_nzcv<OtherVT>;

class AArch64Base_CCMPIVCX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVCX_immX_decode_wrapper";
}

def AArch64Base_CCMPIVCX_immXAsInt64
    : AArch64Base_CCMPIVCX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIVCX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIVCX_immXAsLabel : AArch64Base_CCMPIVCX_immX<OtherVT>;

class AArch64Base_CCMPIVCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIVCX_nzcvAsInt32
    : AArch64Base_CCMPIVCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIVCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIVCX_nzcvAsLabel : AArch64Base_CCMPIVCX_nzcv<OtherVT>;

class AArch64Base_CCMPIVSW_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVSW_immX_decode_wrapper";
}

def AArch64Base_CCMPIVSW_immXAsInt64
    : AArch64Base_CCMPIVSW_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIVSW_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIVSW_immXAsLabel : AArch64Base_CCMPIVSW_immX<OtherVT>;

class AArch64Base_CCMPIVSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIVSW_nzcvAsInt32
    : AArch64Base_CCMPIVSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIVSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIVSW_nzcvAsLabel : AArch64Base_CCMPIVSW_nzcv<OtherVT>;

class AArch64Base_CCMPIVSX_immX<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVSX_immX_decode_wrapper";
}

def AArch64Base_CCMPIVSX_immXAsInt64
    : AArch64Base_CCMPIVSX_immX<i64>
    , ImmLeaf<i64, [{ return AArch64Base_CCMPIVSX_immX_predicate(Imm); }]>;

def AArch64Base_CCMPIVSX_immXAsLabel : AArch64Base_CCMPIVSX_immX<OtherVT>;

class AArch64Base_CCMPIVSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPIVSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPIVSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPIVSX_nzcvAsInt32
    : AArch64Base_CCMPIVSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPIVSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPIVSX_nzcvAsLabel : AArch64Base_CCMPIVSX_nzcv<OtherVT>;

class AArch64Base_CCMPLEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPLEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPLEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPLEW_nzcvAsInt32
    : AArch64Base_CCMPLEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPLEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPLEW_nzcvAsLabel : AArch64Base_CCMPLEW_nzcv<OtherVT>;

class AArch64Base_CCMPLEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPLEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPLEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPLEX_nzcvAsInt32
    : AArch64Base_CCMPLEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPLEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPLEX_nzcvAsLabel : AArch64Base_CCMPLEX_nzcv<OtherVT>;

class AArch64Base_CCMPLSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPLSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPLSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPLSW_nzcvAsInt32
    : AArch64Base_CCMPLSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPLSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPLSW_nzcvAsLabel : AArch64Base_CCMPLSW_nzcv<OtherVT>;

class AArch64Base_CCMPLSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPLSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPLSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPLSX_nzcvAsInt32
    : AArch64Base_CCMPLSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPLSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPLSX_nzcvAsLabel : AArch64Base_CCMPLSX_nzcv<OtherVT>;

class AArch64Base_CCMPLTW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPLTW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPLTW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPLTW_nzcvAsInt32
    : AArch64Base_CCMPLTW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPLTW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPLTW_nzcvAsLabel : AArch64Base_CCMPLTW_nzcv<OtherVT>;

class AArch64Base_CCMPLTX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPLTX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPLTX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPLTX_nzcvAsInt32
    : AArch64Base_CCMPLTX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPLTX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPLTX_nzcvAsLabel : AArch64Base_CCMPLTX_nzcv<OtherVT>;

class AArch64Base_CCMPMIW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPMIW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPMIW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPMIW_nzcvAsInt32
    : AArch64Base_CCMPMIW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPMIW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPMIW_nzcvAsLabel : AArch64Base_CCMPMIW_nzcv<OtherVT>;

class AArch64Base_CCMPMIX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPMIX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPMIX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPMIX_nzcvAsInt32
    : AArch64Base_CCMPMIX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPMIX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPMIX_nzcvAsLabel : AArch64Base_CCMPMIX_nzcv<OtherVT>;

class AArch64Base_CCMPNEW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPNEW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPNEW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPNEW_nzcvAsInt32
    : AArch64Base_CCMPNEW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPNEW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPNEW_nzcvAsLabel : AArch64Base_CCMPNEW_nzcv<OtherVT>;

class AArch64Base_CCMPNEX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPNEX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPNEX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPNEX_nzcvAsInt32
    : AArch64Base_CCMPNEX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPNEX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPNEX_nzcvAsLabel : AArch64Base_CCMPNEX_nzcv<OtherVT>;

class AArch64Base_CCMPNVW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPNVW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPNVW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPNVW_nzcvAsInt32
    : AArch64Base_CCMPNVW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPNVW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPNVW_nzcvAsLabel : AArch64Base_CCMPNVW_nzcv<OtherVT>;

class AArch64Base_CCMPNVX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPNVX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPNVX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPNVX_nzcvAsInt32
    : AArch64Base_CCMPNVX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPNVX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPNVX_nzcvAsLabel : AArch64Base_CCMPNVX_nzcv<OtherVT>;

class AArch64Base_CCMPPLW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPPLW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPPLW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPPLW_nzcvAsInt32
    : AArch64Base_CCMPPLW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPPLW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPPLW_nzcvAsLabel : AArch64Base_CCMPPLW_nzcv<OtherVT>;

class AArch64Base_CCMPPLX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPPLX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPPLX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPPLX_nzcvAsInt32
    : AArch64Base_CCMPPLX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPPLX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPPLX_nzcvAsLabel : AArch64Base_CCMPPLX_nzcv<OtherVT>;

class AArch64Base_CCMPVCW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPVCW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPVCW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPVCW_nzcvAsInt32
    : AArch64Base_CCMPVCW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPVCW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPVCW_nzcvAsLabel : AArch64Base_CCMPVCW_nzcv<OtherVT>;

class AArch64Base_CCMPVCX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPVCX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPVCX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPVCX_nzcvAsInt32
    : AArch64Base_CCMPVCX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPVCX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPVCX_nzcvAsLabel : AArch64Base_CCMPVCX_nzcv<OtherVT>;

class AArch64Base_CCMPVSW_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPVSW_wrapper";
  let DecoderMethod = "AArch64Base_CCMPVSW_nzcv_decode_wrapper";
}

def AArch64Base_CCMPVSW_nzcvAsInt32
    : AArch64Base_CCMPVSW_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPVSW_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPVSW_nzcvAsLabel : AArch64Base_CCMPVSW_nzcv<OtherVT>;

class AArch64Base_CCMPVSX_nzcv<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_CCMPVSX_wrapper";
  let DecoderMethod = "AArch64Base_CCMPVSX_nzcv_decode_wrapper";
}

def AArch64Base_CCMPVSX_nzcvAsInt32
    : AArch64Base_CCMPVSX_nzcv<i32>
    , ImmLeaf<i32, [{ return AArch64Base_CCMPVSX_nzcv_predicate(Imm); }]>;

def AArch64Base_CCMPVSX_nzcvAsLabel : AArch64Base_CCMPVSX_nzcv<OtherVT>;

class AArch64Base_EONWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONWASR_wrapper";
  let DecoderMethod = "AArch64Base_EONWASR_imm6_decode_wrapper";
}

def AArch64Base_EONWASR_imm6AsInt32
    : AArch64Base_EONWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONWASR_imm6_predicate(Imm); }]>;

def AArch64Base_EONWASR_imm6AsLabel : AArch64Base_EONWASR_imm6<OtherVT>;

class AArch64Base_EONWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONWLSL_wrapper";
  let DecoderMethod = "AArch64Base_EONWLSL_imm6_decode_wrapper";
}

def AArch64Base_EONWLSL_imm6AsInt32
    : AArch64Base_EONWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_EONWLSL_imm6AsLabel : AArch64Base_EONWLSL_imm6<OtherVT>;

class AArch64Base_EONWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONWLSR_wrapper";
  let DecoderMethod = "AArch64Base_EONWLSR_imm6_decode_wrapper";
}

def AArch64Base_EONWLSR_imm6AsInt32
    : AArch64Base_EONWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_EONWLSR_imm6AsLabel : AArch64Base_EONWLSR_imm6<OtherVT>;

class AArch64Base_EONWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONWROR_wrapper";
  let DecoderMethod = "AArch64Base_EONWROR_imm6_decode_wrapper";
}

def AArch64Base_EONWROR_imm6AsInt32
    : AArch64Base_EONWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONWROR_imm6_predicate(Imm); }]>;

def AArch64Base_EONWROR_imm6AsLabel : AArch64Base_EONWROR_imm6<OtherVT>;

class AArch64Base_EONXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONXASR_wrapper";
  let DecoderMethod = "AArch64Base_EONXASR_imm6_decode_wrapper";
}

def AArch64Base_EONXASR_imm6AsInt32
    : AArch64Base_EONXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONXASR_imm6_predicate(Imm); }]>;

def AArch64Base_EONXASR_imm6AsLabel : AArch64Base_EONXASR_imm6<OtherVT>;

class AArch64Base_EONXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONXLSL_wrapper";
  let DecoderMethod = "AArch64Base_EONXLSL_imm6_decode_wrapper";
}

def AArch64Base_EONXLSL_imm6AsInt32
    : AArch64Base_EONXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_EONXLSL_imm6AsLabel : AArch64Base_EONXLSL_imm6<OtherVT>;

class AArch64Base_EONXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONXLSR_wrapper";
  let DecoderMethod = "AArch64Base_EONXLSR_imm6_decode_wrapper";
}

def AArch64Base_EONXLSR_imm6AsInt32
    : AArch64Base_EONXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_EONXLSR_imm6AsLabel : AArch64Base_EONXLSR_imm6<OtherVT>;

class AArch64Base_EONXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EONXROR_wrapper";
  let DecoderMethod = "AArch64Base_EONXROR_imm6_decode_wrapper";
}

def AArch64Base_EONXROR_imm6AsInt32
    : AArch64Base_EONXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EONXROR_imm6_predicate(Imm); }]>;

def AArch64Base_EONXROR_imm6AsLabel : AArch64Base_EONXROR_imm6<OtherVT>;

class AArch64Base_EORIW_immWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORIW_wrapper";
  let DecoderMethod = "AArch64Base_EORIW_immWSize_decode_wrapper";
}

def AArch64Base_EORIW_immWSizeAsInt32
    : AArch64Base_EORIW_immWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORIW_immWSize_predicate(Imm); }]>;

def AArch64Base_EORIW_immWSizeAsLabel : AArch64Base_EORIW_immWSize<OtherVT>;

class AArch64Base_EORIX_immXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORIX_wrapper";
  let DecoderMethod = "AArch64Base_EORIX_immXSize_decode_wrapper";
}

def AArch64Base_EORIX_immXSizeAsInt64
    : AArch64Base_EORIX_immXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_EORIX_immXSize_predicate(Imm); }]>;

def AArch64Base_EORIX_immXSizeAsLabel : AArch64Base_EORIX_immXSize<OtherVT>;

class AArch64Base_EORWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORWASR_wrapper";
  let DecoderMethod = "AArch64Base_EORWASR_imm6_decode_wrapper";
}

def AArch64Base_EORWASR_imm6AsInt32
    : AArch64Base_EORWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORWASR_imm6_predicate(Imm); }]>;

def AArch64Base_EORWASR_imm6AsLabel : AArch64Base_EORWASR_imm6<OtherVT>;

class AArch64Base_EORWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORWLSL_wrapper";
  let DecoderMethod = "AArch64Base_EORWLSL_imm6_decode_wrapper";
}

def AArch64Base_EORWLSL_imm6AsInt32
    : AArch64Base_EORWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_EORWLSL_imm6AsLabel : AArch64Base_EORWLSL_imm6<OtherVT>;

class AArch64Base_EORWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORWLSR_wrapper";
  let DecoderMethod = "AArch64Base_EORWLSR_imm6_decode_wrapper";
}

def AArch64Base_EORWLSR_imm6AsInt32
    : AArch64Base_EORWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_EORWLSR_imm6AsLabel : AArch64Base_EORWLSR_imm6<OtherVT>;

class AArch64Base_EORWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORWROR_wrapper";
  let DecoderMethod = "AArch64Base_EORWROR_imm6_decode_wrapper";
}

def AArch64Base_EORWROR_imm6AsInt32
    : AArch64Base_EORWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORWROR_imm6_predicate(Imm); }]>;

def AArch64Base_EORWROR_imm6AsLabel : AArch64Base_EORWROR_imm6<OtherVT>;

class AArch64Base_EORXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORXASR_wrapper";
  let DecoderMethod = "AArch64Base_EORXASR_imm6_decode_wrapper";
}

def AArch64Base_EORXASR_imm6AsInt32
    : AArch64Base_EORXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORXASR_imm6_predicate(Imm); }]>;

def AArch64Base_EORXASR_imm6AsLabel : AArch64Base_EORXASR_imm6<OtherVT>;

class AArch64Base_EORXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORXLSL_wrapper";
  let DecoderMethod = "AArch64Base_EORXLSL_imm6_decode_wrapper";
}

def AArch64Base_EORXLSL_imm6AsInt32
    : AArch64Base_EORXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_EORXLSL_imm6AsLabel : AArch64Base_EORXLSL_imm6<OtherVT>;

class AArch64Base_EORXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORXLSR_wrapper";
  let DecoderMethod = "AArch64Base_EORXLSR_imm6_decode_wrapper";
}

def AArch64Base_EORXLSR_imm6AsInt32
    : AArch64Base_EORXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_EORXLSR_imm6AsLabel : AArch64Base_EORXLSR_imm6<OtherVT>;

class AArch64Base_EORXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EORXROR_wrapper";
  let DecoderMethod = "AArch64Base_EORXROR_imm6_decode_wrapper";
}

def AArch64Base_EORXROR_imm6AsInt32
    : AArch64Base_EORXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EORXROR_imm6_predicate(Imm); }]>;

def AArch64Base_EORXROR_imm6AsLabel : AArch64Base_EORXROR_imm6<OtherVT>;

class AArch64Base_EXTRW_shiftWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EXTRW_wrapper";
  let DecoderMethod = "AArch64Base_EXTRW_shiftWSize_decode_wrapper";
}

def AArch64Base_EXTRW_shiftWSizeAsInt32
    : AArch64Base_EXTRW_shiftWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EXTRW_shiftWSize_predicate(Imm); }]>;

def AArch64Base_EXTRW_shiftWSizeAsLabel : AArch64Base_EXTRW_shiftWSize<OtherVT>;

class AArch64Base_EXTRX_shiftXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_EXTRX_wrapper";
  let DecoderMethod = "AArch64Base_EXTRX_shiftXSize_decode_wrapper";
}

def AArch64Base_EXTRX_shiftXSizeAsInt32
    : AArch64Base_EXTRX_shiftXSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_EXTRX_shiftXSize_predicate(Imm); }]>;

def AArch64Base_EXTRX_shiftXSizeAsLabel : AArch64Base_EXTRX_shiftXSize<OtherVT>;

class AArch64Base_HLT_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_HLT_wrapper";
  let DecoderMethod = "AArch64Base_HLT_imm16_decode_wrapper";
}

def AArch64Base_HLT_imm16AsInt32
    : AArch64Base_HLT_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_HLT_imm16_predicate(Imm); }]>;

def AArch64Base_HLT_imm16AsLabel : AArch64Base_HLT_imm16<OtherVT>;

class AArch64Base_LDPS_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPS_wrapper";
  let DecoderMethod = "AArch64Base_LDPS_offWSize_decode_wrapper";
}

def AArch64Base_LDPS_offWSizeAsInt64
    : AArch64Base_LDPS_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPS_offWSize_predicate(Imm); }]>;

def AArch64Base_LDPS_offWSizeAsLabel : AArch64Base_LDPS_offWSize<OtherVT>;

class AArch64Base_LDPSPre_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPSPre_wrapper";
  let DecoderMethod = "AArch64Base_LDPSPre_offWSize_decode_wrapper";
}

def AArch64Base_LDPSPre_offWSizeAsInt64
    : AArch64Base_LDPSPre_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPSPre_offWSize_predicate(Imm); }]>;

def AArch64Base_LDPSPre_offWSizeAsLabel : AArch64Base_LDPSPre_offWSize<OtherVT>;

class AArch64Base_LDPSPst_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPSPst_wrapper";
  let DecoderMethod = "AArch64Base_LDPSPst_offWSize_decode_wrapper";
}

def AArch64Base_LDPSPst_offWSizeAsInt64
    : AArch64Base_LDPSPst_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPSPst_offWSize_predicate(Imm); }]>;

def AArch64Base_LDPSPst_offWSizeAsLabel : AArch64Base_LDPSPst_offWSize<OtherVT>;

class AArch64Base_LDPW_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPW_wrapper";
  let DecoderMethod = "AArch64Base_LDPW_offWSize_decode_wrapper";
}

def AArch64Base_LDPW_offWSizeAsInt64
    : AArch64Base_LDPW_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPW_offWSize_predicate(Imm); }]>;

def AArch64Base_LDPW_offWSizeAsLabel : AArch64Base_LDPW_offWSize<OtherVT>;

class AArch64Base_LDPWPre_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPWPre_wrapper";
  let DecoderMethod = "AArch64Base_LDPWPre_offWSize_decode_wrapper";
}

def AArch64Base_LDPWPre_offWSizeAsInt64
    : AArch64Base_LDPWPre_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPWPre_offWSize_predicate(Imm); }]>;

def AArch64Base_LDPWPre_offWSizeAsLabel : AArch64Base_LDPWPre_offWSize<OtherVT>;

class AArch64Base_LDPWPst_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPWPst_wrapper";
  let DecoderMethod = "AArch64Base_LDPWPst_offWSize_decode_wrapper";
}

def AArch64Base_LDPWPst_offWSizeAsInt64
    : AArch64Base_LDPWPst_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPWPst_offWSize_predicate(Imm); }]>;

def AArch64Base_LDPWPst_offWSizeAsLabel : AArch64Base_LDPWPst_offWSize<OtherVT>;

class AArch64Base_LDPX_offXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPX_wrapper";
  let DecoderMethod = "AArch64Base_LDPX_offXSize_decode_wrapper";
}

def AArch64Base_LDPX_offXSizeAsInt64
    : AArch64Base_LDPX_offXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPX_offXSize_predicate(Imm); }]>;

def AArch64Base_LDPX_offXSizeAsLabel : AArch64Base_LDPX_offXSize<OtherVT>;

class AArch64Base_LDPXPre_offXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPXPre_wrapper";
  let DecoderMethod = "AArch64Base_LDPXPre_offXSize_decode_wrapper";
}

def AArch64Base_LDPXPre_offXSizeAsInt64
    : AArch64Base_LDPXPre_offXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPXPre_offXSize_predicate(Imm); }]>;

def AArch64Base_LDPXPre_offXSizeAsLabel : AArch64Base_LDPXPre_offXSize<OtherVT>;

class AArch64Base_LDPXPst_offXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDPXPst_wrapper";
  let DecoderMethod = "AArch64Base_LDPXPst_offXSize_decode_wrapper";
}

def AArch64Base_LDPXPst_offXSizeAsInt64
    : AArch64Base_LDPXPst_offXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDPXPst_offXSize_predicate(Imm); }]>;

def AArch64Base_LDPXPst_offXSizeAsLabel : AArch64Base_LDPXPst_offXSize<OtherVT>;

class AArch64Base_LDRB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRB_wrapper";
  let DecoderMethod = "AArch64Base_LDRB_offset_decode_wrapper";
}

def AArch64Base_LDRB_offsetAsInt64
    : AArch64Base_LDRB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRB_offset_predicate(Imm); }]>;

def AArch64Base_LDRB_offsetAsLabel : AArch64Base_LDRB_offset<OtherVT>;

class AArch64Base_LDRH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRH_wrapper";
  let DecoderMethod = "AArch64Base_LDRH_offset_decode_wrapper";
}

def AArch64Base_LDRH_offsetAsInt64
    : AArch64Base_LDRH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRH_offset_predicate(Imm); }]>;

def AArch64Base_LDRH_offsetAsLabel : AArch64Base_LDRH_offset<OtherVT>;

class AArch64Base_LDRLitSWX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRLitSWX_wrapper";
  let DecoderMethod = "AArch64Base_LDRLitSWX_offset_decode_wrapper";
}

def AArch64Base_LDRLitSWX_offsetAsInt64
    : AArch64Base_LDRLitSWX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRLitSWX_offset_predicate(Imm); }]>;

def AArch64Base_LDRLitSWX_offsetAsLabel : AArch64Base_LDRLitSWX_offset<OtherVT>;

class AArch64Base_LDRLitW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRLitW_wrapper";
  let DecoderMethod = "AArch64Base_LDRLitW_offset_decode_wrapper";
}

def AArch64Base_LDRLitW_offsetAsInt64
    : AArch64Base_LDRLitW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRLitW_offset_predicate(Imm); }]>;

def AArch64Base_LDRLitW_offsetAsLabel : AArch64Base_LDRLitW_offset<OtherVT>;

class AArch64Base_LDRLitX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRLitX_wrapper";
  let DecoderMethod = "AArch64Base_LDRLitX_offset_decode_wrapper";
}

def AArch64Base_LDRLitX_offsetAsInt64
    : AArch64Base_LDRLitX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRLitX_offset_predicate(Imm); }]>;

def AArch64Base_LDRLitX_offsetAsLabel : AArch64Base_LDRLitX_offset<OtherVT>;

class AArch64Base_LDRPreB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreB_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreB_offset_decode_wrapper";
}

def AArch64Base_LDRPreB_offsetAsInt64
    : AArch64Base_LDRPreB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreB_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreB_offsetAsLabel : AArch64Base_LDRPreB_offset<OtherVT>;

class AArch64Base_LDRPreH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreH_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreH_offset_decode_wrapper";
}

def AArch64Base_LDRPreH_offsetAsInt64
    : AArch64Base_LDRPreH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreH_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreH_offsetAsLabel : AArch64Base_LDRPreH_offset<OtherVT>;

class AArch64Base_LDRPreSBW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreSBW_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreSBW_offset_decode_wrapper";
}

def AArch64Base_LDRPreSBW_offsetAsInt64
    : AArch64Base_LDRPreSBW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreSBW_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreSBW_offsetAsLabel : AArch64Base_LDRPreSBW_offset<OtherVT>;

class AArch64Base_LDRPreSBX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreSBX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreSBX_offset_decode_wrapper";
}

def AArch64Base_LDRPreSBX_offsetAsInt64
    : AArch64Base_LDRPreSBX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreSBX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreSBX_offsetAsLabel : AArch64Base_LDRPreSBX_offset<OtherVT>;

class AArch64Base_LDRPreSHW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreSHW_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreSHW_offset_decode_wrapper";
}

def AArch64Base_LDRPreSHW_offsetAsInt64
    : AArch64Base_LDRPreSHW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreSHW_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreSHW_offsetAsLabel : AArch64Base_LDRPreSHW_offset<OtherVT>;

class AArch64Base_LDRPreSHX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreSHX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreSHX_offset_decode_wrapper";
}

def AArch64Base_LDRPreSHX_offsetAsInt64
    : AArch64Base_LDRPreSHX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreSHX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreSHX_offsetAsLabel : AArch64Base_LDRPreSHX_offset<OtherVT>;

class AArch64Base_LDRPreSWX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreSWX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreSWX_offset_decode_wrapper";
}

def AArch64Base_LDRPreSWX_offsetAsInt64
    : AArch64Base_LDRPreSWX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreSWX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreSWX_offsetAsLabel : AArch64Base_LDRPreSWX_offset<OtherVT>;

class AArch64Base_LDRPreW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreW_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreW_offset_decode_wrapper";
}

def AArch64Base_LDRPreW_offsetAsInt64
    : AArch64Base_LDRPreW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreW_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreW_offsetAsLabel : AArch64Base_LDRPreW_offset<OtherVT>;

class AArch64Base_LDRPreX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPreX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPreX_offset_decode_wrapper";
}

def AArch64Base_LDRPreX_offsetAsInt64
    : AArch64Base_LDRPreX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPreX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPreX_offsetAsLabel : AArch64Base_LDRPreX_offset<OtherVT>;

class AArch64Base_LDRPstB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstB_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstB_offset_decode_wrapper";
}

def AArch64Base_LDRPstB_offsetAsInt64
    : AArch64Base_LDRPstB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstB_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstB_offsetAsLabel : AArch64Base_LDRPstB_offset<OtherVT>;

class AArch64Base_LDRPstH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstH_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstH_offset_decode_wrapper";
}

def AArch64Base_LDRPstH_offsetAsInt64
    : AArch64Base_LDRPstH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstH_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstH_offsetAsLabel : AArch64Base_LDRPstH_offset<OtherVT>;

class AArch64Base_LDRPstSBW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstSBW_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstSBW_offset_decode_wrapper";
}

def AArch64Base_LDRPstSBW_offsetAsInt64
    : AArch64Base_LDRPstSBW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstSBW_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstSBW_offsetAsLabel : AArch64Base_LDRPstSBW_offset<OtherVT>;

class AArch64Base_LDRPstSBX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstSBX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstSBX_offset_decode_wrapper";
}

def AArch64Base_LDRPstSBX_offsetAsInt64
    : AArch64Base_LDRPstSBX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstSBX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstSBX_offsetAsLabel : AArch64Base_LDRPstSBX_offset<OtherVT>;

class AArch64Base_LDRPstSHW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstSHW_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstSHW_offset_decode_wrapper";
}

def AArch64Base_LDRPstSHW_offsetAsInt64
    : AArch64Base_LDRPstSHW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstSHW_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstSHW_offsetAsLabel : AArch64Base_LDRPstSHW_offset<OtherVT>;

class AArch64Base_LDRPstSHX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstSHX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstSHX_offset_decode_wrapper";
}

def AArch64Base_LDRPstSHX_offsetAsInt64
    : AArch64Base_LDRPstSHX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstSHX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstSHX_offsetAsLabel : AArch64Base_LDRPstSHX_offset<OtherVT>;

class AArch64Base_LDRPstSWX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstSWX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstSWX_offset_decode_wrapper";
}

def AArch64Base_LDRPstSWX_offsetAsInt64
    : AArch64Base_LDRPstSWX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstSWX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstSWX_offsetAsLabel : AArch64Base_LDRPstSWX_offset<OtherVT>;

class AArch64Base_LDRPstW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstW_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstW_offset_decode_wrapper";
}

def AArch64Base_LDRPstW_offsetAsInt64
    : AArch64Base_LDRPstW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstW_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstW_offsetAsLabel : AArch64Base_LDRPstW_offset<OtherVT>;

class AArch64Base_LDRPstX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRPstX_wrapper";
  let DecoderMethod = "AArch64Base_LDRPstX_offset_decode_wrapper";
}

def AArch64Base_LDRPstX_offsetAsInt64
    : AArch64Base_LDRPstX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRPstX_offset_predicate(Imm); }]>;

def AArch64Base_LDRPstX_offsetAsLabel : AArch64Base_LDRPstX_offset<OtherVT>;

class AArch64Base_LDRSBW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRSBW_wrapper";
  let DecoderMethod = "AArch64Base_LDRSBW_offset_decode_wrapper";
}

def AArch64Base_LDRSBW_offsetAsInt64
    : AArch64Base_LDRSBW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRSBW_offset_predicate(Imm); }]>;

def AArch64Base_LDRSBW_offsetAsLabel : AArch64Base_LDRSBW_offset<OtherVT>;

class AArch64Base_LDRSBX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRSBX_wrapper";
  let DecoderMethod = "AArch64Base_LDRSBX_offset_decode_wrapper";
}

def AArch64Base_LDRSBX_offsetAsInt64
    : AArch64Base_LDRSBX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRSBX_offset_predicate(Imm); }]>;

def AArch64Base_LDRSBX_offsetAsLabel : AArch64Base_LDRSBX_offset<OtherVT>;

class AArch64Base_LDRSHW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRSHW_wrapper";
  let DecoderMethod = "AArch64Base_LDRSHW_offset_decode_wrapper";
}

def AArch64Base_LDRSHW_offsetAsInt64
    : AArch64Base_LDRSHW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRSHW_offset_predicate(Imm); }]>;

def AArch64Base_LDRSHW_offsetAsLabel : AArch64Base_LDRSHW_offset<OtherVT>;

class AArch64Base_LDRSHX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRSHX_wrapper";
  let DecoderMethod = "AArch64Base_LDRSHX_offset_decode_wrapper";
}

def AArch64Base_LDRSHX_offsetAsInt64
    : AArch64Base_LDRSHX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRSHX_offset_predicate(Imm); }]>;

def AArch64Base_LDRSHX_offsetAsLabel : AArch64Base_LDRSHX_offset<OtherVT>;

class AArch64Base_LDRSWX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRSWX_wrapper";
  let DecoderMethod = "AArch64Base_LDRSWX_offset_decode_wrapper";
}

def AArch64Base_LDRSWX_offsetAsInt64
    : AArch64Base_LDRSWX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRSWX_offset_predicate(Imm); }]>;

def AArch64Base_LDRSWX_offsetAsLabel : AArch64Base_LDRSWX_offset<OtherVT>;

class AArch64Base_LDRW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRW_wrapper";
  let DecoderMethod = "AArch64Base_LDRW_offset_decode_wrapper";
}

def AArch64Base_LDRW_offsetAsInt64
    : AArch64Base_LDRW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRW_offset_predicate(Imm); }]>;

def AArch64Base_LDRW_offsetAsLabel : AArch64Base_LDRW_offset<OtherVT>;

class AArch64Base_LDRX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDRX_wrapper";
  let DecoderMethod = "AArch64Base_LDRX_offset_decode_wrapper";
}

def AArch64Base_LDRX_offsetAsInt64
    : AArch64Base_LDRX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDRX_offset_predicate(Imm); }]>;

def AArch64Base_LDRX_offsetAsLabel : AArch64Base_LDRX_offset<OtherVT>;

class AArch64Base_LDURB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURB_wrapper";
  let DecoderMethod = "AArch64Base_LDURB_offset_decode_wrapper";
}

def AArch64Base_LDURB_offsetAsInt64
    : AArch64Base_LDURB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURB_offset_predicate(Imm); }]>;

def AArch64Base_LDURB_offsetAsLabel : AArch64Base_LDURB_offset<OtherVT>;

class AArch64Base_LDURH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURH_wrapper";
  let DecoderMethod = "AArch64Base_LDURH_offset_decode_wrapper";
}

def AArch64Base_LDURH_offsetAsInt64
    : AArch64Base_LDURH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURH_offset_predicate(Imm); }]>;

def AArch64Base_LDURH_offsetAsLabel : AArch64Base_LDURH_offset<OtherVT>;

class AArch64Base_LDURSBW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURSBW_wrapper";
  let DecoderMethod = "AArch64Base_LDURSBW_offset_decode_wrapper";
}

def AArch64Base_LDURSBW_offsetAsInt64
    : AArch64Base_LDURSBW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURSBW_offset_predicate(Imm); }]>;

def AArch64Base_LDURSBW_offsetAsLabel : AArch64Base_LDURSBW_offset<OtherVT>;

class AArch64Base_LDURSBX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURSBX_wrapper";
  let DecoderMethod = "AArch64Base_LDURSBX_offset_decode_wrapper";
}

def AArch64Base_LDURSBX_offsetAsInt64
    : AArch64Base_LDURSBX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURSBX_offset_predicate(Imm); }]>;

def AArch64Base_LDURSBX_offsetAsLabel : AArch64Base_LDURSBX_offset<OtherVT>;

class AArch64Base_LDURSHW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURSHW_wrapper";
  let DecoderMethod = "AArch64Base_LDURSHW_offset_decode_wrapper";
}

def AArch64Base_LDURSHW_offsetAsInt64
    : AArch64Base_LDURSHW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURSHW_offset_predicate(Imm); }]>;

def AArch64Base_LDURSHW_offsetAsLabel : AArch64Base_LDURSHW_offset<OtherVT>;

class AArch64Base_LDURSHX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURSHX_wrapper";
  let DecoderMethod = "AArch64Base_LDURSHX_offset_decode_wrapper";
}

def AArch64Base_LDURSHX_offsetAsInt64
    : AArch64Base_LDURSHX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURSHX_offset_predicate(Imm); }]>;

def AArch64Base_LDURSHX_offsetAsLabel : AArch64Base_LDURSHX_offset<OtherVT>;

class AArch64Base_LDURSWX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURSWX_wrapper";
  let DecoderMethod = "AArch64Base_LDURSWX_offset_decode_wrapper";
}

def AArch64Base_LDURSWX_offsetAsInt64
    : AArch64Base_LDURSWX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURSWX_offset_predicate(Imm); }]>;

def AArch64Base_LDURSWX_offsetAsLabel : AArch64Base_LDURSWX_offset<OtherVT>;

class AArch64Base_LDURW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURW_wrapper";
  let DecoderMethod = "AArch64Base_LDURW_offset_decode_wrapper";
}

def AArch64Base_LDURW_offsetAsInt64
    : AArch64Base_LDURW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURW_offset_predicate(Imm); }]>;

def AArch64Base_LDURW_offsetAsLabel : AArch64Base_LDURW_offset<OtherVT>;

class AArch64Base_LDURX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_LDURX_wrapper";
  let DecoderMethod = "AArch64Base_LDURX_offset_decode_wrapper";
}

def AArch64Base_LDURX_offsetAsInt64
    : AArch64Base_LDURX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_LDURX_offset_predicate(Imm); }]>;

def AArch64Base_LDURX_offsetAsLabel : AArch64Base_LDURX_offset<OtherVT>;

class AArch64Base_MOVKWPos00_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVKWPos00_wrapper";
  let DecoderMethod = "AArch64Base_MOVKWPos00_imm16_decode_wrapper";
}

def AArch64Base_MOVKWPos00_imm16AsInt32
    : AArch64Base_MOVKWPos00_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVKWPos00_imm16_predicate(Imm); }]>;

def AArch64Base_MOVKWPos00_imm16AsLabel : AArch64Base_MOVKWPos00_imm16<OtherVT>;

class AArch64Base_MOVKWPos16_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVKWPos16_wrapper";
  let DecoderMethod = "AArch64Base_MOVKWPos16_imm16_decode_wrapper";
}

def AArch64Base_MOVKWPos16_imm16AsInt32
    : AArch64Base_MOVKWPos16_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVKWPos16_imm16_predicate(Imm); }]>;

def AArch64Base_MOVKWPos16_imm16AsLabel : AArch64Base_MOVKWPos16_imm16<OtherVT>;

class AArch64Base_MOVKXPos00_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVKXPos00_wrapper";
  let DecoderMethod = "AArch64Base_MOVKXPos00_imm16_decode_wrapper";
}

def AArch64Base_MOVKXPos00_imm16AsInt32
    : AArch64Base_MOVKXPos00_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVKXPos00_imm16_predicate(Imm); }]>;

def AArch64Base_MOVKXPos00_imm16AsLabel : AArch64Base_MOVKXPos00_imm16<OtherVT>;

class AArch64Base_MOVKXPos16_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVKXPos16_wrapper";
  let DecoderMethod = "AArch64Base_MOVKXPos16_imm16_decode_wrapper";
}

def AArch64Base_MOVKXPos16_imm16AsInt32
    : AArch64Base_MOVKXPos16_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVKXPos16_imm16_predicate(Imm); }]>;

def AArch64Base_MOVKXPos16_imm16AsLabel : AArch64Base_MOVKXPos16_imm16<OtherVT>;

class AArch64Base_MOVKXPos32_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVKXPos32_wrapper";
  let DecoderMethod = "AArch64Base_MOVKXPos32_imm16_decode_wrapper";
}

def AArch64Base_MOVKXPos32_imm16AsInt32
    : AArch64Base_MOVKXPos32_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVKXPos32_imm16_predicate(Imm); }]>;

def AArch64Base_MOVKXPos32_imm16AsLabel : AArch64Base_MOVKXPos32_imm16<OtherVT>;

class AArch64Base_MOVKXPos48_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVKXPos48_wrapper";
  let DecoderMethod = "AArch64Base_MOVKXPos48_imm16_decode_wrapper";
}

def AArch64Base_MOVKXPos48_imm16AsInt32
    : AArch64Base_MOVKXPos48_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVKXPos48_imm16_predicate(Imm); }]>;

def AArch64Base_MOVKXPos48_imm16AsLabel : AArch64Base_MOVKXPos48_imm16<OtherVT>;

class AArch64Base_MOVNWPos00_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVNWPos00_wrapper";
  let DecoderMethod = "AArch64Base_MOVNWPos00_imm16_decode_wrapper";
}

def AArch64Base_MOVNWPos00_imm16AsInt32
    : AArch64Base_MOVNWPos00_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVNWPos00_imm16_predicate(Imm); }]>;

def AArch64Base_MOVNWPos00_imm16AsLabel : AArch64Base_MOVNWPos00_imm16<OtherVT>;

class AArch64Base_MOVNWPos16_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVNWPos16_wrapper";
  let DecoderMethod = "AArch64Base_MOVNWPos16_imm16_decode_wrapper";
}

def AArch64Base_MOVNWPos16_imm16AsInt32
    : AArch64Base_MOVNWPos16_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVNWPos16_imm16_predicate(Imm); }]>;

def AArch64Base_MOVNWPos16_imm16AsLabel : AArch64Base_MOVNWPos16_imm16<OtherVT>;

class AArch64Base_MOVNXPos00_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVNXPos00_wrapper";
  let DecoderMethod = "AArch64Base_MOVNXPos00_imm16_decode_wrapper";
}

def AArch64Base_MOVNXPos00_imm16AsInt32
    : AArch64Base_MOVNXPos00_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVNXPos00_imm16_predicate(Imm); }]>;

def AArch64Base_MOVNXPos00_imm16AsLabel : AArch64Base_MOVNXPos00_imm16<OtherVT>;

class AArch64Base_MOVNXPos16_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVNXPos16_wrapper";
  let DecoderMethod = "AArch64Base_MOVNXPos16_imm16_decode_wrapper";
}

def AArch64Base_MOVNXPos16_imm16AsInt32
    : AArch64Base_MOVNXPos16_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVNXPos16_imm16_predicate(Imm); }]>;

def AArch64Base_MOVNXPos16_imm16AsLabel : AArch64Base_MOVNXPos16_imm16<OtherVT>;

class AArch64Base_MOVNXPos32_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVNXPos32_wrapper";
  let DecoderMethod = "AArch64Base_MOVNXPos32_imm16_decode_wrapper";
}

def AArch64Base_MOVNXPos32_imm16AsInt32
    : AArch64Base_MOVNXPos32_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVNXPos32_imm16_predicate(Imm); }]>;

def AArch64Base_MOVNXPos32_imm16AsLabel : AArch64Base_MOVNXPos32_imm16<OtherVT>;

class AArch64Base_MOVNXPos48_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVNXPos48_wrapper";
  let DecoderMethod = "AArch64Base_MOVNXPos48_imm16_decode_wrapper";
}

def AArch64Base_MOVNXPos48_imm16AsInt32
    : AArch64Base_MOVNXPos48_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVNXPos48_imm16_predicate(Imm); }]>;

def AArch64Base_MOVNXPos48_imm16AsLabel : AArch64Base_MOVNXPos48_imm16<OtherVT>;

class AArch64Base_MOVZWPos00_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVZWPos00_wrapper";
  let DecoderMethod = "AArch64Base_MOVZWPos00_imm16_decode_wrapper";
}

def AArch64Base_MOVZWPos00_imm16AsInt32
    : AArch64Base_MOVZWPos00_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVZWPos00_imm16_predicate(Imm); }]>;

def AArch64Base_MOVZWPos00_imm16AsLabel : AArch64Base_MOVZWPos00_imm16<OtherVT>;

class AArch64Base_MOVZWPos16_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVZWPos16_wrapper";
  let DecoderMethod = "AArch64Base_MOVZWPos16_imm16_decode_wrapper";
}

def AArch64Base_MOVZWPos16_imm16AsInt32
    : AArch64Base_MOVZWPos16_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVZWPos16_imm16_predicate(Imm); }]>;

def AArch64Base_MOVZWPos16_imm16AsLabel : AArch64Base_MOVZWPos16_imm16<OtherVT>;

class AArch64Base_MOVZXPos00_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVZXPos00_wrapper";
  let DecoderMethod = "AArch64Base_MOVZXPos00_imm16_decode_wrapper";
}

def AArch64Base_MOVZXPos00_imm16AsInt32
    : AArch64Base_MOVZXPos00_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVZXPos00_imm16_predicate(Imm); }]>;

def AArch64Base_MOVZXPos00_imm16AsLabel : AArch64Base_MOVZXPos00_imm16<OtherVT>;

class AArch64Base_MOVZXPos16_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVZXPos16_wrapper";
  let DecoderMethod = "AArch64Base_MOVZXPos16_imm16_decode_wrapper";
}

def AArch64Base_MOVZXPos16_imm16AsInt32
    : AArch64Base_MOVZXPos16_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVZXPos16_imm16_predicate(Imm); }]>;

def AArch64Base_MOVZXPos16_imm16AsLabel : AArch64Base_MOVZXPos16_imm16<OtherVT>;

class AArch64Base_MOVZXPos32_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVZXPos32_wrapper";
  let DecoderMethod = "AArch64Base_MOVZXPos32_imm16_decode_wrapper";
}

def AArch64Base_MOVZXPos32_imm16AsInt32
    : AArch64Base_MOVZXPos32_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVZXPos32_imm16_predicate(Imm); }]>;

def AArch64Base_MOVZXPos32_imm16AsLabel : AArch64Base_MOVZXPos32_imm16<OtherVT>;

class AArch64Base_MOVZXPos48_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MOVZXPos48_wrapper";
  let DecoderMethod = "AArch64Base_MOVZXPos48_imm16_decode_wrapper";
}

def AArch64Base_MOVZXPos48_imm16AsInt32
    : AArch64Base_MOVZXPos48_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MOVZXPos48_imm16_predicate(Imm); }]>;

def AArch64Base_MOVZXPos48_imm16AsLabel : AArch64Base_MOVZXPos48_imm16<OtherVT>;

class AArch64Base_MRS_sysreg<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MRS_wrapper";
  let DecoderMethod = "AArch64Base_MRS_sysreg_decode_wrapper";
}

def AArch64Base_MRS_sysregAsInt32
    : AArch64Base_MRS_sysreg<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MRS_sysreg_predicate(Imm); }]>;

def AArch64Base_MRS_sysregAsLabel : AArch64Base_MRS_sysreg<OtherVT>;

class AArch64Base_MSR_sysreg<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_MSR_wrapper";
  let DecoderMethod = "AArch64Base_MSR_sysreg_decode_wrapper";
}

def AArch64Base_MSR_sysregAsInt32
    : AArch64Base_MSR_sysreg<i32>
    , ImmLeaf<i32, [{ return AArch64Base_MSR_sysreg_predicate(Imm); }]>;

def AArch64Base_MSR_sysregAsLabel : AArch64Base_MSR_sysreg<OtherVT>;

class AArch64Base_ORNWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNWASR_wrapper";
  let DecoderMethod = "AArch64Base_ORNWASR_imm6_decode_wrapper";
}

def AArch64Base_ORNWASR_imm6AsInt32
    : AArch64Base_ORNWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNWASR_imm6_predicate(Imm); }]>;

def AArch64Base_ORNWASR_imm6AsLabel : AArch64Base_ORNWASR_imm6<OtherVT>;

class AArch64Base_ORNWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNWLSL_wrapper";
  let DecoderMethod = "AArch64Base_ORNWLSL_imm6_decode_wrapper";
}

def AArch64Base_ORNWLSL_imm6AsInt32
    : AArch64Base_ORNWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ORNWLSL_imm6AsLabel : AArch64Base_ORNWLSL_imm6<OtherVT>;

class AArch64Base_ORNWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNWLSR_wrapper";
  let DecoderMethod = "AArch64Base_ORNWLSR_imm6_decode_wrapper";
}

def AArch64Base_ORNWLSR_imm6AsInt32
    : AArch64Base_ORNWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ORNWLSR_imm6AsLabel : AArch64Base_ORNWLSR_imm6<OtherVT>;

class AArch64Base_ORNWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNWROR_wrapper";
  let DecoderMethod = "AArch64Base_ORNWROR_imm6_decode_wrapper";
}

def AArch64Base_ORNWROR_imm6AsInt32
    : AArch64Base_ORNWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNWROR_imm6_predicate(Imm); }]>;

def AArch64Base_ORNWROR_imm6AsLabel : AArch64Base_ORNWROR_imm6<OtherVT>;

class AArch64Base_ORNXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNXASR_wrapper";
  let DecoderMethod = "AArch64Base_ORNXASR_imm6_decode_wrapper";
}

def AArch64Base_ORNXASR_imm6AsInt32
    : AArch64Base_ORNXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNXASR_imm6_predicate(Imm); }]>;

def AArch64Base_ORNXASR_imm6AsLabel : AArch64Base_ORNXASR_imm6<OtherVT>;

class AArch64Base_ORNXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNXLSL_wrapper";
  let DecoderMethod = "AArch64Base_ORNXLSL_imm6_decode_wrapper";
}

def AArch64Base_ORNXLSL_imm6AsInt32
    : AArch64Base_ORNXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ORNXLSL_imm6AsLabel : AArch64Base_ORNXLSL_imm6<OtherVT>;

class AArch64Base_ORNXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNXLSR_wrapper";
  let DecoderMethod = "AArch64Base_ORNXLSR_imm6_decode_wrapper";
}

def AArch64Base_ORNXLSR_imm6AsInt32
    : AArch64Base_ORNXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ORNXLSR_imm6AsLabel : AArch64Base_ORNXLSR_imm6<OtherVT>;

class AArch64Base_ORNXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORNXROR_wrapper";
  let DecoderMethod = "AArch64Base_ORNXROR_imm6_decode_wrapper";
}

def AArch64Base_ORNXROR_imm6AsInt32
    : AArch64Base_ORNXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORNXROR_imm6_predicate(Imm); }]>;

def AArch64Base_ORNXROR_imm6AsLabel : AArch64Base_ORNXROR_imm6<OtherVT>;

class AArch64Base_ORRIW_immWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRIW_wrapper";
  let DecoderMethod = "AArch64Base_ORRIW_immWSize_decode_wrapper";
}

def AArch64Base_ORRIW_immWSizeAsInt32
    : AArch64Base_ORRIW_immWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRIW_immWSize_predicate(Imm); }]>;

def AArch64Base_ORRIW_immWSizeAsLabel : AArch64Base_ORRIW_immWSize<OtherVT>;

class AArch64Base_ORRIX_immXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRIX_wrapper";
  let DecoderMethod = "AArch64Base_ORRIX_immXSize_decode_wrapper";
}

def AArch64Base_ORRIX_immXSizeAsInt64
    : AArch64Base_ORRIX_immXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_ORRIX_immXSize_predicate(Imm); }]>;

def AArch64Base_ORRIX_immXSizeAsLabel : AArch64Base_ORRIX_immXSize<OtherVT>;

class AArch64Base_ORRWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRWASR_wrapper";
  let DecoderMethod = "AArch64Base_ORRWASR_imm6_decode_wrapper";
}

def AArch64Base_ORRWASR_imm6AsInt32
    : AArch64Base_ORRWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRWASR_imm6_predicate(Imm); }]>;

def AArch64Base_ORRWASR_imm6AsLabel : AArch64Base_ORRWASR_imm6<OtherVT>;

class AArch64Base_ORRWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRWLSL_wrapper";
  let DecoderMethod = "AArch64Base_ORRWLSL_imm6_decode_wrapper";
}

def AArch64Base_ORRWLSL_imm6AsInt32
    : AArch64Base_ORRWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ORRWLSL_imm6AsLabel : AArch64Base_ORRWLSL_imm6<OtherVT>;

class AArch64Base_ORRWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRWLSR_wrapper";
  let DecoderMethod = "AArch64Base_ORRWLSR_imm6_decode_wrapper";
}

def AArch64Base_ORRWLSR_imm6AsInt32
    : AArch64Base_ORRWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ORRWLSR_imm6AsLabel : AArch64Base_ORRWLSR_imm6<OtherVT>;

class AArch64Base_ORRWROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRWROR_wrapper";
  let DecoderMethod = "AArch64Base_ORRWROR_imm6_decode_wrapper";
}

def AArch64Base_ORRWROR_imm6AsInt32
    : AArch64Base_ORRWROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRWROR_imm6_predicate(Imm); }]>;

def AArch64Base_ORRWROR_imm6AsLabel : AArch64Base_ORRWROR_imm6<OtherVT>;

class AArch64Base_ORRXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRXASR_wrapper";
  let DecoderMethod = "AArch64Base_ORRXASR_imm6_decode_wrapper";
}

def AArch64Base_ORRXASR_imm6AsInt32
    : AArch64Base_ORRXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRXASR_imm6_predicate(Imm); }]>;

def AArch64Base_ORRXASR_imm6AsLabel : AArch64Base_ORRXASR_imm6<OtherVT>;

class AArch64Base_ORRXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRXLSL_wrapper";
  let DecoderMethod = "AArch64Base_ORRXLSL_imm6_decode_wrapper";
}

def AArch64Base_ORRXLSL_imm6AsInt32
    : AArch64Base_ORRXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_ORRXLSL_imm6AsLabel : AArch64Base_ORRXLSL_imm6<OtherVT>;

class AArch64Base_ORRXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRXLSR_wrapper";
  let DecoderMethod = "AArch64Base_ORRXLSR_imm6_decode_wrapper";
}

def AArch64Base_ORRXLSR_imm6AsInt32
    : AArch64Base_ORRXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_ORRXLSR_imm6AsLabel : AArch64Base_ORRXLSR_imm6<OtherVT>;

class AArch64Base_ORRXROR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_ORRXROR_wrapper";
  let DecoderMethod = "AArch64Base_ORRXROR_imm6_decode_wrapper";
}

def AArch64Base_ORRXROR_imm6AsInt32
    : AArch64Base_ORRXROR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_ORRXROR_imm6_predicate(Imm); }]>;

def AArch64Base_ORRXROR_imm6AsLabel : AArch64Base_ORRXROR_imm6<OtherVT>;

class AArch64Base_SBFMW_leftWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SBFMW_wrapper";
  let DecoderMethod = "AArch64Base_SBFMW_leftWSize_decode_wrapper";
}

def AArch64Base_SBFMW_leftWSizeAsInt32
    : AArch64Base_SBFMW_leftWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SBFMW_leftWSize_predicate(Imm); }]>;

def AArch64Base_SBFMW_leftWSizeAsLabel : AArch64Base_SBFMW_leftWSize<OtherVT>;

class AArch64Base_SBFMW_rightWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SBFMW_wrapper";
  let DecoderMethod = "AArch64Base_SBFMW_rightWSize_decode_wrapper";
}

def AArch64Base_SBFMW_rightWSizeAsInt32
    : AArch64Base_SBFMW_rightWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SBFMW_rightWSize_predicate(Imm); }]>;

def AArch64Base_SBFMW_rightWSizeAsLabel : AArch64Base_SBFMW_rightWSize<OtherVT>;

class AArch64Base_SBFMX_leftXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SBFMX_wrapper";
  let DecoderMethod = "AArch64Base_SBFMX_leftXSize_decode_wrapper";
}

def AArch64Base_SBFMX_leftXSizeAsInt32
    : AArch64Base_SBFMX_leftXSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SBFMX_leftXSize_predicate(Imm); }]>;

def AArch64Base_SBFMX_leftXSizeAsLabel : AArch64Base_SBFMX_leftXSize<OtherVT>;

class AArch64Base_SBFMX_rightXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SBFMX_wrapper";
  let DecoderMethod = "AArch64Base_SBFMX_rightXSize_decode_wrapper";
}

def AArch64Base_SBFMX_rightXSizeAsInt32
    : AArch64Base_SBFMX_rightXSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SBFMX_rightXSize_predicate(Imm); }]>;

def AArch64Base_SBFMX_rightXSizeAsLabel : AArch64Base_SBFMX_rightXSize<OtherVT>;

class AArch64Base_STPW_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STPW_wrapper";
  let DecoderMethod = "AArch64Base_STPW_offWSize_decode_wrapper";
}

def AArch64Base_STPW_offWSizeAsInt64
    : AArch64Base_STPW_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STPW_offWSize_predicate(Imm); }]>;

def AArch64Base_STPW_offWSizeAsLabel : AArch64Base_STPW_offWSize<OtherVT>;

class AArch64Base_STPWPre_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STPWPre_wrapper";
  let DecoderMethod = "AArch64Base_STPWPre_offWSize_decode_wrapper";
}

def AArch64Base_STPWPre_offWSizeAsInt64
    : AArch64Base_STPWPre_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STPWPre_offWSize_predicate(Imm); }]>;

def AArch64Base_STPWPre_offWSizeAsLabel : AArch64Base_STPWPre_offWSize<OtherVT>;

class AArch64Base_STPWPst_offWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STPWPst_wrapper";
  let DecoderMethod = "AArch64Base_STPWPst_offWSize_decode_wrapper";
}

def AArch64Base_STPWPst_offWSizeAsInt64
    : AArch64Base_STPWPst_offWSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STPWPst_offWSize_predicate(Imm); }]>;

def AArch64Base_STPWPst_offWSizeAsLabel : AArch64Base_STPWPst_offWSize<OtherVT>;

class AArch64Base_STPX_offXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STPX_wrapper";
  let DecoderMethod = "AArch64Base_STPX_offXSize_decode_wrapper";
}

def AArch64Base_STPX_offXSizeAsInt64
    : AArch64Base_STPX_offXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STPX_offXSize_predicate(Imm); }]>;

def AArch64Base_STPX_offXSizeAsLabel : AArch64Base_STPX_offXSize<OtherVT>;

class AArch64Base_STPXPre_offXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STPXPre_wrapper";
  let DecoderMethod = "AArch64Base_STPXPre_offXSize_decode_wrapper";
}

def AArch64Base_STPXPre_offXSizeAsInt64
    : AArch64Base_STPXPre_offXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STPXPre_offXSize_predicate(Imm); }]>;

def AArch64Base_STPXPre_offXSizeAsLabel : AArch64Base_STPXPre_offXSize<OtherVT>;

class AArch64Base_STPXPst_offXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STPXPst_wrapper";
  let DecoderMethod = "AArch64Base_STPXPst_offXSize_decode_wrapper";
}

def AArch64Base_STPXPst_offXSizeAsInt64
    : AArch64Base_STPXPst_offXSize<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STPXPst_offXSize_predicate(Imm); }]>;

def AArch64Base_STPXPst_offXSizeAsLabel : AArch64Base_STPXPst_offXSize<OtherVT>;

class AArch64Base_STRB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRB_wrapper";
  let DecoderMethod = "AArch64Base_STRB_offset_decode_wrapper";
}

def AArch64Base_STRB_offsetAsInt64
    : AArch64Base_STRB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRB_offset_predicate(Imm); }]>;

def AArch64Base_STRB_offsetAsLabel : AArch64Base_STRB_offset<OtherVT>;

class AArch64Base_STRH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRH_wrapper";
  let DecoderMethod = "AArch64Base_STRH_offset_decode_wrapper";
}

def AArch64Base_STRH_offsetAsInt64
    : AArch64Base_STRH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRH_offset_predicate(Imm); }]>;

def AArch64Base_STRH_offsetAsLabel : AArch64Base_STRH_offset<OtherVT>;

class AArch64Base_STRPreB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPreB_wrapper";
  let DecoderMethod = "AArch64Base_STRPreB_offset_decode_wrapper";
}

def AArch64Base_STRPreB_offsetAsInt64
    : AArch64Base_STRPreB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPreB_offset_predicate(Imm); }]>;

def AArch64Base_STRPreB_offsetAsLabel : AArch64Base_STRPreB_offset<OtherVT>;

class AArch64Base_STRPreH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPreH_wrapper";
  let DecoderMethod = "AArch64Base_STRPreH_offset_decode_wrapper";
}

def AArch64Base_STRPreH_offsetAsInt64
    : AArch64Base_STRPreH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPreH_offset_predicate(Imm); }]>;

def AArch64Base_STRPreH_offsetAsLabel : AArch64Base_STRPreH_offset<OtherVT>;

class AArch64Base_STRPreW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPreW_wrapper";
  let DecoderMethod = "AArch64Base_STRPreW_offset_decode_wrapper";
}

def AArch64Base_STRPreW_offsetAsInt64
    : AArch64Base_STRPreW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPreW_offset_predicate(Imm); }]>;

def AArch64Base_STRPreW_offsetAsLabel : AArch64Base_STRPreW_offset<OtherVT>;

class AArch64Base_STRPreX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPreX_wrapper";
  let DecoderMethod = "AArch64Base_STRPreX_offset_decode_wrapper";
}

def AArch64Base_STRPreX_offsetAsInt64
    : AArch64Base_STRPreX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPreX_offset_predicate(Imm); }]>;

def AArch64Base_STRPreX_offsetAsLabel : AArch64Base_STRPreX_offset<OtherVT>;

class AArch64Base_STRPstB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPstB_wrapper";
  let DecoderMethod = "AArch64Base_STRPstB_offset_decode_wrapper";
}

def AArch64Base_STRPstB_offsetAsInt64
    : AArch64Base_STRPstB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPstB_offset_predicate(Imm); }]>;

def AArch64Base_STRPstB_offsetAsLabel : AArch64Base_STRPstB_offset<OtherVT>;

class AArch64Base_STRPstH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPstH_wrapper";
  let DecoderMethod = "AArch64Base_STRPstH_offset_decode_wrapper";
}

def AArch64Base_STRPstH_offsetAsInt64
    : AArch64Base_STRPstH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPstH_offset_predicate(Imm); }]>;

def AArch64Base_STRPstH_offsetAsLabel : AArch64Base_STRPstH_offset<OtherVT>;

class AArch64Base_STRPstW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPstW_wrapper";
  let DecoderMethod = "AArch64Base_STRPstW_offset_decode_wrapper";
}

def AArch64Base_STRPstW_offsetAsInt64
    : AArch64Base_STRPstW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPstW_offset_predicate(Imm); }]>;

def AArch64Base_STRPstW_offsetAsLabel : AArch64Base_STRPstW_offset<OtherVT>;

class AArch64Base_STRPstX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRPstX_wrapper";
  let DecoderMethod = "AArch64Base_STRPstX_offset_decode_wrapper";
}

def AArch64Base_STRPstX_offsetAsInt64
    : AArch64Base_STRPstX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRPstX_offset_predicate(Imm); }]>;

def AArch64Base_STRPstX_offsetAsLabel : AArch64Base_STRPstX_offset<OtherVT>;

class AArch64Base_STRW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRW_wrapper";
  let DecoderMethod = "AArch64Base_STRW_offset_decode_wrapper";
}

def AArch64Base_STRW_offsetAsInt64
    : AArch64Base_STRW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRW_offset_predicate(Imm); }]>;

def AArch64Base_STRW_offsetAsLabel : AArch64Base_STRW_offset<OtherVT>;

class AArch64Base_STRX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STRX_wrapper";
  let DecoderMethod = "AArch64Base_STRX_offset_decode_wrapper";
}

def AArch64Base_STRX_offsetAsInt64
    : AArch64Base_STRX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STRX_offset_predicate(Imm); }]>;

def AArch64Base_STRX_offsetAsLabel : AArch64Base_STRX_offset<OtherVT>;

class AArch64Base_STURB_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STURB_wrapper";
  let DecoderMethod = "AArch64Base_STURB_offset_decode_wrapper";
}

def AArch64Base_STURB_offsetAsInt64
    : AArch64Base_STURB_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STURB_offset_predicate(Imm); }]>;

def AArch64Base_STURB_offsetAsLabel : AArch64Base_STURB_offset<OtherVT>;

class AArch64Base_STURH_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STURH_wrapper";
  let DecoderMethod = "AArch64Base_STURH_offset_decode_wrapper";
}

def AArch64Base_STURH_offsetAsInt64
    : AArch64Base_STURH_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STURH_offset_predicate(Imm); }]>;

def AArch64Base_STURH_offsetAsLabel : AArch64Base_STURH_offset<OtherVT>;

class AArch64Base_STURW_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STURW_wrapper";
  let DecoderMethod = "AArch64Base_STURW_offset_decode_wrapper";
}

def AArch64Base_STURW_offsetAsInt64
    : AArch64Base_STURW_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STURW_offset_predicate(Imm); }]>;

def AArch64Base_STURW_offsetAsLabel : AArch64Base_STURW_offset<OtherVT>;

class AArch64Base_STURX_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_STURX_wrapper";
  let DecoderMethod = "AArch64Base_STURX_offset_decode_wrapper";
}

def AArch64Base_STURX_offsetAsInt64
    : AArch64Base_STURX_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_STURX_offset_predicate(Imm); }]>;

def AArch64Base_STURX_offsetAsLabel : AArch64Base_STURX_offset<OtherVT>;

class AArch64Base_SUBWASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWASR_wrapper";
  let DecoderMethod = "AArch64Base_SUBWASR_imm6_decode_wrapper";
}

def AArch64Base_SUBWASR_imm6AsInt32
    : AArch64Base_SUBWASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWASR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBWASR_imm6AsLabel : AArch64Base_SUBWASR_imm6<OtherVT>;

class AArch64Base_SUBWI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWI_wrapper";
  let DecoderMethod = "AArch64Base_SUBWI_imm12X_decode_wrapper";
}

def AArch64Base_SUBWI_imm12XAsInt64
    : AArch64Base_SUBWI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBWI_imm12X_predicate(Imm); }]>;

def AArch64Base_SUBWI_imm12XAsLabel : AArch64Base_SUBWI_imm12X<OtherVT>;

class AArch64Base_SUBWI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWI12_wrapper";
  let DecoderMethod = "AArch64Base_SUBWI12_imm12S_decode_wrapper";
}

def AArch64Base_SUBWI12_imm12SAsInt64
    : AArch64Base_SUBWI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBWI12_imm12S_predicate(Imm); }]>;

def AArch64Base_SUBWI12_imm12SAsLabel : AArch64Base_SUBWI12_imm12S<OtherVT>;

class AArch64Base_SUBWLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWLSL_wrapper";
  let DecoderMethod = "AArch64Base_SUBWLSL_imm6_decode_wrapper";
}

def AArch64Base_SUBWLSL_imm6AsInt32
    : AArch64Base_SUBWLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWLSL_imm6_predicate(Imm); }]>;

def AArch64Base_SUBWLSL_imm6AsLabel : AArch64Base_SUBWLSL_imm6<OtherVT>;

class AArch64Base_SUBWLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWLSR_wrapper";
  let DecoderMethod = "AArch64Base_SUBWLSR_imm6_decode_wrapper";
}

def AArch64Base_SUBWLSR_imm6AsInt32
    : AArch64Base_SUBWLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWLSR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBWLSR_imm6AsLabel : AArch64Base_SUBWLSR_imm6<OtherVT>;

class AArch64Base_SUBWSASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSASR_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSASR_imm6_decode_wrapper";
}

def AArch64Base_SUBWSASR_imm6AsInt32
    : AArch64Base_SUBWSASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSASR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBWSASR_imm6AsLabel : AArch64Base_SUBWSASR_imm6<OtherVT>;

class AArch64Base_SUBWSI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSI_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSI_imm12X_decode_wrapper";
}

def AArch64Base_SUBWSI_imm12XAsInt64
    : AArch64Base_SUBWSI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBWSI_imm12X_predicate(Imm); }]>;

def AArch64Base_SUBWSI_imm12XAsLabel : AArch64Base_SUBWSI_imm12X<OtherVT>;

class AArch64Base_SUBWSI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSI12_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSI12_imm12S_decode_wrapper";
}

def AArch64Base_SUBWSI12_imm12SAsInt64
    : AArch64Base_SUBWSI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBWSI12_imm12S_predicate(Imm); }]>;

def AArch64Base_SUBWSI12_imm12SAsLabel : AArch64Base_SUBWSI12_imm12S<OtherVT>;

class AArch64Base_SUBWSLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSLSL_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSLSL_imm6_decode_wrapper";
}

def AArch64Base_SUBWSLSL_imm6AsInt32
    : AArch64Base_SUBWSLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSLSL_imm6_predicate(Imm); }]>;

def AArch64Base_SUBWSLSL_imm6AsLabel : AArch64Base_SUBWSLSL_imm6<OtherVT>;

class AArch64Base_SUBWSLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSLSR_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSLSR_imm6_decode_wrapper";
}

def AArch64Base_SUBWSLSR_imm6AsInt32
    : AArch64Base_SUBWSLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSLSR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBWSLSR_imm6AsLabel : AArch64Base_SUBWSLSR_imm6<OtherVT>;

class AArch64Base_SUBWSSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSSXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSSXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBWSSXSB_imm3AsInt32
    : AArch64Base_SUBWSSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSSXSB_imm3AsLabel : AArch64Base_SUBWSSXSB_imm3<OtherVT>;

class AArch64Base_SUBWSSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSSXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSSXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBWSSXSH_imm3AsInt32
    : AArch64Base_SUBWSSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSSXSH_imm3AsLabel : AArch64Base_SUBWSSXSH_imm3<OtherVT>;

class AArch64Base_SUBWSSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSSXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSSXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBWSSXSW_imm3AsInt32
    : AArch64Base_SUBWSSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSSXSW_imm3AsLabel : AArch64Base_SUBWSSXSW_imm3<OtherVT>;

class AArch64Base_SUBWSSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSSXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSSXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBWSSXSX_imm3AsInt32
    : AArch64Base_SUBWSSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSSXSX_imm3AsLabel : AArch64Base_SUBWSSXSX_imm3<OtherVT>;

class AArch64Base_SUBWSUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSUXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSUXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBWSUXSB_imm3AsInt32
    : AArch64Base_SUBWSUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSUXSB_imm3AsLabel : AArch64Base_SUBWSUXSB_imm3<OtherVT>;

class AArch64Base_SUBWSUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSUXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSUXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBWSUXSH_imm3AsInt32
    : AArch64Base_SUBWSUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSUXSH_imm3AsLabel : AArch64Base_SUBWSUXSH_imm3<OtherVT>;

class AArch64Base_SUBWSUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSUXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSUXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBWSUXSW_imm3AsInt32
    : AArch64Base_SUBWSUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSUXSW_imm3AsLabel : AArch64Base_SUBWSUXSW_imm3<OtherVT>;

class AArch64Base_SUBWSUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSUXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSUXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBWSUXSX_imm3AsInt32
    : AArch64Base_SUBWSUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSUXSX_imm3AsLabel : AArch64Base_SUBWSUXSX_imm3<OtherVT>;

class AArch64Base_SUBWSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBWSXSB_imm3AsInt32
    : AArch64Base_SUBWSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSXSB_imm3AsLabel : AArch64Base_SUBWSXSB_imm3<OtherVT>;

class AArch64Base_SUBWSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBWSXSH_imm3AsInt32
    : AArch64Base_SUBWSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSXSH_imm3AsLabel : AArch64Base_SUBWSXSH_imm3<OtherVT>;

class AArch64Base_SUBWSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBWSXSW_imm3AsInt32
    : AArch64Base_SUBWSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSXSW_imm3AsLabel : AArch64Base_SUBWSXSW_imm3<OtherVT>;

class AArch64Base_SUBWSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWSXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBWSXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBWSXSX_imm3AsInt32
    : AArch64Base_SUBWSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWSXSX_imm3AsLabel : AArch64Base_SUBWSXSX_imm3<OtherVT>;

class AArch64Base_SUBWUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWUXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBWUXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBWUXSB_imm3AsInt32
    : AArch64Base_SUBWUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWUXSB_imm3AsLabel : AArch64Base_SUBWUXSB_imm3<OtherVT>;

class AArch64Base_SUBWUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWUXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBWUXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBWUXSH_imm3AsInt32
    : AArch64Base_SUBWUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWUXSH_imm3AsLabel : AArch64Base_SUBWUXSH_imm3<OtherVT>;

class AArch64Base_SUBWUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWUXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBWUXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBWUXSW_imm3AsInt32
    : AArch64Base_SUBWUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWUXSW_imm3AsLabel : AArch64Base_SUBWUXSW_imm3<OtherVT>;

class AArch64Base_SUBWUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBWUXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBWUXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBWUXSX_imm3AsInt32
    : AArch64Base_SUBWUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBWUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBWUXSX_imm3AsLabel : AArch64Base_SUBWUXSX_imm3<OtherVT>;

class AArch64Base_SUBXASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXASR_wrapper";
  let DecoderMethod = "AArch64Base_SUBXASR_imm6_decode_wrapper";
}

def AArch64Base_SUBXASR_imm6AsInt32
    : AArch64Base_SUBXASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXASR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBXASR_imm6AsLabel : AArch64Base_SUBXASR_imm6<OtherVT>;

class AArch64Base_SUBXI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXI_wrapper";
  let DecoderMethod = "AArch64Base_SUBXI_imm12X_decode_wrapper";
}

def AArch64Base_SUBXI_imm12XAsInt64
    : AArch64Base_SUBXI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBXI_imm12X_predicate(Imm); }]>;

def AArch64Base_SUBXI_imm12XAsLabel : AArch64Base_SUBXI_imm12X<OtherVT>;

class AArch64Base_SUBXI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXI12_wrapper";
  let DecoderMethod = "AArch64Base_SUBXI12_imm12S_decode_wrapper";
}

def AArch64Base_SUBXI12_imm12SAsInt64
    : AArch64Base_SUBXI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBXI12_imm12S_predicate(Imm); }]>;

def AArch64Base_SUBXI12_imm12SAsLabel : AArch64Base_SUBXI12_imm12S<OtherVT>;

class AArch64Base_SUBXLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXLSL_wrapper";
  let DecoderMethod = "AArch64Base_SUBXLSL_imm6_decode_wrapper";
}

def AArch64Base_SUBXLSL_imm6AsInt32
    : AArch64Base_SUBXLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXLSL_imm6_predicate(Imm); }]>;

def AArch64Base_SUBXLSL_imm6AsLabel : AArch64Base_SUBXLSL_imm6<OtherVT>;

class AArch64Base_SUBXLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXLSR_wrapper";
  let DecoderMethod = "AArch64Base_SUBXLSR_imm6_decode_wrapper";
}

def AArch64Base_SUBXLSR_imm6AsInt32
    : AArch64Base_SUBXLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXLSR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBXLSR_imm6AsLabel : AArch64Base_SUBXLSR_imm6<OtherVT>;

class AArch64Base_SUBXSASR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSASR_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSASR_imm6_decode_wrapper";
}

def AArch64Base_SUBXSASR_imm6AsInt32
    : AArch64Base_SUBXSASR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSASR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBXSASR_imm6AsLabel : AArch64Base_SUBXSASR_imm6<OtherVT>;

class AArch64Base_SUBXSI_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSI_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSI_imm12X_decode_wrapper";
}

def AArch64Base_SUBXSI_imm12XAsInt64
    : AArch64Base_SUBXSI_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBXSI_imm12X_predicate(Imm); }]>;

def AArch64Base_SUBXSI_imm12XAsLabel : AArch64Base_SUBXSI_imm12X<OtherVT>;

class AArch64Base_SUBXSI12_imm12S<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSI12_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSI12_imm12S_decode_wrapper";
}

def AArch64Base_SUBXSI12_imm12SAsInt64
    : AArch64Base_SUBXSI12_imm12S<i64>
    , ImmLeaf<i64, [{ return AArch64Base_SUBXSI12_imm12S_predicate(Imm); }]>;

def AArch64Base_SUBXSI12_imm12SAsLabel : AArch64Base_SUBXSI12_imm12S<OtherVT>;

class AArch64Base_SUBXSLSL_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSLSL_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSLSL_imm6_decode_wrapper";
}

def AArch64Base_SUBXSLSL_imm6AsInt32
    : AArch64Base_SUBXSLSL_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSLSL_imm6_predicate(Imm); }]>;

def AArch64Base_SUBXSLSL_imm6AsLabel : AArch64Base_SUBXSLSL_imm6<OtherVT>;

class AArch64Base_SUBXSLSR_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSLSR_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSLSR_imm6_decode_wrapper";
}

def AArch64Base_SUBXSLSR_imm6AsInt32
    : AArch64Base_SUBXSLSR_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSLSR_imm6_predicate(Imm); }]>;

def AArch64Base_SUBXSLSR_imm6AsLabel : AArch64Base_SUBXSLSR_imm6<OtherVT>;

class AArch64Base_SUBXSSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSSXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSSXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBXSSXSB_imm3AsInt32
    : AArch64Base_SUBXSSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSSXSB_imm3AsLabel : AArch64Base_SUBXSSXSB_imm3<OtherVT>;

class AArch64Base_SUBXSSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSSXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSSXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBXSSXSH_imm3AsInt32
    : AArch64Base_SUBXSSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSSXSH_imm3AsLabel : AArch64Base_SUBXSSXSH_imm3<OtherVT>;

class AArch64Base_SUBXSSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSSXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSSXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBXSSXSW_imm3AsInt32
    : AArch64Base_SUBXSSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSSXSW_imm3AsLabel : AArch64Base_SUBXSSXSW_imm3<OtherVT>;

class AArch64Base_SUBXSSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSSXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSSXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBXSSXSX_imm3AsInt32
    : AArch64Base_SUBXSSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSSXSX_imm3AsLabel : AArch64Base_SUBXSSXSX_imm3<OtherVT>;

class AArch64Base_SUBXSUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSUXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSUXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBXSUXSB_imm3AsInt32
    : AArch64Base_SUBXSUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSUXSB_imm3AsLabel : AArch64Base_SUBXSUXSB_imm3<OtherVT>;

class AArch64Base_SUBXSUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSUXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSUXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBXSUXSH_imm3AsInt32
    : AArch64Base_SUBXSUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSUXSH_imm3AsLabel : AArch64Base_SUBXSUXSH_imm3<OtherVT>;

class AArch64Base_SUBXSUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSUXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSUXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBXSUXSW_imm3AsInt32
    : AArch64Base_SUBXSUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSUXSW_imm3AsLabel : AArch64Base_SUBXSUXSW_imm3<OtherVT>;

class AArch64Base_SUBXSUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSUXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSUXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBXSUXSX_imm3AsInt32
    : AArch64Base_SUBXSUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSUXSX_imm3AsLabel : AArch64Base_SUBXSUXSX_imm3<OtherVT>;

class AArch64Base_SUBXSXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBXSXSB_imm3AsInt32
    : AArch64Base_SUBXSXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSXSB_imm3AsLabel : AArch64Base_SUBXSXSB_imm3<OtherVT>;

class AArch64Base_SUBXSXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBXSXSH_imm3AsInt32
    : AArch64Base_SUBXSXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSXSH_imm3AsLabel : AArch64Base_SUBXSXSH_imm3<OtherVT>;

class AArch64Base_SUBXSXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBXSXSW_imm3AsInt32
    : AArch64Base_SUBXSXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSXSW_imm3AsLabel : AArch64Base_SUBXSXSW_imm3<OtherVT>;

class AArch64Base_SUBXSXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXSXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBXSXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBXSXSX_imm3AsInt32
    : AArch64Base_SUBXSXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXSXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXSXSX_imm3AsLabel : AArch64Base_SUBXSXSX_imm3<OtherVT>;

class AArch64Base_SUBXUXSB_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXUXSB_wrapper";
  let DecoderMethod = "AArch64Base_SUBXUXSB_imm3_decode_wrapper";
}

def AArch64Base_SUBXUXSB_imm3AsInt32
    : AArch64Base_SUBXUXSB_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXUXSB_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXUXSB_imm3AsLabel : AArch64Base_SUBXUXSB_imm3<OtherVT>;

class AArch64Base_SUBXUXSH_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXUXSH_wrapper";
  let DecoderMethod = "AArch64Base_SUBXUXSH_imm3_decode_wrapper";
}

def AArch64Base_SUBXUXSH_imm3AsInt32
    : AArch64Base_SUBXUXSH_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXUXSH_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXUXSH_imm3AsLabel : AArch64Base_SUBXUXSH_imm3<OtherVT>;

class AArch64Base_SUBXUXSW_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXUXSW_wrapper";
  let DecoderMethod = "AArch64Base_SUBXUXSW_imm3_decode_wrapper";
}

def AArch64Base_SUBXUXSW_imm3AsInt32
    : AArch64Base_SUBXUXSW_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXUXSW_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXUXSW_imm3AsLabel : AArch64Base_SUBXUXSW_imm3<OtherVT>;

class AArch64Base_SUBXUXSX_imm3<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SUBXUXSX_wrapper";
  let DecoderMethod = "AArch64Base_SUBXUXSX_imm3_decode_wrapper";
}

def AArch64Base_SUBXUXSX_imm3AsInt32
    : AArch64Base_SUBXUXSX_imm3<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SUBXUXSX_imm3_predicate(Imm); }]>;

def AArch64Base_SUBXUXSX_imm3AsLabel : AArch64Base_SUBXUXSX_imm3<OtherVT>;

class AArch64Base_SVC_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_SVC_wrapper";
  let DecoderMethod = "AArch64Base_SVC_imm16_decode_wrapper";
}

def AArch64Base_SVC_imm16AsInt32
    : AArch64Base_SVC_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_SVC_imm16_predicate(Imm); }]>;

def AArch64Base_SVC_imm16AsLabel : AArch64Base_SVC_imm16<OtherVT>;

class AArch64Base_TBNZ_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_TBNZ_wrapper";
  let DecoderMethod = "AArch64Base_TBNZ_offset_decode_wrapper";
}

def AArch64Base_TBNZ_offsetAsInt64
    : AArch64Base_TBNZ_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_TBNZ_offset_predicate(Imm); }]>;

def AArch64Base_TBNZ_offsetAsLabel : AArch64Base_TBNZ_offset<OtherVT>;

class AArch64Base_TBNZ_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_TBNZ_wrapper";
  let DecoderMethod = "AArch64Base_TBNZ_imm6_decode_wrapper";
}

def AArch64Base_TBNZ_imm6AsInt32
    : AArch64Base_TBNZ_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_TBNZ_imm6_predicate(Imm); }]>;

def AArch64Base_TBNZ_imm6AsLabel : AArch64Base_TBNZ_imm6<OtherVT>;

class AArch64Base_TBZ_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_TBZ_wrapper";
  let DecoderMethod = "AArch64Base_TBZ_offset_decode_wrapper";
}

def AArch64Base_TBZ_offsetAsInt64
    : AArch64Base_TBZ_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Base_TBZ_offset_predicate(Imm); }]>;

def AArch64Base_TBZ_offsetAsLabel : AArch64Base_TBZ_offset<OtherVT>;

class AArch64Base_TBZ_imm6<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_TBZ_wrapper";
  let DecoderMethod = "AArch64Base_TBZ_imm6_decode_wrapper";
}

def AArch64Base_TBZ_imm6AsInt32
    : AArch64Base_TBZ_imm6<i32>
    , ImmLeaf<i32, [{ return AArch64Base_TBZ_imm6_predicate(Imm); }]>;

def AArch64Base_TBZ_imm6AsLabel : AArch64Base_TBZ_imm6<OtherVT>;

class AArch64Base_UBFMW_leftWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_UBFMW_wrapper";
  let DecoderMethod = "AArch64Base_UBFMW_leftWSize_decode_wrapper";
}

def AArch64Base_UBFMW_leftWSizeAsInt32
    : AArch64Base_UBFMW_leftWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_UBFMW_leftWSize_predicate(Imm); }]>;

def AArch64Base_UBFMW_leftWSizeAsLabel : AArch64Base_UBFMW_leftWSize<OtherVT>;

class AArch64Base_UBFMW_rightWSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_UBFMW_wrapper";
  let DecoderMethod = "AArch64Base_UBFMW_rightWSize_decode_wrapper";
}

def AArch64Base_UBFMW_rightWSizeAsInt32
    : AArch64Base_UBFMW_rightWSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_UBFMW_rightWSize_predicate(Imm); }]>;

def AArch64Base_UBFMW_rightWSizeAsLabel : AArch64Base_UBFMW_rightWSize<OtherVT>;

class AArch64Base_UBFMX_leftXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_UBFMX_wrapper";
  let DecoderMethod = "AArch64Base_UBFMX_leftXSize_decode_wrapper";
}

def AArch64Base_UBFMX_leftXSizeAsInt32
    : AArch64Base_UBFMX_leftXSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_UBFMX_leftXSize_predicate(Imm); }]>;

def AArch64Base_UBFMX_leftXSizeAsLabel : AArch64Base_UBFMX_leftXSize<OtherVT>;

class AArch64Base_UBFMX_rightXSize<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_UBFMX_wrapper";
  let DecoderMethod = "AArch64Base_UBFMX_rightXSize_decode_wrapper";
}

def AArch64Base_UBFMX_rightXSizeAsInt32
    : AArch64Base_UBFMX_rightXSize<i32>
    , ImmLeaf<i32, [{ return AArch64Base_UBFMX_rightXSize_predicate(Imm); }]>;

def AArch64Base_UBFMX_rightXSizeAsLabel : AArch64Base_UBFMX_rightXSize<OtherVT>;

class AArch64Base_UDF_imm16<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Base_UDF_wrapper";
  let DecoderMethod = "AArch64Base_UDF_imm16_decode_wrapper";
}

def AArch64Base_UDF_imm16AsInt32
    : AArch64Base_UDF_imm16<i32>
    , ImmLeaf<i32, [{ return AArch64Base_UDF_imm16_predicate(Imm); }]>;

def AArch64Base_UDF_imm16AsLabel : AArch64Base_UDF_imm16<OtherVT>;

class AArch64Temp_PLDR_offset<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Temp_PLDR_wrapper";
  let DecoderMethod = "AArch64Temp_PLDR_offset_decode_wrapper";
}

def AArch64Temp_PLDR_offsetAsInt64
    : AArch64Temp_PLDR_offset<i64>
    , ImmLeaf<i64, [{ return AArch64Temp_PLDR_offset_predicate(Imm); }]>;

def AArch64Temp_PLDR_offsetAsLabel : AArch64Temp_PLDR_offset<OtherVT>;

class AArch64Temp_PLDR_imm12X<ValueType ty> : Operand<ty>
{
  let EncoderMethod = "AArch64Temp_PLDR_wrapper";
  let DecoderMethod = "AArch64Temp_PLDR_imm12X_decode_wrapper";
}

def AArch64Temp_PLDR_imm12XAsInt64
    : AArch64Temp_PLDR_imm12X<i64>
    , ImmLeaf<i64, [{ return AArch64Temp_PLDR_imm12X_predicate(Imm); }]>;

def AArch64Temp_PLDR_imm12XAsLabel : AArch64Temp_PLDR_imm12X<OtherVT>;




def ADCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0111010000000000;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0111010000000000;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011010000000000;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011010000000000;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ADDWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_ADDWI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_ADDWI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ADDWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ADDWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ADDWSASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_ADDWSI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_ADDWSI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ADDWSLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ADDWSLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDWSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_ADDWUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDWUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ADDXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_ADDXI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_ADDXI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ADDXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ADDXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ADDXSASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_ADDXSI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_ADDXSI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0100010;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ADDXSLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ADDXSLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ADDXSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_ADDXUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADDXUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_ADR_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b010000;
bits<64> offset;
bits<64> rd;

let Inst{28-24} = op{4-0};
let Inst{31} = op{5};
let Inst{30-29} = offset{1-0};
let Inst{23-5} = offset{20-2};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ADRP : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_ADRP_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b110000;
bits<64> offset;
bits<64> rd;

let Inst{28-24} = op{4-0};
let Inst{31} = op{5};
let Inst{30-29} = offset{1-0};
let Inst{23-5} = offset{20-2};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins W:$rn, AArch64Base_ANDIW_immWSizeAsInt32:$immWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00100100;
bits<1> sf = 0b0;
bits<64> immWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immWSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins X:$rn, AArch64Base_ANDIX_immXSizeAsInt64:$immXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00100100;
bits<1> sf = 0b1;
bits<64> immXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immXSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDSIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, AArch64Base_ANDSIW_immWSizeAsInt32:$immWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11100100;
bits<1> sf = 0b0;
bits<64> immWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immWSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, AArch64Base_ANDSIX_immXSizeAsInt64:$immXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11100100;
bits<1> sf = 0b1;
bits<64> immXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immXSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDSWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDSWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDSWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDSWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDSXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDSXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDSXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDSXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDSXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010100;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def ANDW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ANDWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ANDXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ANDXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010100;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ASRW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ASRX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b000101;
bits<64> offset;

let Inst{31-26} = op{5-0};
let Inst{25-0} = offset{25-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 1;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BFMW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rd_6, W:$rn, AArch64Base_BFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_BFMW_rightWSizeAsInt32:$rightWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01100110;
bits<1> sf = 0b0;
bits<1> N = 0b0;
bits<64> rightWSize;
bits<64> leftWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22} = N{0};
let Inst{21-16} = rightWSize{5-0};
let Inst{15-10} = leftWSize{11-6};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_6";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BFMX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rd_7, X:$rn, AArch64Base_BFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_BFMX_rightXSizeAsInt32:$rightXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01100110;
bits<1> sf = 0b1;
bits<1> N = 0b1;
bits<64> rightXSize;
bits<64> leftXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22} = N{0};
let Inst{21-16} = rightXSize{5-0};
let Inst{15-10} = leftXSize{11-6};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_7";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICSWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICSWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICSWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICSWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICSXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICSXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICSXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICSXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICSXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b11010101;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def BICW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_BICWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BICXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_BICXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00010101;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_BL_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b100101;
bits<64> offset;

let Inst{31-26} = op{5-0};
let Inst{25-0} = offset{25-0};

let isTerminator       = 1;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ LR ];
}

def BLR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<27> op = 0b110101100011111100000000000;
bits<64> rn;

let Inst{4-0} = op{4-0};
let Inst{31-10} = op{26-5};
let Inst{9-5} = rn{4-0};

let isTerminator       = 1;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ LR ];
}

def BR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<27> op = 0b110101100001111100000000000;
bits<64> rn;

let Inst{4-0} = op{4-0};
let Inst{31-10} = op{26-5};
let Inst{9-5} = rn{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BRK : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_BRK_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1101010000100000;
bits<64> imm16;

let Inst{4-0} = op{4-0};
let Inst{31-21} = op{15-5};
let Inst{20-5} = imm16{15-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def BTI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_BTI_btiAsInt32:$bti );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1101010100000011;
bits<14> opc = 0b00100100011111;
bits<64> bti;

let Inst{31-16} = op{15-0};
let Inst{5-0} = opc{5-0};
let Inst{15-8} = opc{13-6};
let Inst{7-6} = bti{1-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_AL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_AL_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1110;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 1;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_CC : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_CC_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0011;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_CS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_CS_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0010;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_EQ : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_EQ_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0000;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_GE : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_GE_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1010;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_GT : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_GT_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1100;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_HI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_HI_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1000;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_LE : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_LE_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1101;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_LS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_LS_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1001;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_LT : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_LT_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1011;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_MI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_MI_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0100;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_NE : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_NE_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0001;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_NV : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_NV_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b1111;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 1;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_PL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_PL_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0101;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_VC : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_VC_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0111;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def B_VS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_B_VS_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b010101000;
bits<4> cc = 0b0110;
bits<64> offset;

let Inst{4} = op{0};
let Inst{31-24} = op{8-1};
let Inst{23-5} = offset{18-0};
let Inst{3-0} = cc{3-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CBNZW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rt, AArch64Base_CBNZW_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0110101;
bits<1> sf = 0b0;
bits<64> offset;
bits<64> rt;

let Inst{31} = sf{0};
let Inst{30-24} = op{6-0};
let Inst{23-5} = offset{18-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CBNZX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rt, AArch64Base_CBNZX_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0110101;
bits<1> sf = 0b1;
bits<64> offset;
bits<64> rt;

let Inst{31} = sf{0};
let Inst{30-24} = op{6-0};
let Inst{23-5} = offset{18-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CBZW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rt, AArch64Base_CBZW_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0110100;
bits<1> sf = 0b0;
bits<64> offset;
bits<64> rt;

let Inst{31} = sf{0};
let Inst{30-24} = op{6-0};
let Inst{23-5} = offset{18-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CBZX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rt, AArch64Base_CBZX_offsetAsLabel:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0110100;
bits<1> sf = 0b1;
bits<64> offset;
bits<64> rt;

let Inst{31} = sf{0};
let Inst{30-24} = op{6-0};
let Inst{23-5} = offset{18-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CCMNALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNALW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNALX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNCCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNCCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNCCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNCCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNCSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNCSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNEQW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNEQX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNGEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNGEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNGTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNGTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNHIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNHIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIALW_immXAsInt64:$immX, AArch64Base_CCMNIALW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIALX_immXAsInt64:$immX, AArch64Base_CCMNIALX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNICCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNICCW_immXAsInt64:$immX, AArch64Base_CCMNICCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNICCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNICCX_immXAsInt64:$immX, AArch64Base_CCMNICCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNICSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNICSW_immXAsInt64:$immX, AArch64Base_CCMNICSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNICSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNICSX_immXAsInt64:$immX, AArch64Base_CCMNICSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIEQW_immXAsInt64:$immX, AArch64Base_CCMNIEQW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIEQX_immXAsInt64:$immX, AArch64Base_CCMNIEQX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIGEW_immXAsInt64:$immX, AArch64Base_CCMNIGEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIGEX_immXAsInt64:$immX, AArch64Base_CCMNIGEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIGTW_immXAsInt64:$immX, AArch64Base_CCMNIGTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIGTX_immXAsInt64:$immX, AArch64Base_CCMNIGTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIHIW_immXAsInt64:$immX, AArch64Base_CCMNIHIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIHIX_immXAsInt64:$immX, AArch64Base_CCMNIHIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNILEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNILEW_immXAsInt64:$immX, AArch64Base_CCMNILEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNILEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNILEX_immXAsInt64:$immX, AArch64Base_CCMNILEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNILSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNILSW_immXAsInt64:$immX, AArch64Base_CCMNILSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNILSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNILSX_immXAsInt64:$immX, AArch64Base_CCMNILSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNILTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNILTW_immXAsInt64:$immX, AArch64Base_CCMNILTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNILTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNILTX_immXAsInt64:$immX, AArch64Base_CCMNILTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIMIW_immXAsInt64:$immX, AArch64Base_CCMNIMIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIMIX_immXAsInt64:$immX, AArch64Base_CCMNIMIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNINEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNINEW_immXAsInt64:$immX, AArch64Base_CCMNINEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNINEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNINEX_immXAsInt64:$immX, AArch64Base_CCMNINEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNINVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNINVW_immXAsInt64:$immX, AArch64Base_CCMNINVW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNINVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNINVX_immXAsInt64:$immX, AArch64Base_CCMNINVX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIPLW_immXAsInt64:$immX, AArch64Base_CCMNIPLW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIPLX_immXAsInt64:$immX, AArch64Base_CCMNIPLX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIVCW_immXAsInt64:$immX, AArch64Base_CCMNIVCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIVCX_immXAsInt64:$immX, AArch64Base_CCMNIVCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMNIVSW_immXAsInt64:$immX, AArch64Base_CCMNIVSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNIVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMNIVSX_immXAsInt64:$immX, AArch64Base_CCMNIVSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNLEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNLEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNLEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNLEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNLSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNLSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNLSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNLSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNLTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNLTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNLTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNLTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNMIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNMIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNNEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNNEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNNEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNNEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNNVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNNVW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNNVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNNVX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNPLW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNPLX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNVCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNVCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMNVSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMNVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMNVSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b0111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPALW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPALX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPCCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPCCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPCCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPCCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPCSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPCSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPEQW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPEQX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPGEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPGEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPGTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPGTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPHIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPHIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIALW_immXAsInt64:$immX, AArch64Base_CCMPIALW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIALX_immXAsInt64:$immX, AArch64Base_CCMPIALX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPICCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPICCW_immXAsInt64:$immX, AArch64Base_CCMPICCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPICCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPICCX_immXAsInt64:$immX, AArch64Base_CCMPICCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPICSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPICSW_immXAsInt64:$immX, AArch64Base_CCMPICSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPICSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPICSX_immXAsInt64:$immX, AArch64Base_CCMPICSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIEQW_immXAsInt64:$immX, AArch64Base_CCMPIEQW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIEQX_immXAsInt64:$immX, AArch64Base_CCMPIEQX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIGEW_immXAsInt64:$immX, AArch64Base_CCMPIGEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIGEX_immXAsInt64:$immX, AArch64Base_CCMPIGEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIGTW_immXAsInt64:$immX, AArch64Base_CCMPIGTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIGTX_immXAsInt64:$immX, AArch64Base_CCMPIGTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIHIW_immXAsInt64:$immX, AArch64Base_CCMPIHIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIHIX_immXAsInt64:$immX, AArch64Base_CCMPIHIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPILEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPILEW_immXAsInt64:$immX, AArch64Base_CCMPILEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPILEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPILEX_immXAsInt64:$immX, AArch64Base_CCMPILEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPILSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPILSW_immXAsInt64:$immX, AArch64Base_CCMPILSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPILSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPILSX_immXAsInt64:$immX, AArch64Base_CCMPILSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPILTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPILTW_immXAsInt64:$immX, AArch64Base_CCMPILTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPILTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPILTX_immXAsInt64:$immX, AArch64Base_CCMPILTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIMIW_immXAsInt64:$immX, AArch64Base_CCMPIMIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIMIX_immXAsInt64:$immX, AArch64Base_CCMPIMIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPINEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPINEW_immXAsInt64:$immX, AArch64Base_CCMPINEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPINEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPINEX_immXAsInt64:$immX, AArch64Base_CCMPINEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPINVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPINVW_immXAsInt64:$immX, AArch64Base_CCMPINVW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPINVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPINVX_immXAsInt64:$immX, AArch64Base_CCMPINVX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIPLW_immXAsInt64:$immX, AArch64Base_CCMPIPLW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIPLX_immXAsInt64:$immX, AArch64Base_CCMPIPLX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIVCW_immXAsInt64:$immX, AArch64Base_CCMPIVCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIVCX_immXAsInt64:$immX, AArch64Base_CCMPIVCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, AArch64Base_CCMPIVSW_immXAsInt64:$immX, AArch64Base_CCMPIVSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPIVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, AArch64Base_CCMPIVSX_immXAsInt64:$immX, AArch64Base_CCMPIVSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010100;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> immX;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = immX{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{8-5};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPLEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPLEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPLEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPLEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPLSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPLSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPLSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPLSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPLTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPLTW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPLTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPLTX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPMIW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPMIX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPNEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPNEW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPNEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPNEX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPNVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPNVW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPNVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPNVX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPPLW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPPLX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPVCW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPVCX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_CCMPVSW_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CCMPVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_CCMPVSX_nzcvAsInt32:$nzcv );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<13> op = 0b1111010010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> nzcv;

let Inst{31} = sf{0};
let Inst{4} = op{0};
let Inst{11-10} = op{2-1};
let Inst{30-21} = op{12-3};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{3-0} = nzcv{3-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def CLSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000101;
bits<1> sf = 0b0;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CLSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000101;
bits<1> sf = 0b1;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CLZW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000100;
bits<1> sf = 0b0;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CLZX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000100;
bits<1> sf = 0b1;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELCCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELCCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELLEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELLEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELLSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELLSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELLTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELLTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELNEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELNEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELNVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELNVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSELVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCCCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCCCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCLEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCLEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCLSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCLSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCLTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCLTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCNEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCNEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCNVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCNVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINCVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b001101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVCCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVCCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVLEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVLEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVLSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVLSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVLTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVLTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVNEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVNEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVNVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVNVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSINVVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010000;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGALW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGALX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGCCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGCCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGEQW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGEQX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGGEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGGEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1010;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGGTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGGTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGHIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGHIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGLEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGLEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGLSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGLSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGLTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGLTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1011;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGMIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGMIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0100;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGNEW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGNEX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0001;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGNVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGNVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b1111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGPLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGPLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0101;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGVCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGVCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0111;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGVSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b0;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def CSNEGVSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b101101010001;
bits<1> sf = 0b1;
bits<4> cc = 0b0110;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{11-10} = op{1-0};
let Inst{30-21} = op{11-2};
let Inst{20-16} = rm{4-0};
let Inst{15-12} = cc{3-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EONWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EONWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EONWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EONWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EONXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EONXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EONXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EONXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EONXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010101;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins W:$rn, AArch64Base_EORIW_immWSizeAsInt32:$immWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10100100;
bits<1> sf = 0b0;
bits<64> immWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immWSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins X:$rn, AArch64Base_EORIX_immXSizeAsInt64:$immXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10100100;
bits<1> sf = 0b1;
bits<64> immXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immXSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EORWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EORWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EORWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EORWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EORXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EORXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EORXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EORXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EORXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10010100;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ERET : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins  );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1101011010011111;
bits<16> imm16 = 0b0000001111100000;

let Inst{31-16} = op{15-0};
let Inst{15-0} = imm16{15-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ SP_EL1,NZCV_N,NZCV_Z,NZCV_C,NZCV_V,CurrentEL,SP ];
}

def EXTRW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_EXTRW_shiftWSizeAsInt32:$shiftWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001001110;
bits<1> sf = 0b0;
bits<1> N = 0b0;
bits<64> rm;
bits<64> shiftWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-23} = op{8-1};
let Inst{22} = N{0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = shiftWSize{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def EXTRX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_EXTRX_shiftXSizeAsInt32:$shiftXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b001001110;
bits<1> sf = 0b1;
bits<1> N = 0b1;
bits<64> rm;
bits<64> shiftXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-23} = op{8-1};
let Inst{22} = N{0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = shiftXSize{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def HLT : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_HLT_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1101010001000000;
bits<64> imm16;

let Inst{4-0} = op{4-0};
let Inst{31-21} = op{15-5};
let Inst{20-5} = imm16{15-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn, AArch64Base_LDPS_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0101;
bits<6> op = 0b101010;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPSPre : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn_36, AArch64Base_LDPSPre_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0101;
bits<6> op = 0b101011;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_36";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPSPst : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn_37, AArch64Base_LDPSPst_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0101;
bits<6> op = 0b101001;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_37";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn, AArch64Base_LDPW_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0001;
bits<6> op = 0b101010;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPWPre : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn_34, AArch64Base_LDPWPre_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0001;
bits<6> op = 0b101011;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_34";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPWPst : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn_35, AArch64Base_LDPWPst_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0001;
bits<6> op = 0b101001;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_35";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn, AArch64Base_LDPX_offXSizeAsInt64:$offXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b1001;
bits<6> op = 0b101010;
bits<64> offXSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offXSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPXPre : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn_38, AArch64Base_LDPXPre_offXSizeAsInt64:$offXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b1001;
bits<6> op = 0b101011;
bits<64> offXSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offXSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_38";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDPXPst : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt, X:$rt2 );
let InOperandList = ( ins S:$rn_39, AArch64Base_LDPXPst_offXSizeAsInt64:$offXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b1001;
bits<6> op = 0b101001;
bits<64> offXSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offXSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_39";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRLitSWX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins AArch64Base_LDRLitSWX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b011000;
bits<2> opc = 0b10;
bits<64> offset;
bits<64> rt;

let Inst{31-30} = opc{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-5} = offset{18-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRLitW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins AArch64Base_LDRLitW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b011000;
bits<2> opc = 0b00;
bits<64> offset;
bits<64> rt;

let Inst{31-30} = opc{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-5} = offset{18-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRLitX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins AArch64Base_LDRLitX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b011000;
bits<2> opc = 0b01;
bits<64> offset;
bits<64> rt;

let Inst{31-30} = opc{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-5} = offset{18-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_8, AArch64Base_LDRPreB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_8";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_9, AArch64Base_LDRPreH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_9";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreSBW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_15, AArch64Base_LDRPreSBW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_15";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreSBX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_12, AArch64Base_LDRPreSBX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_12";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreSHW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_16, AArch64Base_LDRPreSHW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_16";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreSHX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_13, AArch64Base_LDRPreSHX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_13";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreSWX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_14, AArch64Base_LDRPreSWX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_14";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, W:$rt );
let InOperandList = ( ins S:$rn_10, AArch64Base_LDRPreW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_10";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPreX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_11, AArch64Base_LDRPreX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_11";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_21, AArch64Base_LDRPstB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_21";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_22, AArch64Base_LDRPstH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_22";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstSBW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_28, AArch64Base_LDRPstSBW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_28";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstSBX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_25, AArch64Base_LDRPstSBX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_25";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstSHW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_29, AArch64Base_LDRPstSHW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_29";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstSHX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_26, AArch64Base_LDRPstSHX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_26";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstSWX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_27, AArch64Base_LDRPstSWX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_27";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, W:$rt );
let InOperandList = ( ins S:$rn_23, AArch64Base_LDRPstW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_23";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRPstX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn, X:$rt );
let InOperandList = ( ins S:$rn_24, AArch64Base_LDRPstX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_24";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegBUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegHUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBWUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSBXUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHWUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSHXUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegSWXUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegWUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRRegXUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRSBW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRSBW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRSBX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRSBX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRSHW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRSHW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRSHX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRSHX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRSWX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRSWX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDRX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDRX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b01;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b01;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURSBW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURSBW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b11;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURSBX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURSBX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b10;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURSHW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURSHW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b11;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURSHX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURSHX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b10;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURSWX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURSWX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b10;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b01;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LDURX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins S:$rn, AArch64Base_LDURX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b01;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 1;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LSLW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LSLX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LSRW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def LSRX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MADDW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$ra, W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110110000;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MADDX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$ra, X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110110000;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVKWPos00 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rd_0, AArch64Base_MOVKWPos00_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b0;
bits<8> op = 0b11100101;
bits<2> pos = 0b00;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_0";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVKWPos16 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rd_1, AArch64Base_MOVKWPos16_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b0;
bits<8> op = 0b11100101;
bits<2> pos = 0b01;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_1";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVKXPos00 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rd_2, AArch64Base_MOVKXPos00_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b11100101;
bits<2> pos = 0b00;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_2";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVKXPos16 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rd_3, AArch64Base_MOVKXPos16_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b11100101;
bits<2> pos = 0b01;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_3";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVKXPos32 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rd_4, AArch64Base_MOVKXPos32_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b11100101;
bits<2> pos = 0b10;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_4";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVKXPos48 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rd_5, AArch64Base_MOVKXPos48_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b11100101;
bits<2> pos = 0b11;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rd = $rd_5";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVNWPos00 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVNWPos00_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b0;
bits<8> op = 0b00100101;
bits<2> pos = 0b00;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVNWPos16 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVNWPos16_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b0;
bits<8> op = 0b00100101;
bits<2> pos = 0b01;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVNXPos00 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVNXPos00_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b00100101;
bits<2> pos = 0b00;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVNXPos16 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVNXPos16_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b00100101;
bits<2> pos = 0b01;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVNXPos32 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVNXPos32_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b00100101;
bits<2> pos = 0b10;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVNXPos48 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVNXPos48_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b00100101;
bits<2> pos = 0b11;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVZWPos00 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVZWPos00_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b0;
bits<8> op = 0b10100101;
bits<2> pos = 0b00;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVZWPos16 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVZWPos16_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b0;
bits<8> op = 0b10100101;
bits<2> pos = 0b01;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVZXPos00 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVZXPos00_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b10100101;
bits<2> pos = 0b00;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVZXPos16 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVZXPos16_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b10100101;
bits<2> pos = 0b01;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVZXPos32 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVZXPos32_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b10100101;
bits<2> pos = 0b10;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MOVZXPos48 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins AArch64Base_MOVZXPos48_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<1> sf = 0b1;
bits<8> op = 0b10100101;
bits<2> pos = 0b11;
bits<64> imm16;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-21} = pos{1-0};
let Inst{20-5} = imm16{15-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MRS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins AArch64Base_MRS_sysregAsInt32:$sysreg );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b110101010011;
bits<64> sysreg;
bits<64> rt;

let Inst{31-20} = op{11-0};
let Inst{19-5} = sysreg{14-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ SP_EL1 ];
}

def MRSNZCV : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rt );
let InOperandList = ( ins  );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b110101010011;
bits<15> rs = 0b101101000010000;
bits<64> rt;

let Inst{31-20} = op{11-0};
let Inst{19-5} = rs{14-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rt, AArch64Base_MSR_sysregAsInt32:$sysreg );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<12> op = 0b110101010001;
bits<64> sysreg;
bits<64> rt;

let Inst{31-20} = op{11-0};
let Inst{19-5} = sysreg{14-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V,CPACR_EL1,VBAR_EL1,ELR_EL1,SP_EL1,SP_EL0,SPSel,ESR_EL1,SPSR_EL1,SP ];
}

def MSUBW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$ra, W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110110001;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def MSUBX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$ra, X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110110001;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def NOP : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins  );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1101010100000011;
bits<16> imm16 = 0b0010000000011111;

let Inst{31-16} = op{15-0};
let Inst{15-0} = imm16{15-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORNWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORNWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORNWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORNWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORNXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORNXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORNXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORNXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORNXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010101;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRIW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins W:$rn, AArch64Base_ORRIW_immWSizeAsInt32:$immWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01100100;
bits<1> sf = 0b0;
bits<64> immWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immWSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRIX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins X:$rn, AArch64Base_ORRIX_immXSizeAsInt64:$immXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01100100;
bits<1> sf = 0b1;
bits<64> immXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22-10} = immXSize{12-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORRWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORRWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORRWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRWROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_ORRWROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORRXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORRXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORRXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def ORRXROR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_ORRXROR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b01010100;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{30-24} = op{7-1};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def RBITW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, bare_symbol:$bits );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000000;
bits<1> sf = 0b0;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def RBITX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, bare_symbol:$bits );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000000;
bits<1> sf = 0b1;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def RET : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<27> op = 0b110101100101111100000000000;
bits<64> rn;

let Inst{4-0} = op{4-0};
let Inst{31-10} = op{26-5};
let Inst{9-5} = rn{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def REV16W : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000001;
bits<1> sf = 0b0;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def REV16X : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000001;
bits<1> sf = 0b1;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def REV32 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000010;
bits<1> sf = 0b1;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def REVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000010;
bits<1> sf = 0b0;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def REVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<21> op = 0b101101011000000000011;
bits<1> sf = 0b1;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-10} = op{20-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def RORW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b0;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def RORX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<14> op = 0b00110101100010;
bits<1> sf = 0b1;
bits<2> shift = 0b11;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-12} = op{3-0};
let Inst{30-21} = op{13-4};
let Inst{20-16} = rm{4-0};
let Inst{11-10} = shift{1-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SBCSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1111010000000000;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SBCSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1111010000000000;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SBCW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1011010000000000;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SBCX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1011010000000000;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SBFMW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, AArch64Base_SBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_SBFMW_rightWSizeAsInt32:$rightWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00100110;
bits<1> sf = 0b0;
bits<1> N = 0b0;
bits<64> rightWSize;
bits<64> leftWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22} = N{0};
let Inst{21-16} = rightWSize{5-0};
let Inst{15-10} = leftWSize{11-6};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SBFMX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, AArch64Base_SBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_SBFMX_rightXSizeAsInt32:$rightXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b00100110;
bits<1> sf = 0b1;
bits<1> N = 0b1;
bits<64> rightXSize;
bits<64> leftXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22} = N{0};
let Inst{21-16} = rightXSize{5-0};
let Inst{15-10} = leftXSize{11-6};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SDIVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rm, W:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011010110000011;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SDIVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rm, X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011010110000011;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SMADDL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$ra, W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110110010;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SMSUBL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$ra, W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110110011;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SMULH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011011010011111;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STPW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rt, X:$rt2, AArch64Base_STPW_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0000;
bits<6> op = 0b101010;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STPWPre : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_40, X:$rt, X:$rt2, AArch64Base_STPWPre_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0000;
bits<6> op = 0b101011;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_40";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STPWPst : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_41, X:$rt, X:$rt2, AArch64Base_STPWPst_offWSizeAsInt64:$offWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b0000;
bits<6> op = 0b101001;
bits<64> offWSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offWSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_41";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STPX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rt, X:$rt2, AArch64Base_STPX_offXSizeAsInt64:$offXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b1000;
bits<6> op = 0b101010;
bits<64> offXSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offXSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STPXPre : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_42, X:$rt, X:$rt2, AArch64Base_STPXPre_offXSizeAsInt64:$offXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b1000;
bits<6> op = 0b101011;
bits<64> offXSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offXSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_42";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STPXPst : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_43, X:$rt, X:$rt2, AArch64Base_STPXPst_offXSizeAsInt64:$offXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<4> opc = 0b1000;
bits<6> op = 0b101001;
bits<64> offXSize;
bits<64> rt2;
bits<64> rn;
bits<64> rt;

let Inst{22} = opc{0};
let Inst{26} = opc{1};
let Inst{31-30} = opc{3-2};
let Inst{25-23} = op{2-0};
let Inst{29-27} = op{5-3};
let Inst{21-15} = offXSize{6-0};
let Inst{14-10} = rt2{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_43";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, W:$rt, AArch64Base_STRB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, W:$rt, AArch64Base_STRH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPreB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_17, W:$rt, AArch64Base_STRPreB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_17";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPreH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_18, W:$rt, AArch64Base_STRPreH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_18";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPreW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_19, W:$rt, AArch64Base_STRPreW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_19";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPreX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_20, X:$rt, AArch64Base_STRPreX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000011;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_20";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPstB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_30, W:$rt, AArch64Base_STRPstB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_30";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPstH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_31, W:$rt, AArch64Base_STRPstH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_31";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPstW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_32, W:$rt, AArch64Base_STRPstW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_32";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRPstX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rn );
let InOperandList = ( ins S:$rn_33, X:$rt, AArch64Base_STRPstX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000001;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "$rn = $rn_33";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegBUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegHUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegWUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, W:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXSXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b110;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXSXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b110;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXSXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b111;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXSXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b111;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXUXTWIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b010;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXUXTWUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b010;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXUXTXIs : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b011;
bits<1> scaled = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRRegXUXTXUn : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rm, X:$rt );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000110;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<3> option = 0b011;
bits<1> scaled = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12} = scaled{0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, W:$rt, AArch64Base_STRW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STRX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rt, AArch64Base_STRX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<6> op = 0b111001;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{29-24} = op{5-0};
let Inst{23-22} = opc{1-0};
let Inst{21-10} = offset{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STURB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, W:$rt, AArch64Base_STURB_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b00;
bits<2> size = 0b00;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STURH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, W:$rt, AArch64Base_STURH_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b00;
bits<2> size = 0b01;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STURW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, W:$rt, AArch64Base_STURW_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b00;
bits<2> size = 0b10;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def STURX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins S:$rn, X:$rt, AArch64Base_STURX_offsetAsInt64:$offset );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b111000000;
bits<2> opc = 0b00;
bits<2> size = 0b11;
bits<64> offset;
bits<64> rn;
bits<64> rt;

let Inst{31-30} = size{1-0};
let Inst{11-10} = op{1-0};
let Inst{21} = op{2};
let Inst{29-24} = op{8-3};
let Inst{23-22} = opc{1-0};
let Inst{20-12} = offset{8-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rt{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 1;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 1;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_SUBWASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_SUBWI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_SUBWI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_SUBWLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_SUBWLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_SUBWSASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_SUBWSI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, AArch64Base_SUBWSI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_SUBWSLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, W:$rm, AArch64Base_SUBWSLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBWSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm, AArch64Base_SUBWUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBWUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs R:$rd );
let InOperandList = ( ins R:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b0;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_SUBXASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_SUBXI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_SUBXI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 1;
let isAsCheapAsAMove   = 1;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_SUBXLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_SUBXLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXS : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<6> imm6 = 0b000000;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSASR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_SUBXSASR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<2> shift = 0b10;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSI : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_SUBXSI_imm12XAsInt64:$imm12X );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<1> sh = 0b0;
bits<64> imm12X;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12X{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSI12 : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, AArch64Base_SUBXSI12_imm12SAsInt64:$imm12S );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1100010;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<1> sh = 0b1;
bits<64> imm12S;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-23} = op{5-0};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{22} = sh{0};
let Inst{21-10} = imm12S{11-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSLSL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_SUBXSLSL_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<2> shift = 0b00;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSLSR : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm, AArch64Base_SUBXSLSR_imm6AsInt32:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b1010110;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<2> shift = 0b01;
bits<64> rm;
bits<64> imm6;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{21} = op{0};
let Inst{28-24} = op{5-1};
let Inst{30} = op{6};
let Inst{29} = ff{0};
let Inst{23-22} = shift{1-0};
let Inst{20-16} = rm{4-0};
let Inst{15-10} = imm6{5-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b1;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [ NZCV_N,NZCV_Z,NZCV_C,NZCV_V ];
}

def SUBXSXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXSXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXSXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXSXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXSXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXSXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b100;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXSXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b101;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXSXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b110;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXSXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b111;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXSB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXUXSB_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXSH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXUXSH_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXSW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXUXSW_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXSX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm, AArch64Base_SUBXUXSX_imm3AsInt32:$imm3 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<64> rm;
bits<64> imm3;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXTB : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b000;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXTH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b001;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXTW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b010;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SUBXUXTX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<9> op = 0b101011001;
bits<1> sf = 0b1;
bits<1> ff = 0b0;
bits<3> option = 0b011;
bits<3> imm3 = 0b000;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{28-21} = op{7-0};
let Inst{30} = op{8};
let Inst{29} = ff{0};
let Inst{20-16} = rm{4-0};
let Inst{15-13} = option{2-0};
let Inst{12-10} = imm3{2-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def SVC : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_SVC_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1101010000000001;
bits<64> imm16;

let Inst{4-0} = op{4-0};
let Inst{31-21} = op{15-5};
let Inst{20-5} = imm16{15-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def TBNZ : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rt, AArch64Base_TBNZ_offsetAsLabel:$offset, AArch64Base_TBNZ_imm6AsLabel:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0110111;
bits<64> imm6;
bits<64> offset;
bits<64> rt;

let Inst{23-19} = imm6{4-0};
let Inst{31} = imm6{5};
let Inst{30-24} = op{6-0};
let Inst{18-5} = offset{19-6};
let Inst{4-0} = rt{4-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def TBZ : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins X:$rt, AArch64Base_TBZ_offsetAsLabel:$offset, AArch64Base_TBZ_imm6AsLabel:$imm6 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<7> op = 0b0110110;
bits<64> imm6;
bits<64> offset;
bits<64> rt;

let Inst{23-19} = imm6{4-0};
let Inst{31} = imm6{5};
let Inst{30-24} = op{6-0};
let Inst{18-5} = offset{19-6};
let Inst{4-0} = rt{4-0};

let isTerminator       = 1;
let isBranch           = 1;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UBFMW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rn, AArch64Base_UBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_UBFMW_rightWSizeAsInt32:$rightWSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10100110;
bits<1> sf = 0b0;
bits<1> N = 0b0;
bits<64> rightWSize;
bits<64> leftWSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22} = N{0};
let Inst{21-16} = rightWSize{5-0};
let Inst{15-10} = leftWSize{11-6};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UBFMX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, AArch64Base_UBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_UBFMX_rightXSizeAsInt32:$rightXSize );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<8> op = 0b10100110;
bits<1> sf = 0b1;
bits<1> N = 0b1;
bits<64> rightXSize;
bits<64> leftXSize;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{30-23} = op{7-0};
let Inst{22} = N{0};
let Inst{21-16} = rightXSize{5-0};
let Inst{15-10} = leftXSize{11-6};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UDF : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins AArch64Base_UDF_imm16AsInt32:$imm16 );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0000000000000000;
bits<64> imm16;

let Inst{31-16} = op{15-0};
let Inst{15-0} = imm16{15-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UDIVW : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs W:$rd );
let InOperandList = ( ins W:$rm, W:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011010110000010;
bits<1> sf = 0b0;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UDIVX : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rm, X:$rn );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011010110000010;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UMADDL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$ra, W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110111010;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UMSUBL : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$ra, W:$rn, W:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<11> op = 0b00110111011;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> ra;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15} = op{0};
let Inst{30-21} = op{10-1};
let Inst{20-16} = rm{4-0};
let Inst{14-10} = ra{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def UMULH : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs X:$rd );
let InOperandList = ( ins X:$rn, X:$rm );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b0011011110011111;
bits<1> sf = 0b1;
bits<64> rm;
bits<64> rn;
bits<64> rd;

let Inst{31} = sf{0};
let Inst{15-10} = op{5-0};
let Inst{30-21} = op{15-6};
let Inst{20-16} = rm{4-0};
let Inst{9-5} = rn{4-0};
let Inst{4-0} = rd{4-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}

def YIELD : Instruction
{
let Namespace = "processorNameValue";

let Size = 4;
let CodeSize = 4;

let OutOperandList = ( outs  );
let InOperandList = ( ins  );

field bits<32> Inst;

// SoftFail is a field the disassembler can use to provide a way for
// instructions to not match without killing the whole decode process. It is
// mainly used for ARM, but Tablegen expects this field to exist or it fails
// to build the decode table.
field bits<32> SoftFail = 0;

bits<16> op = 0b1101010100000011;
bits<16> imm16 = 0b0010000000111111;

let Inst{31-16} = op{15-0};
let Inst{15-0} = imm16{15-0};

let isTerminator       = 0;
let isBranch           = 0;
let isCall             = 0;
let isReturn           = 0;
let isPseudo           = 0;
let isCodeGenOnly      = 0;
let mayLoad            = 0;
let mayStore           = 0;
let isBarrier          = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;
let hasSideEffects     = 0;

let Constraints = "";
let AddedComplexity = 0;

let Pattern = [];

let Uses = [  ];
let Defs = [  ];
}



// Hardcoded
def : Pat<(i64 AddrFI:$rs1),
  (ADDXI AddrFI:$rs1, (i64 0))>;


def ASRIW : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, bare_symbol:$shift );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def ASRIX : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, bare_symbol:$shift );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def ASRVW : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def ASRVX : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSLIW : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, bare_symbol:$shift );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSLIX : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, bare_symbol:$shift );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSLVW : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSLVX : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSRIW : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, bare_symbol:$shift );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSRIX : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, bare_symbol:$shift );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSRVW : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def LSRVX : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def MULW : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def MULX : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins S:$rn, S:$rm );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def PLDR : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins bare_symbol:$symbol );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def TEMP_CALL : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs  );
let InOperandList = ( ins  );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 0;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}






def constMat0 : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins bare_symbol:$val );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 1;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}



def constMat1 : Instruction
{
let Namespace = "processorNameValue";

let OutOperandList = ( outs S:$rd );
let InOperandList = ( ins bare_symbol:$val );

let isTerminator  = 0;
let isBranch      = 0;
let isCall        = 0;
let isReturn      = 0;
let isPseudo      = 1;
let isCodeGenOnly = 1;
let mayLoad       = 0;
let mayStore      = 0;
let isBarrier     = 0;
let isReMaterializable = 0;
let isAsCheapAsAMove   = 0;

let Constraints = "";
let AddedComplexity = 0;

let Uses = [  ];
let Defs = [  ];
}


























def : Pat<(add W:$rn, W:$rm),
        (ADDW W:$rn, W:$rm)>;


def : Pat<(add W:$rn, (sra W:$rm, AArch64Base_ADDWASR_imm6AsInt32:$imm6)),
        (ADDWASR W:$rn, W:$rm, AArch64Base_ADDWASR_imm6AsInt32:$imm6)>;


def : Pat<(add W:$rn, (shl W:$rm, AArch64Base_ADDWLSL_imm6AsInt32:$imm6)),
        (ADDWLSL W:$rn, W:$rm, AArch64Base_ADDWLSL_imm6AsInt32:$imm6)>;


def : Pat<(add W:$rn, (srl W:$rm, AArch64Base_ADDWLSR_imm6AsInt32:$imm6)),
        (ADDWLSR W:$rn, W:$rm, AArch64Base_ADDWLSR_imm6AsInt32:$imm6)>;


def : Pat<(add W:$rn, W:$rm),
        (ADDWS W:$rn, W:$rm)>;


def : Pat<(add W:$rn, (sra W:$rm, AArch64Base_ADDWSASR_imm6AsInt32:$imm6)),
        (ADDWSASR W:$rn, W:$rm, AArch64Base_ADDWSASR_imm6AsInt32:$imm6)>;


def : Pat<(add W:$rn, (shl W:$rm, AArch64Base_ADDWSLSL_imm6AsInt32:$imm6)),
        (ADDWSLSL W:$rn, W:$rm, AArch64Base_ADDWSLSL_imm6AsInt32:$imm6)>;


def : Pat<(add W:$rn, (srl W:$rm, AArch64Base_ADDWSLSR_imm6AsInt32:$imm6)),
        (ADDWSLSR W:$rn, W:$rm, AArch64Base_ADDWSLSR_imm6AsInt32:$imm6)>;


def : Pat<(add X:$rn, X:$rm),
        (ADDX X:$rn, X:$rm)>;


def : Pat<(add X:$rn, (sra X:$rm, AArch64Base_ADDXASR_imm6AsInt32:$imm6)),
        (ADDXASR X:$rn, X:$rm, AArch64Base_ADDXASR_imm6AsInt32:$imm6)>;


def : Pat<(add S:$rn, AArch64Base_ADDXI_imm12XAsInt64:$imm12X),
        (ADDXI S:$rn, AArch64Base_ADDXI_imm12XAsInt64:$imm12X)>;

def : Pat<(add AddrFI:$rn, AArch64Base_ADDXI_imm12XAsInt64:$imm12X),
        (ADDXI AddrFI:$rn, AArch64Base_ADDXI_imm12XAsInt64:$imm12X)>;


def : Pat<(add S:$rn, AArch64Base_ADDXI12_imm12SAsInt64:$imm12S),
        (ADDXI12 S:$rn, AArch64Base_ADDXI12_imm12SAsInt64:$imm12S)>;


def : Pat<(add X:$rn, (shl X:$rm, AArch64Base_ADDXLSL_imm6AsInt32:$imm6)),
        (ADDXLSL X:$rn, X:$rm, AArch64Base_ADDXLSL_imm6AsInt32:$imm6)>;


def : Pat<(add X:$rn, (srl X:$rm, AArch64Base_ADDXLSR_imm6AsInt32:$imm6)),
        (ADDXLSR X:$rn, X:$rm, AArch64Base_ADDXLSR_imm6AsInt32:$imm6)>;


def : Pat<(add X:$rn, X:$rm),
        (ADDXS X:$rn, X:$rm)>;


def : Pat<(add X:$rn, (sra X:$rm, AArch64Base_ADDXSASR_imm6AsInt32:$imm6)),
        (ADDXSASR X:$rn, X:$rm, AArch64Base_ADDXSASR_imm6AsInt32:$imm6)>;


def : Pat<(add S:$rn, AArch64Base_ADDXSI_imm12XAsInt64:$imm12X),
        (ADDXSI S:$rn, AArch64Base_ADDXSI_imm12XAsInt64:$imm12X)>;


def : Pat<(add S:$rn, AArch64Base_ADDXSI12_imm12SAsInt64:$imm12S),
        (ADDXSI12 S:$rn, AArch64Base_ADDXSI12_imm12SAsInt64:$imm12S)>;


def : Pat<(add X:$rn, (shl X:$rm, AArch64Base_ADDXSLSL_imm6AsInt32:$imm6)),
        (ADDXSLSL X:$rn, X:$rm, AArch64Base_ADDXSLSL_imm6AsInt32:$imm6)>;


def : Pat<(add X:$rn, (srl X:$rm, AArch64Base_ADDXSLSR_imm6AsInt32:$imm6)),
        (ADDXSLSR X:$rn, X:$rm, AArch64Base_ADDXSLSR_imm6AsInt32:$imm6)>;


def : Pat<(add S:$rn, (shl X:$rm, AArch64Base_ADDXSSXSX_imm3AsInt32:$imm3)),
        (ADDXSSXSX S:$rn, X:$rm, AArch64Base_ADDXSSXSX_imm3AsInt32:$imm3)>;


def : Pat<(add S:$rn, X:$rm),
        (ADDXSSXTX S:$rn, X:$rm)>;


def : Pat<(add S:$rn, (shl X:$rm, AArch64Base_ADDXSUXSX_imm3AsInt32:$imm3)),
        (ADDXSUXSX S:$rn, X:$rm, AArch64Base_ADDXSUXSX_imm3AsInt32:$imm3)>;


def : Pat<(add S:$rn, X:$rm),
        (ADDXSUXTX S:$rn, X:$rm)>;


def : Pat<(add S:$rn, (shl X:$rm, AArch64Base_ADDXSXSX_imm3AsInt32:$imm3)),
        (ADDXSXSX S:$rn, X:$rm, AArch64Base_ADDXSXSX_imm3AsInt32:$imm3)>;


def : Pat<(add S:$rn, X:$rm),
        (ADDXSXTX S:$rn, X:$rm)>;


def : Pat<(add S:$rn, (shl X:$rm, AArch64Base_ADDXUXSX_imm3AsInt32:$imm3)),
        (ADDXUXSX S:$rn, X:$rm, AArch64Base_ADDXUXSX_imm3AsInt32:$imm3)>;


def : Pat<(add S:$rn, X:$rm),
        (ADDXUXTX S:$rn, X:$rm)>;


def : Pat<(and W:$rn, AArch64Base_ANDIW_immWSizeAsInt32:$immWSize),
        (ANDIW W:$rn, AArch64Base_ANDIW_immWSizeAsInt32:$immWSize)>;


def : Pat<(and X:$rn, AArch64Base_ANDIX_immXSizeAsInt64:$immXSize),
        (ANDIX X:$rn, AArch64Base_ANDIX_immXSizeAsInt64:$immXSize)>;


def : Pat<(and W:$rn, AArch64Base_ANDSIW_immWSizeAsInt32:$immWSize),
        (ANDSIW W:$rn, AArch64Base_ANDSIW_immWSizeAsInt32:$immWSize)>;


def : Pat<(and X:$rn, AArch64Base_ANDSIX_immXSizeAsInt64:$immXSize),
        (ANDSIX X:$rn, AArch64Base_ANDSIX_immXSizeAsInt64:$immXSize)>;


def : Pat<(and W:$rn, W:$rm),
        (ANDSW W:$rn, W:$rm)>;


def : Pat<(and W:$rn, (sra W:$rm, AArch64Base_ANDSWASR_imm6AsInt32:$imm6)),
        (ANDSWASR W:$rn, W:$rm, AArch64Base_ANDSWASR_imm6AsInt32:$imm6)>;


def : Pat<(and W:$rn, (shl W:$rm, AArch64Base_ANDSWLSL_imm6AsInt32:$imm6)),
        (ANDSWLSL W:$rn, W:$rm, AArch64Base_ANDSWLSL_imm6AsInt32:$imm6)>;


def : Pat<(and W:$rn, (srl W:$rm, AArch64Base_ANDSWLSR_imm6AsInt32:$imm6)),
        (ANDSWLSR W:$rn, W:$rm, AArch64Base_ANDSWLSR_imm6AsInt32:$imm6)>;


def : Pat<(and X:$rn, X:$rm),
        (ANDSX X:$rn, X:$rm)>;


def : Pat<(and X:$rn, (sra X:$rm, AArch64Base_ANDSXASR_imm6AsInt32:$imm6)),
        (ANDSXASR X:$rn, X:$rm, AArch64Base_ANDSXASR_imm6AsInt32:$imm6)>;


def : Pat<(and X:$rn, (shl X:$rm, AArch64Base_ANDSXLSL_imm6AsInt32:$imm6)),
        (ANDSXLSL X:$rn, X:$rm, AArch64Base_ANDSXLSL_imm6AsInt32:$imm6)>;


def : Pat<(and X:$rn, (srl X:$rm, AArch64Base_ANDSXLSR_imm6AsInt32:$imm6)),
        (ANDSXLSR X:$rn, X:$rm, AArch64Base_ANDSXLSR_imm6AsInt32:$imm6)>;


def : Pat<(and W:$rn, W:$rm),
        (ANDW W:$rn, W:$rm)>;


def : Pat<(and W:$rn, (sra W:$rm, AArch64Base_ANDWASR_imm6AsInt32:$imm6)),
        (ANDWASR W:$rn, W:$rm, AArch64Base_ANDWASR_imm6AsInt32:$imm6)>;


def : Pat<(and W:$rn, (shl W:$rm, AArch64Base_ANDWLSL_imm6AsInt32:$imm6)),
        (ANDWLSL W:$rn, W:$rm, AArch64Base_ANDWLSL_imm6AsInt32:$imm6)>;


def : Pat<(and W:$rn, (srl W:$rm, AArch64Base_ANDWLSR_imm6AsInt32:$imm6)),
        (ANDWLSR W:$rn, W:$rm, AArch64Base_ANDWLSR_imm6AsInt32:$imm6)>;


def : Pat<(and X:$rn, X:$rm),
        (ANDX X:$rn, X:$rm)>;


def : Pat<(and X:$rn, (sra X:$rm, AArch64Base_ANDXASR_imm6AsInt32:$imm6)),
        (ANDXASR X:$rn, X:$rm, AArch64Base_ANDXASR_imm6AsInt32:$imm6)>;


def : Pat<(and X:$rn, (shl X:$rm, AArch64Base_ANDXLSL_imm6AsInt32:$imm6)),
        (ANDXLSL X:$rn, X:$rm, AArch64Base_ANDXLSL_imm6AsInt32:$imm6)>;


def : Pat<(and X:$rn, (srl X:$rm, AArch64Base_ANDXLSR_imm6AsInt32:$imm6)),
        (ANDXLSR X:$rn, X:$rm, AArch64Base_ANDXLSR_imm6AsInt32:$imm6)>;


def : Pat<(sra W:$rn, W:$rm),
        (ASRW W:$rn, W:$rm)>;


def : Pat<(sra X:$rn, X:$rm),
        (ASRX X:$rn, X:$rm)>;


def : Pat<(br bb:$offset),
        (B bb:$offset)>;


def : Pat<(br bb:$offset),
        (B_AL bb:$offset)>;


def : Pat<(br bb:$offset),
        (B_NV bb:$offset)>;


def : Pat<(xor W:$rn, AArch64Base_EORIW_immWSizeAsInt32:$immWSize),
        (EORIW W:$rn, AArch64Base_EORIW_immWSizeAsInt32:$immWSize)>;


def : Pat<(xor X:$rn, AArch64Base_EORIX_immXSizeAsInt64:$immXSize),
        (EORIX X:$rn, AArch64Base_EORIX_immXSizeAsInt64:$immXSize)>;


def : Pat<(xor W:$rn, W:$rm),
        (EORW W:$rn, W:$rm)>;


def : Pat<(xor W:$rn, (sra W:$rm, AArch64Base_EORWASR_imm6AsInt32:$imm6)),
        (EORWASR W:$rn, W:$rm, AArch64Base_EORWASR_imm6AsInt32:$imm6)>;


def : Pat<(xor W:$rn, (shl W:$rm, AArch64Base_EORWLSL_imm6AsInt32:$imm6)),
        (EORWLSL W:$rn, W:$rm, AArch64Base_EORWLSL_imm6AsInt32:$imm6)>;


def : Pat<(xor W:$rn, (srl W:$rm, AArch64Base_EORWLSR_imm6AsInt32:$imm6)),
        (EORWLSR W:$rn, W:$rm, AArch64Base_EORWLSR_imm6AsInt32:$imm6)>;


def : Pat<(xor X:$rn, X:$rm),
        (EORX X:$rn, X:$rm)>;


def : Pat<(xor X:$rn, (sra X:$rm, AArch64Base_EORXASR_imm6AsInt32:$imm6)),
        (EORXASR X:$rn, X:$rm, AArch64Base_EORXASR_imm6AsInt32:$imm6)>;


def : Pat<(xor X:$rn, (shl X:$rm, AArch64Base_EORXLSL_imm6AsInt32:$imm6)),
        (EORXLSL X:$rn, X:$rm, AArch64Base_EORXLSL_imm6AsInt32:$imm6)>;


def : Pat<(xor X:$rn, (srl X:$rm, AArch64Base_EORXLSR_imm6AsInt32:$imm6)),
        (EORXLSR X:$rn, X:$rm, AArch64Base_EORXLSR_imm6AsInt32:$imm6)>;


def : Pat<(zextloadi8 (add S:$rn, AArch64Base_LDRB_offsetAsInt64:$offset)),
        (LDRB S:$rn, AArch64Base_LDRB_offsetAsInt64:$offset)>;

def : Pat<(zextloadi8 (add AddrFI:$rn, AArch64Base_LDRB_offsetAsInt64:$offset)),
        (LDRB AddrFI:$rn, AArch64Base_LDRB_offsetAsInt64:$offset)>;

def : Pat<(zextloadi8 S:$rn),
        (LDRB S:$rn, (i64 0))>;

def : Pat<(zextloadi8 AddrFI:$rn),
        (LDRB AddrFI:$rn, (i64 0))>;


def : Pat<(zextloadi16 (add S:$rn, (shl AArch64Base_LDRH_offsetAsInt64:$offset, (i64 1)))),
        (LDRH S:$rn, AArch64Base_LDRH_offsetAsInt64:$offset)>;

def : Pat<(zextloadi16 (add AddrFI:$rn, (shl AArch64Base_LDRH_offsetAsInt64:$offset, (i64 1)))),
        (LDRH AddrFI:$rn, AArch64Base_LDRH_offsetAsInt64:$offset)>;


def : Pat<(sextloadi8 (add S:$rn, AArch64Base_LDRSBW_offsetAsInt64:$offset)),
        (LDRSBW S:$rn, AArch64Base_LDRSBW_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 (add AddrFI:$rn, AArch64Base_LDRSBW_offsetAsInt64:$offset)),
        (LDRSBW AddrFI:$rn, AArch64Base_LDRSBW_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 S:$rn),
        (LDRSBW S:$rn, (i64 0))>;

def : Pat<(extloadi8 S:$rn),
        (LDRSBW S:$rn, (i64 0))>;

def : Pat<(sextloadi8 AddrFI:$rn),
        (LDRSBW AddrFI:$rn, (i64 0))>;

def : Pat<(extloadi8 AddrFI:$rn),
        (LDRSBW AddrFI:$rn, (i64 0))>;


def : Pat<(sextloadi8 (add S:$rn, AArch64Base_LDRSBX_offsetAsInt64:$offset)),
        (LDRSBX S:$rn, AArch64Base_LDRSBX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 (add AddrFI:$rn, AArch64Base_LDRSBX_offsetAsInt64:$offset)),
        (LDRSBX AddrFI:$rn, AArch64Base_LDRSBX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 S:$rn),
        (LDRSBX S:$rn, (i64 0))>;

def : Pat<(extloadi8 S:$rn),
        (LDRSBX S:$rn, (i64 0))>;

def : Pat<(sextloadi8 AddrFI:$rn),
        (LDRSBX AddrFI:$rn, (i64 0))>;

def : Pat<(extloadi8 AddrFI:$rn),
        (LDRSBX AddrFI:$rn, (i64 0))>;


def : Pat<(sextloadi16 (add S:$rn, (shl AArch64Base_LDRSHW_offsetAsInt64:$offset, (i64 1)))),
        (LDRSHW S:$rn, AArch64Base_LDRSHW_offsetAsInt64:$offset)>;

def : Pat<(sextloadi16 (add AddrFI:$rn, (shl AArch64Base_LDRSHW_offsetAsInt64:$offset, (i64 1)))),
        (LDRSHW AddrFI:$rn, AArch64Base_LDRSHW_offsetAsInt64:$offset)>;


def : Pat<(sextloadi16 (add S:$rn, (shl AArch64Base_LDRSHX_offsetAsInt64:$offset, (i64 1)))),
        (LDRSHX S:$rn, AArch64Base_LDRSHX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi16 (add AddrFI:$rn, (shl AArch64Base_LDRSHX_offsetAsInt64:$offset, (i64 1)))),
        (LDRSHX AddrFI:$rn, AArch64Base_LDRSHX_offsetAsInt64:$offset)>;


def : Pat<(sextloadi32 (add S:$rn, (shl AArch64Base_LDRSWX_offsetAsInt64:$offset, (i64 2)))),
        (LDRSWX S:$rn, AArch64Base_LDRSWX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi32 (add AddrFI:$rn, (shl AArch64Base_LDRSWX_offsetAsInt64:$offset, (i64 2)))),
        (LDRSWX AddrFI:$rn, AArch64Base_LDRSWX_offsetAsInt64:$offset)>;


def : Pat<(load (add S:$rn, (shl AArch64Base_LDRW_offsetAsInt64:$offset, (i64 2)))),
        (LDRW S:$rn, AArch64Base_LDRW_offsetAsInt64:$offset)>;

def : Pat<(load (add AddrFI:$rn, (shl AArch64Base_LDRW_offsetAsInt64:$offset, (i64 2)))),
        (LDRW AddrFI:$rn, AArch64Base_LDRW_offsetAsInt64:$offset)>;


def : Pat<(load (add S:$rn, (shl AArch64Base_LDRX_offsetAsInt64:$offset, (i64 3)))),
        (LDRX S:$rn, AArch64Base_LDRX_offsetAsInt64:$offset)>;

def : Pat<(load (add AddrFI:$rn, (shl AArch64Base_LDRX_offsetAsInt64:$offset, (i64 3)))),
        (LDRX AddrFI:$rn, AArch64Base_LDRX_offsetAsInt64:$offset)>;


def : Pat<(zextloadi8 (add S:$rn, AArch64Base_LDURB_offsetAsInt64:$offset)),
        (LDURB S:$rn, AArch64Base_LDURB_offsetAsInt64:$offset)>;

def : Pat<(zextloadi8 (add AddrFI:$rn, AArch64Base_LDURB_offsetAsInt64:$offset)),
        (LDURB AddrFI:$rn, AArch64Base_LDURB_offsetAsInt64:$offset)>;

def : Pat<(zextloadi8 S:$rn),
        (LDURB S:$rn, (i64 0))>;

def : Pat<(zextloadi8 AddrFI:$rn),
        (LDURB AddrFI:$rn, (i64 0))>;


def : Pat<(zextloadi16 (add S:$rn, AArch64Base_LDURH_offsetAsInt64:$offset)),
        (LDURH S:$rn, AArch64Base_LDURH_offsetAsInt64:$offset)>;

def : Pat<(zextloadi16 (add AddrFI:$rn, AArch64Base_LDURH_offsetAsInt64:$offset)),
        (LDURH AddrFI:$rn, AArch64Base_LDURH_offsetAsInt64:$offset)>;

def : Pat<(zextloadi16 S:$rn),
        (LDURH S:$rn, (i64 0))>;

def : Pat<(zextloadi16 AddrFI:$rn),
        (LDURH AddrFI:$rn, (i64 0))>;


def : Pat<(sextloadi8 (add S:$rn, AArch64Base_LDURSBW_offsetAsInt64:$offset)),
        (LDURSBW S:$rn, AArch64Base_LDURSBW_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 (add AddrFI:$rn, AArch64Base_LDURSBW_offsetAsInt64:$offset)),
        (LDURSBW AddrFI:$rn, AArch64Base_LDURSBW_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 S:$rn),
        (LDURSBW S:$rn, (i64 0))>;

def : Pat<(extloadi8 S:$rn),
        (LDURSBW S:$rn, (i64 0))>;

def : Pat<(sextloadi8 AddrFI:$rn),
        (LDURSBW AddrFI:$rn, (i64 0))>;

def : Pat<(extloadi8 AddrFI:$rn),
        (LDURSBW AddrFI:$rn, (i64 0))>;


def : Pat<(sextloadi8 (add S:$rn, AArch64Base_LDURSBX_offsetAsInt64:$offset)),
        (LDURSBX S:$rn, AArch64Base_LDURSBX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 (add AddrFI:$rn, AArch64Base_LDURSBX_offsetAsInt64:$offset)),
        (LDURSBX AddrFI:$rn, AArch64Base_LDURSBX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi8 S:$rn),
        (LDURSBX S:$rn, (i64 0))>;

def : Pat<(extloadi8 S:$rn),
        (LDURSBX S:$rn, (i64 0))>;

def : Pat<(sextloadi8 AddrFI:$rn),
        (LDURSBX AddrFI:$rn, (i64 0))>;

def : Pat<(extloadi8 AddrFI:$rn),
        (LDURSBX AddrFI:$rn, (i64 0))>;


def : Pat<(sextloadi16 (add S:$rn, AArch64Base_LDURSHW_offsetAsInt64:$offset)),
        (LDURSHW S:$rn, AArch64Base_LDURSHW_offsetAsInt64:$offset)>;

def : Pat<(sextloadi16 (add AddrFI:$rn, AArch64Base_LDURSHW_offsetAsInt64:$offset)),
        (LDURSHW AddrFI:$rn, AArch64Base_LDURSHW_offsetAsInt64:$offset)>;

def : Pat<(sextloadi16 S:$rn),
        (LDURSHW S:$rn, (i64 0))>;

def : Pat<(extloadi16 S:$rn),
        (LDURSHW S:$rn, (i64 0))>;

def : Pat<(sextloadi16 AddrFI:$rn),
        (LDURSHW AddrFI:$rn, (i64 0))>;

def : Pat<(extloadi16 AddrFI:$rn),
        (LDURSHW AddrFI:$rn, (i64 0))>;


def : Pat<(sextloadi16 (add S:$rn, AArch64Base_LDURSHX_offsetAsInt64:$offset)),
        (LDURSHX S:$rn, AArch64Base_LDURSHX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi16 (add AddrFI:$rn, AArch64Base_LDURSHX_offsetAsInt64:$offset)),
        (LDURSHX AddrFI:$rn, AArch64Base_LDURSHX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi16 S:$rn),
        (LDURSHX S:$rn, (i64 0))>;

def : Pat<(extloadi16 S:$rn),
        (LDURSHX S:$rn, (i64 0))>;

def : Pat<(sextloadi16 AddrFI:$rn),
        (LDURSHX AddrFI:$rn, (i64 0))>;

def : Pat<(extloadi16 AddrFI:$rn),
        (LDURSHX AddrFI:$rn, (i64 0))>;


def : Pat<(sextloadi32 (add S:$rn, AArch64Base_LDURSWX_offsetAsInt64:$offset)),
        (LDURSWX S:$rn, AArch64Base_LDURSWX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi32 (add AddrFI:$rn, AArch64Base_LDURSWX_offsetAsInt64:$offset)),
        (LDURSWX AddrFI:$rn, AArch64Base_LDURSWX_offsetAsInt64:$offset)>;

def : Pat<(sextloadi32 S:$rn),
        (LDURSWX S:$rn, (i64 0))>;

def : Pat<(extloadi32 S:$rn),
        (LDURSWX S:$rn, (i64 0))>;

def : Pat<(sextloadi32 AddrFI:$rn),
        (LDURSWX AddrFI:$rn, (i64 0))>;

def : Pat<(extloadi32 AddrFI:$rn),
        (LDURSWX AddrFI:$rn, (i64 0))>;


def : Pat<(load (add S:$rn, AArch64Base_LDURW_offsetAsInt64:$offset)),
        (LDURW S:$rn, AArch64Base_LDURW_offsetAsInt64:$offset)>;

def : Pat<(load (add AddrFI:$rn, AArch64Base_LDURW_offsetAsInt64:$offset)),
        (LDURW AddrFI:$rn, AArch64Base_LDURW_offsetAsInt64:$offset)>;

def : Pat<(load S:$rn),
        (LDURW S:$rn, (i64 0))>;

def : Pat<(load AddrFI:$rn),
        (LDURW AddrFI:$rn, (i64 0))>;


def : Pat<(load (add S:$rn, AArch64Base_LDURX_offsetAsInt64:$offset)),
        (LDURX S:$rn, AArch64Base_LDURX_offsetAsInt64:$offset)>;

def : Pat<(load (add AddrFI:$rn, AArch64Base_LDURX_offsetAsInt64:$offset)),
        (LDURX AddrFI:$rn, AArch64Base_LDURX_offsetAsInt64:$offset)>;

def : Pat<(load S:$rn),
        (LDURX S:$rn, (i64 0))>;

def : Pat<(load AddrFI:$rn),
        (LDURX AddrFI:$rn, (i64 0))>;


def : Pat<(shl W:$rn, W:$rm),
        (LSLW W:$rn, W:$rm)>;


def : Pat<(shl X:$rn, X:$rm),
        (LSLX X:$rn, X:$rm)>;


def : Pat<(srl W:$rn, W:$rm),
        (LSRW W:$rn, W:$rm)>;


def : Pat<(srl X:$rn, X:$rm),
        (LSRX X:$rn, X:$rm)>;


def : Pat<(add W:$ra, (mul W:$rn, W:$rm)),
        (MADDW W:$ra, W:$rn, W:$rm)>;


def : Pat<(add X:$ra, (mul X:$rn, X:$rm)),
        (MADDX X:$ra, X:$rn, X:$rm)>;


def : Pat<(zext AArch64Base_MOVZWPos00_imm16AsInt32:$imm16),
        (MOVZWPos00 AArch64Base_MOVZWPos00_imm16AsInt32:$imm16)>;


def : Pat<(shl (zext AArch64Base_MOVZWPos16_imm16AsInt32:$imm16), (i64 16)),
        (MOVZWPos16 AArch64Base_MOVZWPos16_imm16AsInt32:$imm16)>;


def : Pat<(zext AArch64Base_MOVZXPos00_imm16AsInt32:$imm16),
        (MOVZXPos00 AArch64Base_MOVZXPos00_imm16AsInt32:$imm16)>;


def : Pat<(shl (zext AArch64Base_MOVZXPos16_imm16AsInt32:$imm16), (i64 16)),
        (MOVZXPos16 AArch64Base_MOVZXPos16_imm16AsInt32:$imm16)>;


def : Pat<(shl (zext AArch64Base_MOVZXPos32_imm16AsInt32:$imm16), (i64 32)),
        (MOVZXPos32 AArch64Base_MOVZXPos32_imm16AsInt32:$imm16)>;


def : Pat<(shl (zext AArch64Base_MOVZXPos48_imm16AsInt32:$imm16), (i64 48)),
        (MOVZXPos48 AArch64Base_MOVZXPos48_imm16AsInt32:$imm16)>;


def : Pat<(sub W:$ra, (mul W:$rn, W:$rm)),
        (MSUBW W:$ra, W:$rn, W:$rm)>;


def : Pat<(sub X:$ra, (mul X:$rn, X:$rm)),
        (MSUBX X:$ra, X:$rn, X:$rm)>;


def : Pat<(or W:$rn, AArch64Base_ORRIW_immWSizeAsInt32:$immWSize),
        (ORRIW W:$rn, AArch64Base_ORRIW_immWSizeAsInt32:$immWSize)>;


def : Pat<(or X:$rn, AArch64Base_ORRIX_immXSizeAsInt64:$immXSize),
        (ORRIX X:$rn, AArch64Base_ORRIX_immXSizeAsInt64:$immXSize)>;


def : Pat<(or W:$rn, W:$rm),
        (ORRW W:$rn, W:$rm)>;


def : Pat<(or W:$rn, (sra W:$rm, AArch64Base_ORRWASR_imm6AsInt32:$imm6)),
        (ORRWASR W:$rn, W:$rm, AArch64Base_ORRWASR_imm6AsInt32:$imm6)>;


def : Pat<(or W:$rn, (shl W:$rm, AArch64Base_ORRWLSL_imm6AsInt32:$imm6)),
        (ORRWLSL W:$rn, W:$rm, AArch64Base_ORRWLSL_imm6AsInt32:$imm6)>;


def : Pat<(or W:$rn, (srl W:$rm, AArch64Base_ORRWLSR_imm6AsInt32:$imm6)),
        (ORRWLSR W:$rn, W:$rm, AArch64Base_ORRWLSR_imm6AsInt32:$imm6)>;


def : Pat<(or X:$rn, X:$rm),
        (ORRX X:$rn, X:$rm)>;


def : Pat<(or X:$rn, (sra X:$rm, AArch64Base_ORRXASR_imm6AsInt32:$imm6)),
        (ORRXASR X:$rn, X:$rm, AArch64Base_ORRXASR_imm6AsInt32:$imm6)>;


def : Pat<(or X:$rn, (shl X:$rm, AArch64Base_ORRXLSL_imm6AsInt32:$imm6)),
        (ORRXLSL X:$rn, X:$rm, AArch64Base_ORRXLSL_imm6AsInt32:$imm6)>;


def : Pat<(or X:$rn, (srl X:$rm, AArch64Base_ORRXLSR_imm6AsInt32:$imm6)),
        (ORRXLSR X:$rn, X:$rm, AArch64Base_ORRXLSR_imm6AsInt32:$imm6)>;


def : Pat<(sra (shl W:$rn, AArch64Base_SBFMW_leftWSizeAsInt32:$leftWSize), AArch64Base_SBFMW_rightWSizeAsInt32:$rightWSize),
        (SBFMW W:$rn, AArch64Base_SBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_SBFMW_rightWSizeAsInt32:$rightWSize)>;


def : Pat<(sra (shl X:$rn, AArch64Base_SBFMX_leftXSizeAsInt32:$leftXSize), AArch64Base_SBFMX_rightXSizeAsInt32:$rightXSize),
        (SBFMX X:$rn, AArch64Base_SBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_SBFMX_rightXSizeAsInt32:$rightXSize)>;


def : Pat<(sdiv W:$rn, W:$rm),
        (SDIVW W:$rm, W:$rn)>;


def : Pat<(sdiv X:$rn, X:$rm),
        (SDIVX X:$rm, X:$rn)>;


def : Pat<(mulhs X:$rn, X:$rm),
        (SMULH X:$rn, X:$rm)>;


def : Pat<(truncstorei8 W:$rt, (add S:$rn, AArch64Base_STRB_offsetAsInt64:$offset)),
        (STRB S:$rn, W:$rt, AArch64Base_STRB_offsetAsInt64:$offset)>;

def : Pat<(truncstorei8 W:$rt, (add AddrFI:$rn, AArch64Base_STRB_offsetAsInt64:$offset)),
        (STRB AddrFI:$rn, W:$rt, AArch64Base_STRB_offsetAsInt64:$offset)>;

def : Pat<(truncstorei8 W:$rt, S:$rn),
        (STRB S:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei8 S:$rt, S:$rn),
        (STRB S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei8 S:$rt, (add S:$rn, AArch64Base_STRB_offsetAsInt64:$offset)),
        (STRB S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STRB_offsetAsInt64:$offset)>;

def : Pat<(truncstorei8 W:$rt, AddrFI:$rn),
        (STRB AddrFI:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei8 S:$rt, AddrFI:$rn),
        (STRB AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei8 S:$rt, (add AddrFI:$rn, AArch64Base_STRB_offsetAsInt64:$offset)),
        (STRB AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STRB_offsetAsInt64:$offset)>;


def : Pat<(truncstorei16 W:$rt, (add S:$rn, (shl AArch64Base_STRH_offsetAsInt64:$offset, (i64 1)))),
        (STRH S:$rn, W:$rt, AArch64Base_STRH_offsetAsInt64:$offset)>;

def : Pat<(truncstorei16 W:$rt, (add AddrFI:$rn, (shl AArch64Base_STRH_offsetAsInt64:$offset, (i64 1)))),
        (STRH AddrFI:$rn, W:$rt, AArch64Base_STRH_offsetAsInt64:$offset)>;

def : Pat<(truncstorei16 S:$rt, (add S:$rn, (shl AArch64Base_STRH_offsetAsInt64:$offset, (i64 1)))),
        (STRH S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STRH_offsetAsInt64:$offset)>;

def : Pat<(truncstorei16 S:$rt, (add AddrFI:$rn, (shl AArch64Base_STRH_offsetAsInt64:$offset, (i64 1)))),
        (STRH AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STRH_offsetAsInt64:$offset)>;


def : Pat<(store W:$rt, (add S:$rn, (shl AArch64Base_STRW_offsetAsInt64:$offset, (i64 2)))),
        (STRW S:$rn, W:$rt, AArch64Base_STRW_offsetAsInt64:$offset)>;

def : Pat<(store W:$rt, (add AddrFI:$rn, (shl AArch64Base_STRW_offsetAsInt64:$offset, (i64 2)))),
        (STRW AddrFI:$rn, W:$rt, AArch64Base_STRW_offsetAsInt64:$offset)>;

def : Pat<(truncstorei32 S:$rt, (add S:$rn, (shl AArch64Base_STRW_offsetAsInt64:$offset, (i64 2)))),
        (STRW S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STRW_offsetAsInt64:$offset)>;

def : Pat<(truncstorei32 S:$rt, (add AddrFI:$rn, (shl AArch64Base_STRW_offsetAsInt64:$offset, (i64 2)))),
        (STRW AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STRW_offsetAsInt64:$offset)>;


def : Pat<(store X:$rt, (add S:$rn, (shl AArch64Base_STRX_offsetAsInt64:$offset, (i64 3)))),
        (STRX S:$rn, X:$rt, AArch64Base_STRX_offsetAsInt64:$offset)>;

def : Pat<(store X:$rt, (add AddrFI:$rn, (shl AArch64Base_STRX_offsetAsInt64:$offset, (i64 3)))),
        (STRX AddrFI:$rn, X:$rt, AArch64Base_STRX_offsetAsInt64:$offset)>;

def : Pat<(truncstorei64 S:$rt, (add S:$rn, (shl AArch64Base_STRX_offsetAsInt64:$offset, (i64 3)))),
        (STRX S:$rn, (EXTRACT_SUBREG S:$rt, FULL_64), AArch64Base_STRX_offsetAsInt64:$offset)>;

def : Pat<(truncstorei64 S:$rt, (add AddrFI:$rn, (shl AArch64Base_STRX_offsetAsInt64:$offset, (i64 3)))),
        (STRX AddrFI:$rn, (EXTRACT_SUBREG S:$rt, FULL_64), AArch64Base_STRX_offsetAsInt64:$offset)>;


def : Pat<(truncstorei8 W:$rt, (add S:$rn, AArch64Base_STURB_offsetAsInt64:$offset)),
        (STURB S:$rn, W:$rt, AArch64Base_STURB_offsetAsInt64:$offset)>;

def : Pat<(truncstorei8 W:$rt, (add AddrFI:$rn, AArch64Base_STURB_offsetAsInt64:$offset)),
        (STURB AddrFI:$rn, W:$rt, AArch64Base_STURB_offsetAsInt64:$offset)>;

def : Pat<(truncstorei8 W:$rt, S:$rn),
        (STURB S:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei8 S:$rt, S:$rn),
        (STURB S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei8 S:$rt, (add S:$rn, AArch64Base_STURB_offsetAsInt64:$offset)),
        (STURB S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STURB_offsetAsInt64:$offset)>;

def : Pat<(truncstorei8 W:$rt, AddrFI:$rn),
        (STURB AddrFI:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei8 S:$rt, AddrFI:$rn),
        (STURB AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei8 S:$rt, (add AddrFI:$rn, AArch64Base_STURB_offsetAsInt64:$offset)),
        (STURB AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STURB_offsetAsInt64:$offset)>;


def : Pat<(truncstorei16 W:$rt, (add S:$rn, AArch64Base_STURH_offsetAsInt64:$offset)),
        (STURH S:$rn, W:$rt, AArch64Base_STURH_offsetAsInt64:$offset)>;

def : Pat<(truncstorei16 W:$rt, (add AddrFI:$rn, AArch64Base_STURH_offsetAsInt64:$offset)),
        (STURH AddrFI:$rn, W:$rt, AArch64Base_STURH_offsetAsInt64:$offset)>;

def : Pat<(truncstorei16 W:$rt, S:$rn),
        (STURH S:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei16 S:$rt, S:$rn),
        (STURH S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei16 S:$rt, (add S:$rn, AArch64Base_STURH_offsetAsInt64:$offset)),
        (STURH S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STURH_offsetAsInt64:$offset)>;

def : Pat<(truncstorei16 W:$rt, AddrFI:$rn),
        (STURH AddrFI:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei16 S:$rt, AddrFI:$rn),
        (STURH AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei16 S:$rt, (add AddrFI:$rn, AArch64Base_STURH_offsetAsInt64:$offset)),
        (STURH AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STURH_offsetAsInt64:$offset)>;


def : Pat<(store W:$rt, (add S:$rn, AArch64Base_STURW_offsetAsInt64:$offset)),
        (STURW S:$rn, W:$rt, AArch64Base_STURW_offsetAsInt64:$offset)>;

def : Pat<(store W:$rt, (add AddrFI:$rn, AArch64Base_STURW_offsetAsInt64:$offset)),
        (STURW AddrFI:$rn, W:$rt, AArch64Base_STURW_offsetAsInt64:$offset)>;

def : Pat<(store W:$rt, S:$rn),
        (STURW S:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei32 S:$rt, S:$rn),
        (STURW S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei32 S:$rt, (add S:$rn, AArch64Base_STURW_offsetAsInt64:$offset)),
        (STURW S:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STURW_offsetAsInt64:$offset)>;

def : Pat<(store W:$rt, AddrFI:$rn),
        (STURW AddrFI:$rn, W:$rt, (i64 0))>;

def : Pat<(truncstorei32 S:$rt, AddrFI:$rn),
        (STURW AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), (i64 0))>;

def : Pat<(truncstorei32 S:$rt, (add AddrFI:$rn, AArch64Base_STURW_offsetAsInt64:$offset)),
        (STURW AddrFI:$rn, (EXTRACT_SUBREG S:$rt, SUB_32), AArch64Base_STURW_offsetAsInt64:$offset)>;


def : Pat<(store X:$rt, (add S:$rn, AArch64Base_STURX_offsetAsInt64:$offset)),
        (STURX S:$rn, X:$rt, AArch64Base_STURX_offsetAsInt64:$offset)>;

def : Pat<(store X:$rt, (add AddrFI:$rn, AArch64Base_STURX_offsetAsInt64:$offset)),
        (STURX AddrFI:$rn, X:$rt, AArch64Base_STURX_offsetAsInt64:$offset)>;

def : Pat<(store X:$rt, S:$rn),
        (STURX S:$rn, X:$rt, (i64 0))>;

def : Pat<(truncstorei64 S:$rt, S:$rn),
        (STURX S:$rn, (EXTRACT_SUBREG S:$rt, FULL_64), (i64 0))>;

def : Pat<(truncstorei64 S:$rt, (add S:$rn, AArch64Base_STURX_offsetAsInt64:$offset)),
        (STURX S:$rn, (EXTRACT_SUBREG S:$rt, FULL_64), AArch64Base_STURX_offsetAsInt64:$offset)>;

def : Pat<(store X:$rt, AddrFI:$rn),
        (STURX AddrFI:$rn, X:$rt, (i64 0))>;

def : Pat<(truncstorei64 S:$rt, AddrFI:$rn),
        (STURX AddrFI:$rn, (EXTRACT_SUBREG S:$rt, FULL_64), (i64 0))>;

def : Pat<(truncstorei64 S:$rt, (add AddrFI:$rn, AArch64Base_STURX_offsetAsInt64:$offset)),
        (STURX AddrFI:$rn, (EXTRACT_SUBREG S:$rt, FULL_64), AArch64Base_STURX_offsetAsInt64:$offset)>;


def : Pat<(sub W:$rn, W:$rm),
        (SUBW W:$rn, W:$rm)>;


def : Pat<(sub W:$rn, (sra W:$rm, AArch64Base_SUBWASR_imm6AsInt32:$imm6)),
        (SUBWASR W:$rn, W:$rm, AArch64Base_SUBWASR_imm6AsInt32:$imm6)>;


def : Pat<(sub W:$rn, (shl W:$rm, AArch64Base_SUBWLSL_imm6AsInt32:$imm6)),
        (SUBWLSL W:$rn, W:$rm, AArch64Base_SUBWLSL_imm6AsInt32:$imm6)>;


def : Pat<(sub W:$rn, (srl W:$rm, AArch64Base_SUBWLSR_imm6AsInt32:$imm6)),
        (SUBWLSR W:$rn, W:$rm, AArch64Base_SUBWLSR_imm6AsInt32:$imm6)>;


def : Pat<(sub W:$rn, W:$rm),
        (SUBWS W:$rn, W:$rm)>;


def : Pat<(sub W:$rn, (sra W:$rm, AArch64Base_SUBWSASR_imm6AsInt32:$imm6)),
        (SUBWSASR W:$rn, W:$rm, AArch64Base_SUBWSASR_imm6AsInt32:$imm6)>;


def : Pat<(sub W:$rn, (shl W:$rm, AArch64Base_SUBWSLSL_imm6AsInt32:$imm6)),
        (SUBWSLSL W:$rn, W:$rm, AArch64Base_SUBWSLSL_imm6AsInt32:$imm6)>;


def : Pat<(sub W:$rn, (srl W:$rm, AArch64Base_SUBWSLSR_imm6AsInt32:$imm6)),
        (SUBWSLSR W:$rn, W:$rm, AArch64Base_SUBWSLSR_imm6AsInt32:$imm6)>;


def : Pat<(sub X:$rn, X:$rm),
        (SUBX X:$rn, X:$rm)>;


def : Pat<(sub X:$rn, (sra X:$rm, AArch64Base_SUBXASR_imm6AsInt32:$imm6)),
        (SUBXASR X:$rn, X:$rm, AArch64Base_SUBXASR_imm6AsInt32:$imm6)>;


def : Pat<(sub S:$rn, AArch64Base_SUBXI_imm12XAsInt64:$imm12X),
        (SUBXI S:$rn, AArch64Base_SUBXI_imm12XAsInt64:$imm12X)>;


def : Pat<(sub S:$rn, AArch64Base_SUBXI12_imm12SAsInt64:$imm12S),
        (SUBXI12 S:$rn, AArch64Base_SUBXI12_imm12SAsInt64:$imm12S)>;


def : Pat<(sub X:$rn, (shl X:$rm, AArch64Base_SUBXLSL_imm6AsInt32:$imm6)),
        (SUBXLSL X:$rn, X:$rm, AArch64Base_SUBXLSL_imm6AsInt32:$imm6)>;


def : Pat<(sub X:$rn, (srl X:$rm, AArch64Base_SUBXLSR_imm6AsInt32:$imm6)),
        (SUBXLSR X:$rn, X:$rm, AArch64Base_SUBXLSR_imm6AsInt32:$imm6)>;


def : Pat<(sub X:$rn, X:$rm),
        (SUBXS X:$rn, X:$rm)>;


def : Pat<(sub X:$rn, (sra X:$rm, AArch64Base_SUBXSASR_imm6AsInt32:$imm6)),
        (SUBXSASR X:$rn, X:$rm, AArch64Base_SUBXSASR_imm6AsInt32:$imm6)>;


def : Pat<(sub S:$rn, AArch64Base_SUBXSI_imm12XAsInt64:$imm12X),
        (SUBXSI S:$rn, AArch64Base_SUBXSI_imm12XAsInt64:$imm12X)>;


def : Pat<(sub S:$rn, AArch64Base_SUBXSI12_imm12SAsInt64:$imm12S),
        (SUBXSI12 S:$rn, AArch64Base_SUBXSI12_imm12SAsInt64:$imm12S)>;


def : Pat<(sub X:$rn, (shl X:$rm, AArch64Base_SUBXSLSL_imm6AsInt32:$imm6)),
        (SUBXSLSL X:$rn, X:$rm, AArch64Base_SUBXSLSL_imm6AsInt32:$imm6)>;


def : Pat<(sub X:$rn, (srl X:$rm, AArch64Base_SUBXSLSR_imm6AsInt32:$imm6)),
        (SUBXSLSR X:$rn, X:$rm, AArch64Base_SUBXSLSR_imm6AsInt32:$imm6)>;


def : Pat<(sub S:$rn, (shl X:$rm, AArch64Base_SUBXSSXSX_imm3AsInt32:$imm3)),
        (SUBXSSXSX S:$rn, X:$rm, AArch64Base_SUBXSSXSX_imm3AsInt32:$imm3)>;


def : Pat<(sub S:$rn, X:$rm),
        (SUBXSSXTX S:$rn, X:$rm)>;


def : Pat<(sub S:$rn, (shl X:$rm, AArch64Base_SUBXSUXSX_imm3AsInt32:$imm3)),
        (SUBXSUXSX S:$rn, X:$rm, AArch64Base_SUBXSUXSX_imm3AsInt32:$imm3)>;


def : Pat<(sub S:$rn, X:$rm),
        (SUBXSUXTX S:$rn, X:$rm)>;


def : Pat<(sub S:$rn, (shl X:$rm, AArch64Base_SUBXSXSX_imm3AsInt32:$imm3)),
        (SUBXSXSX S:$rn, X:$rm, AArch64Base_SUBXSXSX_imm3AsInt32:$imm3)>;


def : Pat<(sub S:$rn, X:$rm),
        (SUBXSXTX S:$rn, X:$rm)>;


def : Pat<(sub S:$rn, (shl X:$rm, AArch64Base_SUBXUXSX_imm3AsInt32:$imm3)),
        (SUBXUXSX S:$rn, X:$rm, AArch64Base_SUBXUXSX_imm3AsInt32:$imm3)>;


def : Pat<(sub S:$rn, X:$rm),
        (SUBXUXTX S:$rn, X:$rm)>;


def : Pat<(srl (shl W:$rn, AArch64Base_UBFMW_leftWSizeAsInt32:$leftWSize), AArch64Base_UBFMW_rightWSizeAsInt32:$rightWSize),
        (UBFMW W:$rn, AArch64Base_UBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_UBFMW_rightWSizeAsInt32:$rightWSize)>;


def : Pat<(srl (shl X:$rn, AArch64Base_UBFMX_leftXSizeAsInt32:$leftXSize), AArch64Base_UBFMX_rightXSizeAsInt32:$rightXSize),
        (UBFMX X:$rn, AArch64Base_UBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_UBFMX_rightXSizeAsInt32:$rightXSize)>;


def : Pat<(udiv W:$rn, W:$rm),
        (UDIVW W:$rm, W:$rn)>;


def : Pat<(udiv X:$rn, X:$rm),
        (UDIVX X:$rm, X:$rn)>;


def : Pat<(mulhu X:$rn, X:$rm),
        (UMULH X:$rn, X:$rm)>;






/* Match the call sequence for global and external symbols */
def : Pat<(target_call tglobaladdr:$func), (BL tglobaladdr:$func)>;
def : Pat<(target_call texternalsym:$func), (BL texternalsym:$func)>;

/* Intrinsics */


def : Pat<(int_processorNameValue_ADCSW W:$rn, W:$rm),
        (ADCSW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ADCSX X:$rn, X:$rm),
        (ADCSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADCW W:$rn, W:$rm),
        (ADCW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ADCX X:$rn, X:$rm),
        (ADCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDW W:$rn, W:$rm),
        (ADDW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ADDWASR W:$rn, W:$rm, AArch64Base_ADDWASR_imm6AsInt32:$imm6),
        (ADDWASR W:$rn, W:$rm, AArch64Base_ADDWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDWI R:$rn, AArch64Base_ADDWI_imm12XAsInt64:$imm12X),
        (ADDWI R:$rn, AArch64Base_ADDWI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_ADDWI12 R:$rn, AArch64Base_ADDWI12_imm12SAsInt64:$imm12S),
        (ADDWI12 R:$rn, AArch64Base_ADDWI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_ADDWLSL W:$rn, W:$rm, AArch64Base_ADDWLSL_imm6AsInt32:$imm6),
        (ADDWLSL W:$rn, W:$rm, AArch64Base_ADDWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDWLSR W:$rn, W:$rm, AArch64Base_ADDWLSR_imm6AsInt32:$imm6),
        (ADDWLSR W:$rn, W:$rm, AArch64Base_ADDWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDWS W:$rn, W:$rm),
        (ADDWS W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ADDWSASR W:$rn, W:$rm, AArch64Base_ADDWSASR_imm6AsInt32:$imm6),
        (ADDWSASR W:$rn, W:$rm, AArch64Base_ADDWSASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDWSI R:$rn, AArch64Base_ADDWSI_imm12XAsInt64:$imm12X),
        (ADDWSI R:$rn, AArch64Base_ADDWSI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_ADDWSI12 R:$rn, AArch64Base_ADDWSI12_imm12SAsInt64:$imm12S),
        (ADDWSI12 R:$rn, AArch64Base_ADDWSI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_ADDWSLSL W:$rn, W:$rm, AArch64Base_ADDWSLSL_imm6AsInt32:$imm6),
        (ADDWSLSL W:$rn, W:$rm, AArch64Base_ADDWSLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDWSLSR W:$rn, W:$rm, AArch64Base_ADDWSLSR_imm6AsInt32:$imm6),
        (ADDWSLSR W:$rn, W:$rm, AArch64Base_ADDWSLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDWSSXSB R:$rn, X:$rm, AArch64Base_ADDWSSXSB_imm3AsInt32:$imm3),
        (ADDWSSXSB R:$rn, X:$rm, AArch64Base_ADDWSSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSSXSH R:$rn, X:$rm, AArch64Base_ADDWSSXSH_imm3AsInt32:$imm3),
        (ADDWSSXSH R:$rn, X:$rm, AArch64Base_ADDWSSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSSXSW R:$rn, X:$rm, AArch64Base_ADDWSSXSW_imm3AsInt32:$imm3),
        (ADDWSSXSW R:$rn, X:$rm, AArch64Base_ADDWSSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSSXSX R:$rn, X:$rm, AArch64Base_ADDWSSXSX_imm3AsInt32:$imm3),
        (ADDWSSXSX R:$rn, X:$rm, AArch64Base_ADDWSSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSSXTB R:$rn, X:$rm),
        (ADDWSSXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSSXTH R:$rn, X:$rm),
        (ADDWSSXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSSXTW R:$rn, X:$rm),
        (ADDWSSXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSSXTX R:$rn, X:$rm),
        (ADDWSSXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSUXSB R:$rn, X:$rm, AArch64Base_ADDWSUXSB_imm3AsInt32:$imm3),
        (ADDWSUXSB R:$rn, X:$rm, AArch64Base_ADDWSUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSUXSH R:$rn, X:$rm, AArch64Base_ADDWSUXSH_imm3AsInt32:$imm3),
        (ADDWSUXSH R:$rn, X:$rm, AArch64Base_ADDWSUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSUXSW R:$rn, X:$rm, AArch64Base_ADDWSUXSW_imm3AsInt32:$imm3),
        (ADDWSUXSW R:$rn, X:$rm, AArch64Base_ADDWSUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSUXSX R:$rn, X:$rm, AArch64Base_ADDWSUXSX_imm3AsInt32:$imm3),
        (ADDWSUXSX R:$rn, X:$rm, AArch64Base_ADDWSUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSUXTB R:$rn, X:$rm),
        (ADDWSUXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSUXTH R:$rn, X:$rm),
        (ADDWSUXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSUXTW R:$rn, X:$rm),
        (ADDWSUXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSUXTX R:$rn, X:$rm),
        (ADDWSUXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSXSB R:$rn, X:$rm, AArch64Base_ADDWSXSB_imm3AsInt32:$imm3),
        (ADDWSXSB R:$rn, X:$rm, AArch64Base_ADDWSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSXSH R:$rn, X:$rm, AArch64Base_ADDWSXSH_imm3AsInt32:$imm3),
        (ADDWSXSH R:$rn, X:$rm, AArch64Base_ADDWSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSXSW R:$rn, X:$rm, AArch64Base_ADDWSXSW_imm3AsInt32:$imm3),
        (ADDWSXSW R:$rn, X:$rm, AArch64Base_ADDWSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSXSX R:$rn, X:$rm, AArch64Base_ADDWSXSX_imm3AsInt32:$imm3),
        (ADDWSXSX R:$rn, X:$rm, AArch64Base_ADDWSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWSXTB R:$rn, X:$rm),
        (ADDWSXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSXTH R:$rn, X:$rm),
        (ADDWSXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSXTW R:$rn, X:$rm),
        (ADDWSXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWSXTX R:$rn, X:$rm),
        (ADDWSXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWUXSB R:$rn, X:$rm, AArch64Base_ADDWUXSB_imm3AsInt32:$imm3),
        (ADDWUXSB R:$rn, X:$rm, AArch64Base_ADDWUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWUXSH R:$rn, X:$rm, AArch64Base_ADDWUXSH_imm3AsInt32:$imm3),
        (ADDWUXSH R:$rn, X:$rm, AArch64Base_ADDWUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWUXSW R:$rn, X:$rm, AArch64Base_ADDWUXSW_imm3AsInt32:$imm3),
        (ADDWUXSW R:$rn, X:$rm, AArch64Base_ADDWUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWUXSX R:$rn, X:$rm, AArch64Base_ADDWUXSX_imm3AsInt32:$imm3),
        (ADDWUXSX R:$rn, X:$rm, AArch64Base_ADDWUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDWUXTB R:$rn, X:$rm),
        (ADDWUXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWUXTH R:$rn, X:$rm),
        (ADDWUXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWUXTW R:$rn, X:$rm),
        (ADDWUXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDWUXTX R:$rn, X:$rm),
        (ADDWUXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDX X:$rn, X:$rm),
        (ADDX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXASR X:$rn, X:$rm, AArch64Base_ADDXASR_imm6AsInt32:$imm6),
        (ADDXASR X:$rn, X:$rm, AArch64Base_ADDXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDXI S:$rn, AArch64Base_ADDXI_imm12XAsInt64:$imm12X),
        (ADDXI S:$rn, AArch64Base_ADDXI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_ADDXI12 S:$rn, AArch64Base_ADDXI12_imm12SAsInt64:$imm12S),
        (ADDXI12 S:$rn, AArch64Base_ADDXI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_ADDXLSL X:$rn, X:$rm, AArch64Base_ADDXLSL_imm6AsInt32:$imm6),
        (ADDXLSL X:$rn, X:$rm, AArch64Base_ADDXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDXLSR X:$rn, X:$rm, AArch64Base_ADDXLSR_imm6AsInt32:$imm6),
        (ADDXLSR X:$rn, X:$rm, AArch64Base_ADDXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDXS X:$rn, X:$rm),
        (ADDXS X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSASR X:$rn, X:$rm, AArch64Base_ADDXSASR_imm6AsInt32:$imm6),
        (ADDXSASR X:$rn, X:$rm, AArch64Base_ADDXSASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDXSI S:$rn, AArch64Base_ADDXSI_imm12XAsInt64:$imm12X),
        (ADDXSI S:$rn, AArch64Base_ADDXSI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_ADDXSI12 S:$rn, AArch64Base_ADDXSI12_imm12SAsInt64:$imm12S),
        (ADDXSI12 S:$rn, AArch64Base_ADDXSI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_ADDXSLSL X:$rn, X:$rm, AArch64Base_ADDXSLSL_imm6AsInt32:$imm6),
        (ADDXSLSL X:$rn, X:$rm, AArch64Base_ADDXSLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDXSLSR X:$rn, X:$rm, AArch64Base_ADDXSLSR_imm6AsInt32:$imm6),
        (ADDXSLSR X:$rn, X:$rm, AArch64Base_ADDXSLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ADDXSSXSB S:$rn, X:$rm, AArch64Base_ADDXSSXSB_imm3AsInt32:$imm3),
        (ADDXSSXSB S:$rn, X:$rm, AArch64Base_ADDXSSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSSXSH S:$rn, X:$rm, AArch64Base_ADDXSSXSH_imm3AsInt32:$imm3),
        (ADDXSSXSH S:$rn, X:$rm, AArch64Base_ADDXSSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSSXSW S:$rn, X:$rm, AArch64Base_ADDXSSXSW_imm3AsInt32:$imm3),
        (ADDXSSXSW S:$rn, X:$rm, AArch64Base_ADDXSSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSSXSX S:$rn, X:$rm, AArch64Base_ADDXSSXSX_imm3AsInt32:$imm3),
        (ADDXSSXSX S:$rn, X:$rm, AArch64Base_ADDXSSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSSXTB S:$rn, X:$rm),
        (ADDXSSXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSSXTH S:$rn, X:$rm),
        (ADDXSSXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSSXTW S:$rn, X:$rm),
        (ADDXSSXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSSXTX S:$rn, X:$rm),
        (ADDXSSXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSUXSB S:$rn, X:$rm, AArch64Base_ADDXSUXSB_imm3AsInt32:$imm3),
        (ADDXSUXSB S:$rn, X:$rm, AArch64Base_ADDXSUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSUXSH S:$rn, X:$rm, AArch64Base_ADDXSUXSH_imm3AsInt32:$imm3),
        (ADDXSUXSH S:$rn, X:$rm, AArch64Base_ADDXSUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSUXSW S:$rn, X:$rm, AArch64Base_ADDXSUXSW_imm3AsInt32:$imm3),
        (ADDXSUXSW S:$rn, X:$rm, AArch64Base_ADDXSUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSUXSX S:$rn, X:$rm, AArch64Base_ADDXSUXSX_imm3AsInt32:$imm3),
        (ADDXSUXSX S:$rn, X:$rm, AArch64Base_ADDXSUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSUXTB S:$rn, X:$rm),
        (ADDXSUXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSUXTH S:$rn, X:$rm),
        (ADDXSUXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSUXTW S:$rn, X:$rm),
        (ADDXSUXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSUXTX S:$rn, X:$rm),
        (ADDXSUXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSXSB S:$rn, X:$rm, AArch64Base_ADDXSXSB_imm3AsInt32:$imm3),
        (ADDXSXSB S:$rn, X:$rm, AArch64Base_ADDXSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSXSH S:$rn, X:$rm, AArch64Base_ADDXSXSH_imm3AsInt32:$imm3),
        (ADDXSXSH S:$rn, X:$rm, AArch64Base_ADDXSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSXSW S:$rn, X:$rm, AArch64Base_ADDXSXSW_imm3AsInt32:$imm3),
        (ADDXSXSW S:$rn, X:$rm, AArch64Base_ADDXSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSXSX S:$rn, X:$rm, AArch64Base_ADDXSXSX_imm3AsInt32:$imm3),
        (ADDXSXSX S:$rn, X:$rm, AArch64Base_ADDXSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXSXTB S:$rn, X:$rm),
        (ADDXSXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSXTH S:$rn, X:$rm),
        (ADDXSXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSXTW S:$rn, X:$rm),
        (ADDXSXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXSXTX S:$rn, X:$rm),
        (ADDXSXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXUXSB S:$rn, X:$rm, AArch64Base_ADDXUXSB_imm3AsInt32:$imm3),
        (ADDXUXSB S:$rn, X:$rm, AArch64Base_ADDXUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXUXSH S:$rn, X:$rm, AArch64Base_ADDXUXSH_imm3AsInt32:$imm3),
        (ADDXUXSH S:$rn, X:$rm, AArch64Base_ADDXUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXUXSW S:$rn, X:$rm, AArch64Base_ADDXUXSW_imm3AsInt32:$imm3),
        (ADDXUXSW S:$rn, X:$rm, AArch64Base_ADDXUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXUXSX S:$rn, X:$rm, AArch64Base_ADDXUXSX_imm3AsInt32:$imm3),
        (ADDXUXSX S:$rn, X:$rm, AArch64Base_ADDXUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_ADDXUXTB S:$rn, X:$rm),
        (ADDXUXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXUXTH S:$rn, X:$rm),
        (ADDXUXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXUXTW S:$rn, X:$rm),
        (ADDXUXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ADDXUXTX S:$rn, X:$rm),
        (ADDXUXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ANDIW W:$rn, AArch64Base_ANDIW_immWSizeAsInt32:$immWSize),
        (ANDIW W:$rn, AArch64Base_ANDIW_immWSizeAsInt32:$immWSize)>;

def : Pat<(int_processorNameValue_ANDIX X:$rn, AArch64Base_ANDIX_immXSizeAsInt64:$immXSize),
        (ANDIX X:$rn, AArch64Base_ANDIX_immXSizeAsInt64:$immXSize)>;

def : Pat<(int_processorNameValue_ANDSIW W:$rn, AArch64Base_ANDSIW_immWSizeAsInt32:$immWSize),
        (ANDSIW W:$rn, AArch64Base_ANDSIW_immWSizeAsInt32:$immWSize)>;

def : Pat<(int_processorNameValue_ANDSIX X:$rn, AArch64Base_ANDSIX_immXSizeAsInt64:$immXSize),
        (ANDSIX X:$rn, AArch64Base_ANDSIX_immXSizeAsInt64:$immXSize)>;

def : Pat<(int_processorNameValue_ANDSW W:$rn, W:$rm),
        (ANDSW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ANDSWASR W:$rn, W:$rm, AArch64Base_ANDSWASR_imm6AsInt32:$imm6),
        (ANDSWASR W:$rn, W:$rm, AArch64Base_ANDSWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDSWLSL W:$rn, W:$rm, AArch64Base_ANDSWLSL_imm6AsInt32:$imm6),
        (ANDSWLSL W:$rn, W:$rm, AArch64Base_ANDSWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDSWLSR W:$rn, W:$rm, AArch64Base_ANDSWLSR_imm6AsInt32:$imm6),
        (ANDSWLSR W:$rn, W:$rm, AArch64Base_ANDSWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDSWROR W:$rn, W:$rm, AArch64Base_ANDSWROR_imm6AsInt32:$imm6),
        (ANDSWROR W:$rn, W:$rm, AArch64Base_ANDSWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDSX X:$rn, X:$rm),
        (ANDSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ANDSXASR X:$rn, X:$rm, AArch64Base_ANDSXASR_imm6AsInt32:$imm6),
        (ANDSXASR X:$rn, X:$rm, AArch64Base_ANDSXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDSXLSL X:$rn, X:$rm, AArch64Base_ANDSXLSL_imm6AsInt32:$imm6),
        (ANDSXLSL X:$rn, X:$rm, AArch64Base_ANDSXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDSXLSR X:$rn, X:$rm, AArch64Base_ANDSXLSR_imm6AsInt32:$imm6),
        (ANDSXLSR X:$rn, X:$rm, AArch64Base_ANDSXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDSXROR X:$rn, X:$rm, AArch64Base_ANDSXROR_imm6AsInt32:$imm6),
        (ANDSXROR X:$rn, X:$rm, AArch64Base_ANDSXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDW W:$rn, W:$rm),
        (ANDW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ANDWASR W:$rn, W:$rm, AArch64Base_ANDWASR_imm6AsInt32:$imm6),
        (ANDWASR W:$rn, W:$rm, AArch64Base_ANDWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDWLSL W:$rn, W:$rm, AArch64Base_ANDWLSL_imm6AsInt32:$imm6),
        (ANDWLSL W:$rn, W:$rm, AArch64Base_ANDWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDWLSR W:$rn, W:$rm, AArch64Base_ANDWLSR_imm6AsInt32:$imm6),
        (ANDWLSR W:$rn, W:$rm, AArch64Base_ANDWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDWROR W:$rn, W:$rm, AArch64Base_ANDWROR_imm6AsInt32:$imm6),
        (ANDWROR W:$rn, W:$rm, AArch64Base_ANDWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDX X:$rn, X:$rm),
        (ANDX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ANDXASR X:$rn, X:$rm, AArch64Base_ANDXASR_imm6AsInt32:$imm6),
        (ANDXASR X:$rn, X:$rm, AArch64Base_ANDXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDXLSL X:$rn, X:$rm, AArch64Base_ANDXLSL_imm6AsInt32:$imm6),
        (ANDXLSL X:$rn, X:$rm, AArch64Base_ANDXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDXLSR X:$rn, X:$rm, AArch64Base_ANDXLSR_imm6AsInt32:$imm6),
        (ANDXLSR X:$rn, X:$rm, AArch64Base_ANDXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ANDXROR X:$rn, X:$rm, AArch64Base_ANDXROR_imm6AsInt32:$imm6),
        (ANDXROR X:$rn, X:$rm, AArch64Base_ANDXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ASRW W:$rn, W:$rm),
        (ASRW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ASRX X:$rn, X:$rm),
        (ASRX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_BFMW W:$rd_6, W:$rn, AArch64Base_BFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_BFMW_rightWSizeAsInt32:$rightWSize),
        (BFMW W:$rd_6, W:$rn, AArch64Base_BFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_BFMW_rightWSizeAsInt32:$rightWSize)>;

def : Pat<(int_processorNameValue_BFMX X:$rd_7, X:$rn, AArch64Base_BFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_BFMX_rightXSizeAsInt32:$rightXSize),
        (BFMX X:$rd_7, X:$rn, AArch64Base_BFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_BFMX_rightXSizeAsInt32:$rightXSize)>;

def : Pat<(int_processorNameValue_BICSW W:$rn, W:$rm),
        (BICSW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_BICSWASR W:$rn, W:$rm, AArch64Base_BICSWASR_imm6AsInt32:$imm6),
        (BICSWASR W:$rn, W:$rm, AArch64Base_BICSWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICSWLSL W:$rn, W:$rm, AArch64Base_BICSWLSL_imm6AsInt32:$imm6),
        (BICSWLSL W:$rn, W:$rm, AArch64Base_BICSWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICSWLSR W:$rn, W:$rm, AArch64Base_BICSWLSR_imm6AsInt32:$imm6),
        (BICSWLSR W:$rn, W:$rm, AArch64Base_BICSWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICSWROR W:$rn, W:$rm, AArch64Base_BICSWROR_imm6AsInt32:$imm6),
        (BICSWROR W:$rn, W:$rm, AArch64Base_BICSWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICSX X:$rn, X:$rm),
        (BICSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_BICSXASR X:$rn, X:$rm, AArch64Base_BICSXASR_imm6AsInt32:$imm6),
        (BICSXASR X:$rn, X:$rm, AArch64Base_BICSXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICSXLSL X:$rn, X:$rm, AArch64Base_BICSXLSL_imm6AsInt32:$imm6),
        (BICSXLSL X:$rn, X:$rm, AArch64Base_BICSXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICSXLSR X:$rn, X:$rm, AArch64Base_BICSXLSR_imm6AsInt32:$imm6),
        (BICSXLSR X:$rn, X:$rm, AArch64Base_BICSXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICSXROR X:$rn, X:$rm, AArch64Base_BICSXROR_imm6AsInt32:$imm6),
        (BICSXROR X:$rn, X:$rm, AArch64Base_BICSXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICW W:$rn, W:$rm),
        (BICW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_BICWASR W:$rn, W:$rm, AArch64Base_BICWASR_imm6AsInt32:$imm6),
        (BICWASR W:$rn, W:$rm, AArch64Base_BICWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICWLSL W:$rn, W:$rm, AArch64Base_BICWLSL_imm6AsInt32:$imm6),
        (BICWLSL W:$rn, W:$rm, AArch64Base_BICWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICWLSR W:$rn, W:$rm, AArch64Base_BICWLSR_imm6AsInt32:$imm6),
        (BICWLSR W:$rn, W:$rm, AArch64Base_BICWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICWROR W:$rn, W:$rm, AArch64Base_BICWROR_imm6AsInt32:$imm6),
        (BICWROR W:$rn, W:$rm, AArch64Base_BICWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICX X:$rn, X:$rm),
        (BICX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_BICXASR X:$rn, X:$rm, AArch64Base_BICXASR_imm6AsInt32:$imm6),
        (BICXASR X:$rn, X:$rm, AArch64Base_BICXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICXLSL X:$rn, X:$rm, AArch64Base_BICXLSL_imm6AsInt32:$imm6),
        (BICXLSL X:$rn, X:$rm, AArch64Base_BICXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICXLSR X:$rn, X:$rm, AArch64Base_BICXLSR_imm6AsInt32:$imm6),
        (BICXLSR X:$rn, X:$rm, AArch64Base_BICXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BICXROR X:$rn, X:$rm, AArch64Base_BICXROR_imm6AsInt32:$imm6),
        (BICXROR X:$rn, X:$rm, AArch64Base_BICXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_BR X:$rn),
        (BR X:$rn)>;

def : Pat<(int_processorNameValue_BTI AArch64Base_BTI_btiAsInt32:$bti),
        (BTI AArch64Base_BTI_btiAsInt32:$bti)>;

def : Pat<(int_processorNameValue_CCMNALW W:$rn, W:$rm, AArch64Base_CCMNALW_nzcvAsInt32:$nzcv),
        (CCMNALW W:$rn, W:$rm, AArch64Base_CCMNALW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNALX X:$rn, X:$rm, AArch64Base_CCMNALX_nzcvAsInt32:$nzcv),
        (CCMNALX X:$rn, X:$rm, AArch64Base_CCMNALX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNCCW W:$rn, W:$rm, AArch64Base_CCMNCCW_nzcvAsInt32:$nzcv),
        (CCMNCCW W:$rn, W:$rm, AArch64Base_CCMNCCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNCCX X:$rn, X:$rm, AArch64Base_CCMNCCX_nzcvAsInt32:$nzcv),
        (CCMNCCX X:$rn, X:$rm, AArch64Base_CCMNCCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNCSW W:$rn, W:$rm, AArch64Base_CCMNCSW_nzcvAsInt32:$nzcv),
        (CCMNCSW W:$rn, W:$rm, AArch64Base_CCMNCSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNCSX X:$rn, X:$rm, AArch64Base_CCMNCSX_nzcvAsInt32:$nzcv),
        (CCMNCSX X:$rn, X:$rm, AArch64Base_CCMNCSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNEQW W:$rn, W:$rm, AArch64Base_CCMNEQW_nzcvAsInt32:$nzcv),
        (CCMNEQW W:$rn, W:$rm, AArch64Base_CCMNEQW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNEQX X:$rn, X:$rm, AArch64Base_CCMNEQX_nzcvAsInt32:$nzcv),
        (CCMNEQX X:$rn, X:$rm, AArch64Base_CCMNEQX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNGEW W:$rn, W:$rm, AArch64Base_CCMNGEW_nzcvAsInt32:$nzcv),
        (CCMNGEW W:$rn, W:$rm, AArch64Base_CCMNGEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNGEX X:$rn, X:$rm, AArch64Base_CCMNGEX_nzcvAsInt32:$nzcv),
        (CCMNGEX X:$rn, X:$rm, AArch64Base_CCMNGEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNGTW W:$rn, W:$rm, AArch64Base_CCMNGTW_nzcvAsInt32:$nzcv),
        (CCMNGTW W:$rn, W:$rm, AArch64Base_CCMNGTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNGTX X:$rn, X:$rm, AArch64Base_CCMNGTX_nzcvAsInt32:$nzcv),
        (CCMNGTX X:$rn, X:$rm, AArch64Base_CCMNGTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNHIW W:$rn, W:$rm, AArch64Base_CCMNHIW_nzcvAsInt32:$nzcv),
        (CCMNHIW W:$rn, W:$rm, AArch64Base_CCMNHIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNHIX X:$rn, X:$rm, AArch64Base_CCMNHIX_nzcvAsInt32:$nzcv),
        (CCMNHIX X:$rn, X:$rm, AArch64Base_CCMNHIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIALW W:$rn, AArch64Base_CCMNIALW_immXAsInt64:$immX, AArch64Base_CCMNIALW_nzcvAsInt32:$nzcv),
        (CCMNIALW W:$rn, AArch64Base_CCMNIALW_immXAsInt64:$immX, AArch64Base_CCMNIALW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIALX X:$rn, AArch64Base_CCMNIALX_immXAsInt64:$immX, AArch64Base_CCMNIALX_nzcvAsInt32:$nzcv),
        (CCMNIALX X:$rn, AArch64Base_CCMNIALX_immXAsInt64:$immX, AArch64Base_CCMNIALX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNICCW W:$rn, AArch64Base_CCMNICCW_immXAsInt64:$immX, AArch64Base_CCMNICCW_nzcvAsInt32:$nzcv),
        (CCMNICCW W:$rn, AArch64Base_CCMNICCW_immXAsInt64:$immX, AArch64Base_CCMNICCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNICCX X:$rn, AArch64Base_CCMNICCX_immXAsInt64:$immX, AArch64Base_CCMNICCX_nzcvAsInt32:$nzcv),
        (CCMNICCX X:$rn, AArch64Base_CCMNICCX_immXAsInt64:$immX, AArch64Base_CCMNICCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNICSW W:$rn, AArch64Base_CCMNICSW_immXAsInt64:$immX, AArch64Base_CCMNICSW_nzcvAsInt32:$nzcv),
        (CCMNICSW W:$rn, AArch64Base_CCMNICSW_immXAsInt64:$immX, AArch64Base_CCMNICSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNICSX X:$rn, AArch64Base_CCMNICSX_immXAsInt64:$immX, AArch64Base_CCMNICSX_nzcvAsInt32:$nzcv),
        (CCMNICSX X:$rn, AArch64Base_CCMNICSX_immXAsInt64:$immX, AArch64Base_CCMNICSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIEQW W:$rn, AArch64Base_CCMNIEQW_immXAsInt64:$immX, AArch64Base_CCMNIEQW_nzcvAsInt32:$nzcv),
        (CCMNIEQW W:$rn, AArch64Base_CCMNIEQW_immXAsInt64:$immX, AArch64Base_CCMNIEQW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIEQX X:$rn, AArch64Base_CCMNIEQX_immXAsInt64:$immX, AArch64Base_CCMNIEQX_nzcvAsInt32:$nzcv),
        (CCMNIEQX X:$rn, AArch64Base_CCMNIEQX_immXAsInt64:$immX, AArch64Base_CCMNIEQX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIGEW W:$rn, AArch64Base_CCMNIGEW_immXAsInt64:$immX, AArch64Base_CCMNIGEW_nzcvAsInt32:$nzcv),
        (CCMNIGEW W:$rn, AArch64Base_CCMNIGEW_immXAsInt64:$immX, AArch64Base_CCMNIGEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIGEX X:$rn, AArch64Base_CCMNIGEX_immXAsInt64:$immX, AArch64Base_CCMNIGEX_nzcvAsInt32:$nzcv),
        (CCMNIGEX X:$rn, AArch64Base_CCMNIGEX_immXAsInt64:$immX, AArch64Base_CCMNIGEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIGTW W:$rn, AArch64Base_CCMNIGTW_immXAsInt64:$immX, AArch64Base_CCMNIGTW_nzcvAsInt32:$nzcv),
        (CCMNIGTW W:$rn, AArch64Base_CCMNIGTW_immXAsInt64:$immX, AArch64Base_CCMNIGTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIGTX X:$rn, AArch64Base_CCMNIGTX_immXAsInt64:$immX, AArch64Base_CCMNIGTX_nzcvAsInt32:$nzcv),
        (CCMNIGTX X:$rn, AArch64Base_CCMNIGTX_immXAsInt64:$immX, AArch64Base_CCMNIGTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIHIW W:$rn, AArch64Base_CCMNIHIW_immXAsInt64:$immX, AArch64Base_CCMNIHIW_nzcvAsInt32:$nzcv),
        (CCMNIHIW W:$rn, AArch64Base_CCMNIHIW_immXAsInt64:$immX, AArch64Base_CCMNIHIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIHIX X:$rn, AArch64Base_CCMNIHIX_immXAsInt64:$immX, AArch64Base_CCMNIHIX_nzcvAsInt32:$nzcv),
        (CCMNIHIX X:$rn, AArch64Base_CCMNIHIX_immXAsInt64:$immX, AArch64Base_CCMNIHIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNILEW W:$rn, AArch64Base_CCMNILEW_immXAsInt64:$immX, AArch64Base_CCMNILEW_nzcvAsInt32:$nzcv),
        (CCMNILEW W:$rn, AArch64Base_CCMNILEW_immXAsInt64:$immX, AArch64Base_CCMNILEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNILEX X:$rn, AArch64Base_CCMNILEX_immXAsInt64:$immX, AArch64Base_CCMNILEX_nzcvAsInt32:$nzcv),
        (CCMNILEX X:$rn, AArch64Base_CCMNILEX_immXAsInt64:$immX, AArch64Base_CCMNILEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNILSW W:$rn, AArch64Base_CCMNILSW_immXAsInt64:$immX, AArch64Base_CCMNILSW_nzcvAsInt32:$nzcv),
        (CCMNILSW W:$rn, AArch64Base_CCMNILSW_immXAsInt64:$immX, AArch64Base_CCMNILSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNILSX X:$rn, AArch64Base_CCMNILSX_immXAsInt64:$immX, AArch64Base_CCMNILSX_nzcvAsInt32:$nzcv),
        (CCMNILSX X:$rn, AArch64Base_CCMNILSX_immXAsInt64:$immX, AArch64Base_CCMNILSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNILTW W:$rn, AArch64Base_CCMNILTW_immXAsInt64:$immX, AArch64Base_CCMNILTW_nzcvAsInt32:$nzcv),
        (CCMNILTW W:$rn, AArch64Base_CCMNILTW_immXAsInt64:$immX, AArch64Base_CCMNILTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNILTX X:$rn, AArch64Base_CCMNILTX_immXAsInt64:$immX, AArch64Base_CCMNILTX_nzcvAsInt32:$nzcv),
        (CCMNILTX X:$rn, AArch64Base_CCMNILTX_immXAsInt64:$immX, AArch64Base_CCMNILTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIMIW W:$rn, AArch64Base_CCMNIMIW_immXAsInt64:$immX, AArch64Base_CCMNIMIW_nzcvAsInt32:$nzcv),
        (CCMNIMIW W:$rn, AArch64Base_CCMNIMIW_immXAsInt64:$immX, AArch64Base_CCMNIMIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIMIX X:$rn, AArch64Base_CCMNIMIX_immXAsInt64:$immX, AArch64Base_CCMNIMIX_nzcvAsInt32:$nzcv),
        (CCMNIMIX X:$rn, AArch64Base_CCMNIMIX_immXAsInt64:$immX, AArch64Base_CCMNIMIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNINEW W:$rn, AArch64Base_CCMNINEW_immXAsInt64:$immX, AArch64Base_CCMNINEW_nzcvAsInt32:$nzcv),
        (CCMNINEW W:$rn, AArch64Base_CCMNINEW_immXAsInt64:$immX, AArch64Base_CCMNINEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNINEX X:$rn, AArch64Base_CCMNINEX_immXAsInt64:$immX, AArch64Base_CCMNINEX_nzcvAsInt32:$nzcv),
        (CCMNINEX X:$rn, AArch64Base_CCMNINEX_immXAsInt64:$immX, AArch64Base_CCMNINEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNINVW W:$rn, AArch64Base_CCMNINVW_immXAsInt64:$immX, AArch64Base_CCMNINVW_nzcvAsInt32:$nzcv),
        (CCMNINVW W:$rn, AArch64Base_CCMNINVW_immXAsInt64:$immX, AArch64Base_CCMNINVW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNINVX X:$rn, AArch64Base_CCMNINVX_immXAsInt64:$immX, AArch64Base_CCMNINVX_nzcvAsInt32:$nzcv),
        (CCMNINVX X:$rn, AArch64Base_CCMNINVX_immXAsInt64:$immX, AArch64Base_CCMNINVX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIPLW W:$rn, AArch64Base_CCMNIPLW_immXAsInt64:$immX, AArch64Base_CCMNIPLW_nzcvAsInt32:$nzcv),
        (CCMNIPLW W:$rn, AArch64Base_CCMNIPLW_immXAsInt64:$immX, AArch64Base_CCMNIPLW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIPLX X:$rn, AArch64Base_CCMNIPLX_immXAsInt64:$immX, AArch64Base_CCMNIPLX_nzcvAsInt32:$nzcv),
        (CCMNIPLX X:$rn, AArch64Base_CCMNIPLX_immXAsInt64:$immX, AArch64Base_CCMNIPLX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIVCW W:$rn, AArch64Base_CCMNIVCW_immXAsInt64:$immX, AArch64Base_CCMNIVCW_nzcvAsInt32:$nzcv),
        (CCMNIVCW W:$rn, AArch64Base_CCMNIVCW_immXAsInt64:$immX, AArch64Base_CCMNIVCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIVCX X:$rn, AArch64Base_CCMNIVCX_immXAsInt64:$immX, AArch64Base_CCMNIVCX_nzcvAsInt32:$nzcv),
        (CCMNIVCX X:$rn, AArch64Base_CCMNIVCX_immXAsInt64:$immX, AArch64Base_CCMNIVCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIVSW W:$rn, AArch64Base_CCMNIVSW_immXAsInt64:$immX, AArch64Base_CCMNIVSW_nzcvAsInt32:$nzcv),
        (CCMNIVSW W:$rn, AArch64Base_CCMNIVSW_immXAsInt64:$immX, AArch64Base_CCMNIVSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNIVSX X:$rn, AArch64Base_CCMNIVSX_immXAsInt64:$immX, AArch64Base_CCMNIVSX_nzcvAsInt32:$nzcv),
        (CCMNIVSX X:$rn, AArch64Base_CCMNIVSX_immXAsInt64:$immX, AArch64Base_CCMNIVSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNLEW W:$rn, W:$rm, AArch64Base_CCMNLEW_nzcvAsInt32:$nzcv),
        (CCMNLEW W:$rn, W:$rm, AArch64Base_CCMNLEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNLEX X:$rn, X:$rm, AArch64Base_CCMNLEX_nzcvAsInt32:$nzcv),
        (CCMNLEX X:$rn, X:$rm, AArch64Base_CCMNLEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNLSW W:$rn, W:$rm, AArch64Base_CCMNLSW_nzcvAsInt32:$nzcv),
        (CCMNLSW W:$rn, W:$rm, AArch64Base_CCMNLSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNLSX X:$rn, X:$rm, AArch64Base_CCMNLSX_nzcvAsInt32:$nzcv),
        (CCMNLSX X:$rn, X:$rm, AArch64Base_CCMNLSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNLTW W:$rn, W:$rm, AArch64Base_CCMNLTW_nzcvAsInt32:$nzcv),
        (CCMNLTW W:$rn, W:$rm, AArch64Base_CCMNLTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNLTX X:$rn, X:$rm, AArch64Base_CCMNLTX_nzcvAsInt32:$nzcv),
        (CCMNLTX X:$rn, X:$rm, AArch64Base_CCMNLTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNMIW W:$rn, W:$rm, AArch64Base_CCMNMIW_nzcvAsInt32:$nzcv),
        (CCMNMIW W:$rn, W:$rm, AArch64Base_CCMNMIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNMIX X:$rn, X:$rm, AArch64Base_CCMNMIX_nzcvAsInt32:$nzcv),
        (CCMNMIX X:$rn, X:$rm, AArch64Base_CCMNMIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNNEW W:$rn, W:$rm, AArch64Base_CCMNNEW_nzcvAsInt32:$nzcv),
        (CCMNNEW W:$rn, W:$rm, AArch64Base_CCMNNEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNNEX X:$rn, X:$rm, AArch64Base_CCMNNEX_nzcvAsInt32:$nzcv),
        (CCMNNEX X:$rn, X:$rm, AArch64Base_CCMNNEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNNVW W:$rn, W:$rm, AArch64Base_CCMNNVW_nzcvAsInt32:$nzcv),
        (CCMNNVW W:$rn, W:$rm, AArch64Base_CCMNNVW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNNVX X:$rn, X:$rm, AArch64Base_CCMNNVX_nzcvAsInt32:$nzcv),
        (CCMNNVX X:$rn, X:$rm, AArch64Base_CCMNNVX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNPLW W:$rn, W:$rm, AArch64Base_CCMNPLW_nzcvAsInt32:$nzcv),
        (CCMNPLW W:$rn, W:$rm, AArch64Base_CCMNPLW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNPLX X:$rn, X:$rm, AArch64Base_CCMNPLX_nzcvAsInt32:$nzcv),
        (CCMNPLX X:$rn, X:$rm, AArch64Base_CCMNPLX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNVCW W:$rn, W:$rm, AArch64Base_CCMNVCW_nzcvAsInt32:$nzcv),
        (CCMNVCW W:$rn, W:$rm, AArch64Base_CCMNVCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNVCX X:$rn, X:$rm, AArch64Base_CCMNVCX_nzcvAsInt32:$nzcv),
        (CCMNVCX X:$rn, X:$rm, AArch64Base_CCMNVCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNVSW W:$rn, W:$rm, AArch64Base_CCMNVSW_nzcvAsInt32:$nzcv),
        (CCMNVSW W:$rn, W:$rm, AArch64Base_CCMNVSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMNVSX X:$rn, X:$rm, AArch64Base_CCMNVSX_nzcvAsInt32:$nzcv),
        (CCMNVSX X:$rn, X:$rm, AArch64Base_CCMNVSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPALW W:$rn, W:$rm, AArch64Base_CCMPALW_nzcvAsInt32:$nzcv),
        (CCMPALW W:$rn, W:$rm, AArch64Base_CCMPALW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPALX X:$rn, X:$rm, AArch64Base_CCMPALX_nzcvAsInt32:$nzcv),
        (CCMPALX X:$rn, X:$rm, AArch64Base_CCMPALX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPCCW W:$rn, W:$rm, AArch64Base_CCMPCCW_nzcvAsInt32:$nzcv),
        (CCMPCCW W:$rn, W:$rm, AArch64Base_CCMPCCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPCCX X:$rn, X:$rm, AArch64Base_CCMPCCX_nzcvAsInt32:$nzcv),
        (CCMPCCX X:$rn, X:$rm, AArch64Base_CCMPCCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPCSW W:$rn, W:$rm, AArch64Base_CCMPCSW_nzcvAsInt32:$nzcv),
        (CCMPCSW W:$rn, W:$rm, AArch64Base_CCMPCSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPCSX X:$rn, X:$rm, AArch64Base_CCMPCSX_nzcvAsInt32:$nzcv),
        (CCMPCSX X:$rn, X:$rm, AArch64Base_CCMPCSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPEQW W:$rn, W:$rm, AArch64Base_CCMPEQW_nzcvAsInt32:$nzcv),
        (CCMPEQW W:$rn, W:$rm, AArch64Base_CCMPEQW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPEQX X:$rn, X:$rm, AArch64Base_CCMPEQX_nzcvAsInt32:$nzcv),
        (CCMPEQX X:$rn, X:$rm, AArch64Base_CCMPEQX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPGEW W:$rn, W:$rm, AArch64Base_CCMPGEW_nzcvAsInt32:$nzcv),
        (CCMPGEW W:$rn, W:$rm, AArch64Base_CCMPGEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPGEX X:$rn, X:$rm, AArch64Base_CCMPGEX_nzcvAsInt32:$nzcv),
        (CCMPGEX X:$rn, X:$rm, AArch64Base_CCMPGEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPGTW W:$rn, W:$rm, AArch64Base_CCMPGTW_nzcvAsInt32:$nzcv),
        (CCMPGTW W:$rn, W:$rm, AArch64Base_CCMPGTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPGTX X:$rn, X:$rm, AArch64Base_CCMPGTX_nzcvAsInt32:$nzcv),
        (CCMPGTX X:$rn, X:$rm, AArch64Base_CCMPGTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPHIW W:$rn, W:$rm, AArch64Base_CCMPHIW_nzcvAsInt32:$nzcv),
        (CCMPHIW W:$rn, W:$rm, AArch64Base_CCMPHIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPHIX X:$rn, X:$rm, AArch64Base_CCMPHIX_nzcvAsInt32:$nzcv),
        (CCMPHIX X:$rn, X:$rm, AArch64Base_CCMPHIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIALW W:$rn, AArch64Base_CCMPIALW_immXAsInt64:$immX, AArch64Base_CCMPIALW_nzcvAsInt32:$nzcv),
        (CCMPIALW W:$rn, AArch64Base_CCMPIALW_immXAsInt64:$immX, AArch64Base_CCMPIALW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIALX X:$rn, AArch64Base_CCMPIALX_immXAsInt64:$immX, AArch64Base_CCMPIALX_nzcvAsInt32:$nzcv),
        (CCMPIALX X:$rn, AArch64Base_CCMPIALX_immXAsInt64:$immX, AArch64Base_CCMPIALX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPICCW W:$rn, AArch64Base_CCMPICCW_immXAsInt64:$immX, AArch64Base_CCMPICCW_nzcvAsInt32:$nzcv),
        (CCMPICCW W:$rn, AArch64Base_CCMPICCW_immXAsInt64:$immX, AArch64Base_CCMPICCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPICCX X:$rn, AArch64Base_CCMPICCX_immXAsInt64:$immX, AArch64Base_CCMPICCX_nzcvAsInt32:$nzcv),
        (CCMPICCX X:$rn, AArch64Base_CCMPICCX_immXAsInt64:$immX, AArch64Base_CCMPICCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPICSW W:$rn, AArch64Base_CCMPICSW_immXAsInt64:$immX, AArch64Base_CCMPICSW_nzcvAsInt32:$nzcv),
        (CCMPICSW W:$rn, AArch64Base_CCMPICSW_immXAsInt64:$immX, AArch64Base_CCMPICSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPICSX X:$rn, AArch64Base_CCMPICSX_immXAsInt64:$immX, AArch64Base_CCMPICSX_nzcvAsInt32:$nzcv),
        (CCMPICSX X:$rn, AArch64Base_CCMPICSX_immXAsInt64:$immX, AArch64Base_CCMPICSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIEQW W:$rn, AArch64Base_CCMPIEQW_immXAsInt64:$immX, AArch64Base_CCMPIEQW_nzcvAsInt32:$nzcv),
        (CCMPIEQW W:$rn, AArch64Base_CCMPIEQW_immXAsInt64:$immX, AArch64Base_CCMPIEQW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIEQX X:$rn, AArch64Base_CCMPIEQX_immXAsInt64:$immX, AArch64Base_CCMPIEQX_nzcvAsInt32:$nzcv),
        (CCMPIEQX X:$rn, AArch64Base_CCMPIEQX_immXAsInt64:$immX, AArch64Base_CCMPIEQX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIGEW W:$rn, AArch64Base_CCMPIGEW_immXAsInt64:$immX, AArch64Base_CCMPIGEW_nzcvAsInt32:$nzcv),
        (CCMPIGEW W:$rn, AArch64Base_CCMPIGEW_immXAsInt64:$immX, AArch64Base_CCMPIGEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIGEX X:$rn, AArch64Base_CCMPIGEX_immXAsInt64:$immX, AArch64Base_CCMPIGEX_nzcvAsInt32:$nzcv),
        (CCMPIGEX X:$rn, AArch64Base_CCMPIGEX_immXAsInt64:$immX, AArch64Base_CCMPIGEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIGTW W:$rn, AArch64Base_CCMPIGTW_immXAsInt64:$immX, AArch64Base_CCMPIGTW_nzcvAsInt32:$nzcv),
        (CCMPIGTW W:$rn, AArch64Base_CCMPIGTW_immXAsInt64:$immX, AArch64Base_CCMPIGTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIGTX X:$rn, AArch64Base_CCMPIGTX_immXAsInt64:$immX, AArch64Base_CCMPIGTX_nzcvAsInt32:$nzcv),
        (CCMPIGTX X:$rn, AArch64Base_CCMPIGTX_immXAsInt64:$immX, AArch64Base_CCMPIGTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIHIW W:$rn, AArch64Base_CCMPIHIW_immXAsInt64:$immX, AArch64Base_CCMPIHIW_nzcvAsInt32:$nzcv),
        (CCMPIHIW W:$rn, AArch64Base_CCMPIHIW_immXAsInt64:$immX, AArch64Base_CCMPIHIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIHIX X:$rn, AArch64Base_CCMPIHIX_immXAsInt64:$immX, AArch64Base_CCMPIHIX_nzcvAsInt32:$nzcv),
        (CCMPIHIX X:$rn, AArch64Base_CCMPIHIX_immXAsInt64:$immX, AArch64Base_CCMPIHIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPILEW W:$rn, AArch64Base_CCMPILEW_immXAsInt64:$immX, AArch64Base_CCMPILEW_nzcvAsInt32:$nzcv),
        (CCMPILEW W:$rn, AArch64Base_CCMPILEW_immXAsInt64:$immX, AArch64Base_CCMPILEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPILEX X:$rn, AArch64Base_CCMPILEX_immXAsInt64:$immX, AArch64Base_CCMPILEX_nzcvAsInt32:$nzcv),
        (CCMPILEX X:$rn, AArch64Base_CCMPILEX_immXAsInt64:$immX, AArch64Base_CCMPILEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPILSW W:$rn, AArch64Base_CCMPILSW_immXAsInt64:$immX, AArch64Base_CCMPILSW_nzcvAsInt32:$nzcv),
        (CCMPILSW W:$rn, AArch64Base_CCMPILSW_immXAsInt64:$immX, AArch64Base_CCMPILSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPILSX X:$rn, AArch64Base_CCMPILSX_immXAsInt64:$immX, AArch64Base_CCMPILSX_nzcvAsInt32:$nzcv),
        (CCMPILSX X:$rn, AArch64Base_CCMPILSX_immXAsInt64:$immX, AArch64Base_CCMPILSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPILTW W:$rn, AArch64Base_CCMPILTW_immXAsInt64:$immX, AArch64Base_CCMPILTW_nzcvAsInt32:$nzcv),
        (CCMPILTW W:$rn, AArch64Base_CCMPILTW_immXAsInt64:$immX, AArch64Base_CCMPILTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPILTX X:$rn, AArch64Base_CCMPILTX_immXAsInt64:$immX, AArch64Base_CCMPILTX_nzcvAsInt32:$nzcv),
        (CCMPILTX X:$rn, AArch64Base_CCMPILTX_immXAsInt64:$immX, AArch64Base_CCMPILTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIMIW W:$rn, AArch64Base_CCMPIMIW_immXAsInt64:$immX, AArch64Base_CCMPIMIW_nzcvAsInt32:$nzcv),
        (CCMPIMIW W:$rn, AArch64Base_CCMPIMIW_immXAsInt64:$immX, AArch64Base_CCMPIMIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIMIX X:$rn, AArch64Base_CCMPIMIX_immXAsInt64:$immX, AArch64Base_CCMPIMIX_nzcvAsInt32:$nzcv),
        (CCMPIMIX X:$rn, AArch64Base_CCMPIMIX_immXAsInt64:$immX, AArch64Base_CCMPIMIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPINEW W:$rn, AArch64Base_CCMPINEW_immXAsInt64:$immX, AArch64Base_CCMPINEW_nzcvAsInt32:$nzcv),
        (CCMPINEW W:$rn, AArch64Base_CCMPINEW_immXAsInt64:$immX, AArch64Base_CCMPINEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPINEX X:$rn, AArch64Base_CCMPINEX_immXAsInt64:$immX, AArch64Base_CCMPINEX_nzcvAsInt32:$nzcv),
        (CCMPINEX X:$rn, AArch64Base_CCMPINEX_immXAsInt64:$immX, AArch64Base_CCMPINEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPINVW W:$rn, AArch64Base_CCMPINVW_immXAsInt64:$immX, AArch64Base_CCMPINVW_nzcvAsInt32:$nzcv),
        (CCMPINVW W:$rn, AArch64Base_CCMPINVW_immXAsInt64:$immX, AArch64Base_CCMPINVW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPINVX X:$rn, AArch64Base_CCMPINVX_immXAsInt64:$immX, AArch64Base_CCMPINVX_nzcvAsInt32:$nzcv),
        (CCMPINVX X:$rn, AArch64Base_CCMPINVX_immXAsInt64:$immX, AArch64Base_CCMPINVX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIPLW W:$rn, AArch64Base_CCMPIPLW_immXAsInt64:$immX, AArch64Base_CCMPIPLW_nzcvAsInt32:$nzcv),
        (CCMPIPLW W:$rn, AArch64Base_CCMPIPLW_immXAsInt64:$immX, AArch64Base_CCMPIPLW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIPLX X:$rn, AArch64Base_CCMPIPLX_immXAsInt64:$immX, AArch64Base_CCMPIPLX_nzcvAsInt32:$nzcv),
        (CCMPIPLX X:$rn, AArch64Base_CCMPIPLX_immXAsInt64:$immX, AArch64Base_CCMPIPLX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIVCW W:$rn, AArch64Base_CCMPIVCW_immXAsInt64:$immX, AArch64Base_CCMPIVCW_nzcvAsInt32:$nzcv),
        (CCMPIVCW W:$rn, AArch64Base_CCMPIVCW_immXAsInt64:$immX, AArch64Base_CCMPIVCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIVCX X:$rn, AArch64Base_CCMPIVCX_immXAsInt64:$immX, AArch64Base_CCMPIVCX_nzcvAsInt32:$nzcv),
        (CCMPIVCX X:$rn, AArch64Base_CCMPIVCX_immXAsInt64:$immX, AArch64Base_CCMPIVCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIVSW W:$rn, AArch64Base_CCMPIVSW_immXAsInt64:$immX, AArch64Base_CCMPIVSW_nzcvAsInt32:$nzcv),
        (CCMPIVSW W:$rn, AArch64Base_CCMPIVSW_immXAsInt64:$immX, AArch64Base_CCMPIVSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPIVSX X:$rn, AArch64Base_CCMPIVSX_immXAsInt64:$immX, AArch64Base_CCMPIVSX_nzcvAsInt32:$nzcv),
        (CCMPIVSX X:$rn, AArch64Base_CCMPIVSX_immXAsInt64:$immX, AArch64Base_CCMPIVSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPLEW W:$rn, W:$rm, AArch64Base_CCMPLEW_nzcvAsInt32:$nzcv),
        (CCMPLEW W:$rn, W:$rm, AArch64Base_CCMPLEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPLEX X:$rn, X:$rm, AArch64Base_CCMPLEX_nzcvAsInt32:$nzcv),
        (CCMPLEX X:$rn, X:$rm, AArch64Base_CCMPLEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPLSW W:$rn, W:$rm, AArch64Base_CCMPLSW_nzcvAsInt32:$nzcv),
        (CCMPLSW W:$rn, W:$rm, AArch64Base_CCMPLSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPLSX X:$rn, X:$rm, AArch64Base_CCMPLSX_nzcvAsInt32:$nzcv),
        (CCMPLSX X:$rn, X:$rm, AArch64Base_CCMPLSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPLTW W:$rn, W:$rm, AArch64Base_CCMPLTW_nzcvAsInt32:$nzcv),
        (CCMPLTW W:$rn, W:$rm, AArch64Base_CCMPLTW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPLTX X:$rn, X:$rm, AArch64Base_CCMPLTX_nzcvAsInt32:$nzcv),
        (CCMPLTX X:$rn, X:$rm, AArch64Base_CCMPLTX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPMIW W:$rn, W:$rm, AArch64Base_CCMPMIW_nzcvAsInt32:$nzcv),
        (CCMPMIW W:$rn, W:$rm, AArch64Base_CCMPMIW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPMIX X:$rn, X:$rm, AArch64Base_CCMPMIX_nzcvAsInt32:$nzcv),
        (CCMPMIX X:$rn, X:$rm, AArch64Base_CCMPMIX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPNEW W:$rn, W:$rm, AArch64Base_CCMPNEW_nzcvAsInt32:$nzcv),
        (CCMPNEW W:$rn, W:$rm, AArch64Base_CCMPNEW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPNEX X:$rn, X:$rm, AArch64Base_CCMPNEX_nzcvAsInt32:$nzcv),
        (CCMPNEX X:$rn, X:$rm, AArch64Base_CCMPNEX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPNVW W:$rn, W:$rm, AArch64Base_CCMPNVW_nzcvAsInt32:$nzcv),
        (CCMPNVW W:$rn, W:$rm, AArch64Base_CCMPNVW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPNVX X:$rn, X:$rm, AArch64Base_CCMPNVX_nzcvAsInt32:$nzcv),
        (CCMPNVX X:$rn, X:$rm, AArch64Base_CCMPNVX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPPLW W:$rn, W:$rm, AArch64Base_CCMPPLW_nzcvAsInt32:$nzcv),
        (CCMPPLW W:$rn, W:$rm, AArch64Base_CCMPPLW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPPLX X:$rn, X:$rm, AArch64Base_CCMPPLX_nzcvAsInt32:$nzcv),
        (CCMPPLX X:$rn, X:$rm, AArch64Base_CCMPPLX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPVCW W:$rn, W:$rm, AArch64Base_CCMPVCW_nzcvAsInt32:$nzcv),
        (CCMPVCW W:$rn, W:$rm, AArch64Base_CCMPVCW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPVCX X:$rn, X:$rm, AArch64Base_CCMPVCX_nzcvAsInt32:$nzcv),
        (CCMPVCX X:$rn, X:$rm, AArch64Base_CCMPVCX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPVSW W:$rn, W:$rm, AArch64Base_CCMPVSW_nzcvAsInt32:$nzcv),
        (CCMPVSW W:$rn, W:$rm, AArch64Base_CCMPVSW_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CCMPVSX X:$rn, X:$rm, AArch64Base_CCMPVSX_nzcvAsInt32:$nzcv),
        (CCMPVSX X:$rn, X:$rm, AArch64Base_CCMPVSX_nzcvAsInt32:$nzcv)>;

def : Pat<(int_processorNameValue_CLSW W:$rn),
        (CLSW W:$rn)>;

def : Pat<(int_processorNameValue_CLSX X:$rn),
        (CLSX X:$rn)>;

def : Pat<(int_processorNameValue_CLZW W:$rn),
        (CLZW W:$rn)>;

def : Pat<(int_processorNameValue_CLZX X:$rn),
        (CLZX X:$rn)>;

def : Pat<(int_processorNameValue_CSELALW X:$rn, X:$rm),
        (CSELALW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELALX X:$rn, X:$rm),
        (CSELALX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELCCW X:$rn, X:$rm),
        (CSELCCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELCCX X:$rn, X:$rm),
        (CSELCCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELCSW X:$rn, X:$rm),
        (CSELCSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELCSX X:$rn, X:$rm),
        (CSELCSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELEQW X:$rn, X:$rm),
        (CSELEQW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELEQX X:$rn, X:$rm),
        (CSELEQX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELGEW X:$rn, X:$rm),
        (CSELGEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELGEX X:$rn, X:$rm),
        (CSELGEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELGTW X:$rn, X:$rm),
        (CSELGTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELGTX X:$rn, X:$rm),
        (CSELGTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELHIW X:$rn, X:$rm),
        (CSELHIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELHIX X:$rn, X:$rm),
        (CSELHIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELLEW X:$rn, X:$rm),
        (CSELLEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELLEX X:$rn, X:$rm),
        (CSELLEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELLSW X:$rn, X:$rm),
        (CSELLSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELLSX X:$rn, X:$rm),
        (CSELLSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELLTW X:$rn, X:$rm),
        (CSELLTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELLTX X:$rn, X:$rm),
        (CSELLTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELMIW X:$rn, X:$rm),
        (CSELMIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELMIX X:$rn, X:$rm),
        (CSELMIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELNEW X:$rn, X:$rm),
        (CSELNEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELNEX X:$rn, X:$rm),
        (CSELNEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELNVW X:$rn, X:$rm),
        (CSELNVW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELNVX X:$rn, X:$rm),
        (CSELNVX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELPLW X:$rn, X:$rm),
        (CSELPLW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELPLX X:$rn, X:$rm),
        (CSELPLX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELVCW X:$rn, X:$rm),
        (CSELVCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELVCX X:$rn, X:$rm),
        (CSELVCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELVSW X:$rn, X:$rm),
        (CSELVSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSELVSX X:$rn, X:$rm),
        (CSELVSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCALW X:$rn, X:$rm),
        (CSINCALW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCALX X:$rn, X:$rm),
        (CSINCALX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCCCW X:$rn, X:$rm),
        (CSINCCCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCCCX X:$rn, X:$rm),
        (CSINCCCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCCSW X:$rn, X:$rm),
        (CSINCCSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCCSX X:$rn, X:$rm),
        (CSINCCSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCEQW X:$rn, X:$rm),
        (CSINCEQW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCEQX X:$rn, X:$rm),
        (CSINCEQX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCGEW X:$rn, X:$rm),
        (CSINCGEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCGEX X:$rn, X:$rm),
        (CSINCGEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCGTW X:$rn, X:$rm),
        (CSINCGTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCGTX X:$rn, X:$rm),
        (CSINCGTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCHIW X:$rn, X:$rm),
        (CSINCHIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCHIX X:$rn, X:$rm),
        (CSINCHIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCLEW X:$rn, X:$rm),
        (CSINCLEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCLEX X:$rn, X:$rm),
        (CSINCLEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCLSW X:$rn, X:$rm),
        (CSINCLSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCLSX X:$rn, X:$rm),
        (CSINCLSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCLTW X:$rn, X:$rm),
        (CSINCLTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCLTX X:$rn, X:$rm),
        (CSINCLTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCMIW X:$rn, X:$rm),
        (CSINCMIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCMIX X:$rn, X:$rm),
        (CSINCMIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCNEW X:$rn, X:$rm),
        (CSINCNEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCNEX X:$rn, X:$rm),
        (CSINCNEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCNVW X:$rn, X:$rm),
        (CSINCNVW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCNVX X:$rn, X:$rm),
        (CSINCNVX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCPLW X:$rn, X:$rm),
        (CSINCPLW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCPLX X:$rn, X:$rm),
        (CSINCPLX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCVCW X:$rn, X:$rm),
        (CSINCVCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCVCX X:$rn, X:$rm),
        (CSINCVCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCVSW X:$rn, X:$rm),
        (CSINCVSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINCVSX X:$rn, X:$rm),
        (CSINCVSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVALW X:$rn, X:$rm),
        (CSINVALW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVALX X:$rn, X:$rm),
        (CSINVALX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVCCW X:$rn, X:$rm),
        (CSINVCCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVCCX X:$rn, X:$rm),
        (CSINVCCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVCSW X:$rn, X:$rm),
        (CSINVCSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVCSX X:$rn, X:$rm),
        (CSINVCSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVEQW X:$rn, X:$rm),
        (CSINVEQW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVEQX X:$rn, X:$rm),
        (CSINVEQX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVGEW X:$rn, X:$rm),
        (CSINVGEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVGEX X:$rn, X:$rm),
        (CSINVGEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVGTW X:$rn, X:$rm),
        (CSINVGTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVGTX X:$rn, X:$rm),
        (CSINVGTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVHIW X:$rn, X:$rm),
        (CSINVHIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVHIX X:$rn, X:$rm),
        (CSINVHIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVLEW X:$rn, X:$rm),
        (CSINVLEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVLEX X:$rn, X:$rm),
        (CSINVLEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVLSW X:$rn, X:$rm),
        (CSINVLSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVLSX X:$rn, X:$rm),
        (CSINVLSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVLTW X:$rn, X:$rm),
        (CSINVLTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVLTX X:$rn, X:$rm),
        (CSINVLTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVMIW X:$rn, X:$rm),
        (CSINVMIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVMIX X:$rn, X:$rm),
        (CSINVMIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVNEW X:$rn, X:$rm),
        (CSINVNEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVNEX X:$rn, X:$rm),
        (CSINVNEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVNVW X:$rn, X:$rm),
        (CSINVNVW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVNVX X:$rn, X:$rm),
        (CSINVNVX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVPLW X:$rn, X:$rm),
        (CSINVPLW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVPLX X:$rn, X:$rm),
        (CSINVPLX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVVCW X:$rn, X:$rm),
        (CSINVVCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVVCX X:$rn, X:$rm),
        (CSINVVCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVVSW X:$rn, X:$rm),
        (CSINVVSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSINVVSX X:$rn, X:$rm),
        (CSINVVSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGALW X:$rn, X:$rm),
        (CSNEGALW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGALX X:$rn, X:$rm),
        (CSNEGALX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGCCW X:$rn, X:$rm),
        (CSNEGCCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGCCX X:$rn, X:$rm),
        (CSNEGCCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGCSW X:$rn, X:$rm),
        (CSNEGCSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGCSX X:$rn, X:$rm),
        (CSNEGCSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGEQW X:$rn, X:$rm),
        (CSNEGEQW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGEQX X:$rn, X:$rm),
        (CSNEGEQX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGGEW X:$rn, X:$rm),
        (CSNEGGEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGGEX X:$rn, X:$rm),
        (CSNEGGEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGGTW X:$rn, X:$rm),
        (CSNEGGTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGGTX X:$rn, X:$rm),
        (CSNEGGTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGHIW X:$rn, X:$rm),
        (CSNEGHIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGHIX X:$rn, X:$rm),
        (CSNEGHIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGLEW X:$rn, X:$rm),
        (CSNEGLEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGLEX X:$rn, X:$rm),
        (CSNEGLEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGLSW X:$rn, X:$rm),
        (CSNEGLSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGLSX X:$rn, X:$rm),
        (CSNEGLSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGLTW X:$rn, X:$rm),
        (CSNEGLTW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGLTX X:$rn, X:$rm),
        (CSNEGLTX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGMIW X:$rn, X:$rm),
        (CSNEGMIW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGMIX X:$rn, X:$rm),
        (CSNEGMIX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGNEW X:$rn, X:$rm),
        (CSNEGNEW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGNEX X:$rn, X:$rm),
        (CSNEGNEX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGNVW X:$rn, X:$rm),
        (CSNEGNVW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGNVX X:$rn, X:$rm),
        (CSNEGNVX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGPLW X:$rn, X:$rm),
        (CSNEGPLW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGPLX X:$rn, X:$rm),
        (CSNEGPLX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGVCW X:$rn, X:$rm),
        (CSNEGVCW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGVCX X:$rn, X:$rm),
        (CSNEGVCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGVSW X:$rn, X:$rm),
        (CSNEGVSW X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_CSNEGVSX X:$rn, X:$rm),
        (CSNEGVSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_EONW W:$rn, W:$rm),
        (EONW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_EONWASR W:$rn, W:$rm, AArch64Base_EONWASR_imm6AsInt32:$imm6),
        (EONWASR W:$rn, W:$rm, AArch64Base_EONWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EONWLSL W:$rn, W:$rm, AArch64Base_EONWLSL_imm6AsInt32:$imm6),
        (EONWLSL W:$rn, W:$rm, AArch64Base_EONWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EONWLSR W:$rn, W:$rm, AArch64Base_EONWLSR_imm6AsInt32:$imm6),
        (EONWLSR W:$rn, W:$rm, AArch64Base_EONWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EONWROR W:$rn, W:$rm, AArch64Base_EONWROR_imm6AsInt32:$imm6),
        (EONWROR W:$rn, W:$rm, AArch64Base_EONWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EONX X:$rn, X:$rm),
        (EONX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_EONXASR X:$rn, X:$rm, AArch64Base_EONXASR_imm6AsInt32:$imm6),
        (EONXASR X:$rn, X:$rm, AArch64Base_EONXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EONXLSL X:$rn, X:$rm, AArch64Base_EONXLSL_imm6AsInt32:$imm6),
        (EONXLSL X:$rn, X:$rm, AArch64Base_EONXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EONXLSR X:$rn, X:$rm, AArch64Base_EONXLSR_imm6AsInt32:$imm6),
        (EONXLSR X:$rn, X:$rm, AArch64Base_EONXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EONXROR X:$rn, X:$rm, AArch64Base_EONXROR_imm6AsInt32:$imm6),
        (EONXROR X:$rn, X:$rm, AArch64Base_EONXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORIW W:$rn, AArch64Base_EORIW_immWSizeAsInt32:$immWSize),
        (EORIW W:$rn, AArch64Base_EORIW_immWSizeAsInt32:$immWSize)>;

def : Pat<(int_processorNameValue_EORIX X:$rn, AArch64Base_EORIX_immXSizeAsInt64:$immXSize),
        (EORIX X:$rn, AArch64Base_EORIX_immXSizeAsInt64:$immXSize)>;

def : Pat<(int_processorNameValue_EORW W:$rn, W:$rm),
        (EORW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_EORWASR W:$rn, W:$rm, AArch64Base_EORWASR_imm6AsInt32:$imm6),
        (EORWASR W:$rn, W:$rm, AArch64Base_EORWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORWLSL W:$rn, W:$rm, AArch64Base_EORWLSL_imm6AsInt32:$imm6),
        (EORWLSL W:$rn, W:$rm, AArch64Base_EORWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORWLSR W:$rn, W:$rm, AArch64Base_EORWLSR_imm6AsInt32:$imm6),
        (EORWLSR W:$rn, W:$rm, AArch64Base_EORWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORWROR W:$rn, W:$rm, AArch64Base_EORWROR_imm6AsInt32:$imm6),
        (EORWROR W:$rn, W:$rm, AArch64Base_EORWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORX X:$rn, X:$rm),
        (EORX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_EORXASR X:$rn, X:$rm, AArch64Base_EORXASR_imm6AsInt32:$imm6),
        (EORXASR X:$rn, X:$rm, AArch64Base_EORXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORXLSL X:$rn, X:$rm, AArch64Base_EORXLSL_imm6AsInt32:$imm6),
        (EORXLSL X:$rn, X:$rm, AArch64Base_EORXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORXLSR X:$rn, X:$rm, AArch64Base_EORXLSR_imm6AsInt32:$imm6),
        (EORXLSR X:$rn, X:$rm, AArch64Base_EORXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EORXROR X:$rn, X:$rm, AArch64Base_EORXROR_imm6AsInt32:$imm6),
        (EORXROR X:$rn, X:$rm, AArch64Base_EORXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_EXTRW W:$rn, W:$rm, AArch64Base_EXTRW_shiftWSizeAsInt32:$shiftWSize),
        (EXTRW W:$rn, W:$rm, AArch64Base_EXTRW_shiftWSizeAsInt32:$shiftWSize)>;

def : Pat<(int_processorNameValue_EXTRX X:$rn, X:$rm, AArch64Base_EXTRX_shiftXSizeAsInt32:$shiftXSize),
        (EXTRX X:$rn, X:$rm, AArch64Base_EXTRX_shiftXSizeAsInt32:$shiftXSize)>;

def : Pat<(int_processorNameValue_HLT AArch64Base_HLT_imm16AsInt32:$imm16),
        (HLT AArch64Base_HLT_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_LDRB S:$rn, AArch64Base_LDRB_offsetAsInt64:$offset),
        (LDRB S:$rn, AArch64Base_LDRB_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRH S:$rn, AArch64Base_LDRH_offsetAsInt64:$offset),
        (LDRH S:$rn, AArch64Base_LDRH_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRRegBSXTWIs S:$rn, X:$rm),
        (LDRRegBSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegBSXTWUn S:$rn, X:$rm),
        (LDRRegBSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegBSXTXIs S:$rn, X:$rm),
        (LDRRegBSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegBSXTXUn S:$rn, X:$rm),
        (LDRRegBSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegBUXTWIs S:$rn, X:$rm),
        (LDRRegBUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegBUXTWUn S:$rn, X:$rm),
        (LDRRegBUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegBUXTXIs S:$rn, X:$rm),
        (LDRRegBUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegBUXTXUn S:$rn, X:$rm),
        (LDRRegBUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHSXTWIs S:$rn, X:$rm),
        (LDRRegHSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHSXTWUn S:$rn, X:$rm),
        (LDRRegHSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHSXTXIs S:$rn, X:$rm),
        (LDRRegHSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHSXTXUn S:$rn, X:$rm),
        (LDRRegHSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHUXTWIs S:$rn, X:$rm),
        (LDRRegHUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHUXTWUn S:$rn, X:$rm),
        (LDRRegHUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHUXTXIs S:$rn, X:$rm),
        (LDRRegHUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegHUXTXUn S:$rn, X:$rm),
        (LDRRegHUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWSXTWIs S:$rn, X:$rm),
        (LDRRegSBWSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWSXTWUn S:$rn, X:$rm),
        (LDRRegSBWSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWSXTXIs S:$rn, X:$rm),
        (LDRRegSBWSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWSXTXUn S:$rn, X:$rm),
        (LDRRegSBWSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWUXTWIs S:$rn, X:$rm),
        (LDRRegSBWUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWUXTWUn S:$rn, X:$rm),
        (LDRRegSBWUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWUXTXIs S:$rn, X:$rm),
        (LDRRegSBWUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBWUXTXUn S:$rn, X:$rm),
        (LDRRegSBWUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXSXTWIs S:$rn, X:$rm),
        (LDRRegSBXSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXSXTWUn S:$rn, X:$rm),
        (LDRRegSBXSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXSXTXIs S:$rn, X:$rm),
        (LDRRegSBXSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXSXTXUn S:$rn, X:$rm),
        (LDRRegSBXSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXUXTWIs S:$rn, X:$rm),
        (LDRRegSBXUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXUXTWUn S:$rn, X:$rm),
        (LDRRegSBXUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXUXTXIs S:$rn, X:$rm),
        (LDRRegSBXUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSBXUXTXUn S:$rn, X:$rm),
        (LDRRegSBXUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWSXTWIs S:$rn, X:$rm),
        (LDRRegSHWSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWSXTWUn S:$rn, X:$rm),
        (LDRRegSHWSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWSXTXIs S:$rn, X:$rm),
        (LDRRegSHWSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWSXTXUn S:$rn, X:$rm),
        (LDRRegSHWSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWUXTWIs S:$rn, X:$rm),
        (LDRRegSHWUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWUXTWUn S:$rn, X:$rm),
        (LDRRegSHWUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWUXTXIs S:$rn, X:$rm),
        (LDRRegSHWUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHWUXTXUn S:$rn, X:$rm),
        (LDRRegSHWUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXSXTWIs S:$rn, X:$rm),
        (LDRRegSHXSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXSXTWUn S:$rn, X:$rm),
        (LDRRegSHXSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXSXTXIs S:$rn, X:$rm),
        (LDRRegSHXSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXSXTXUn S:$rn, X:$rm),
        (LDRRegSHXSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXUXTWIs S:$rn, X:$rm),
        (LDRRegSHXUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXUXTWUn S:$rn, X:$rm),
        (LDRRegSHXUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXUXTXIs S:$rn, X:$rm),
        (LDRRegSHXUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSHXUXTXUn S:$rn, X:$rm),
        (LDRRegSHXUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXSXTWIs S:$rn, X:$rm),
        (LDRRegSWXSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXSXTWUn S:$rn, X:$rm),
        (LDRRegSWXSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXSXTXIs S:$rn, X:$rm),
        (LDRRegSWXSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXSXTXUn S:$rn, X:$rm),
        (LDRRegSWXSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXUXTWIs S:$rn, X:$rm),
        (LDRRegSWXUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXUXTWUn S:$rn, X:$rm),
        (LDRRegSWXUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXUXTXIs S:$rn, X:$rm),
        (LDRRegSWXUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegSWXUXTXUn S:$rn, X:$rm),
        (LDRRegSWXUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWSXTWIs S:$rn, X:$rm),
        (LDRRegWSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWSXTWUn S:$rn, X:$rm),
        (LDRRegWSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWSXTXIs S:$rn, X:$rm),
        (LDRRegWSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWSXTXUn S:$rn, X:$rm),
        (LDRRegWSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWUXTWIs S:$rn, X:$rm),
        (LDRRegWUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWUXTWUn S:$rn, X:$rm),
        (LDRRegWUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWUXTXIs S:$rn, X:$rm),
        (LDRRegWUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegWUXTXUn S:$rn, X:$rm),
        (LDRRegWUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXSXTWIs S:$rn, X:$rm),
        (LDRRegXSXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXSXTWUn S:$rn, X:$rm),
        (LDRRegXSXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXSXTXIs S:$rn, X:$rm),
        (LDRRegXSXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXSXTXUn S:$rn, X:$rm),
        (LDRRegXSXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXUXTWIs S:$rn, X:$rm),
        (LDRRegXUXTWIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXUXTWUn S:$rn, X:$rm),
        (LDRRegXUXTWUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXUXTXIs S:$rn, X:$rm),
        (LDRRegXUXTXIs S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRRegXUXTXUn S:$rn, X:$rm),
        (LDRRegXUXTXUn S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LDRSBW S:$rn, AArch64Base_LDRSBW_offsetAsInt64:$offset),
        (LDRSBW S:$rn, AArch64Base_LDRSBW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRSBX S:$rn, AArch64Base_LDRSBX_offsetAsInt64:$offset),
        (LDRSBX S:$rn, AArch64Base_LDRSBX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRSHW S:$rn, AArch64Base_LDRSHW_offsetAsInt64:$offset),
        (LDRSHW S:$rn, AArch64Base_LDRSHW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRSHX S:$rn, AArch64Base_LDRSHX_offsetAsInt64:$offset),
        (LDRSHX S:$rn, AArch64Base_LDRSHX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRSWX S:$rn, AArch64Base_LDRSWX_offsetAsInt64:$offset),
        (LDRSWX S:$rn, AArch64Base_LDRSWX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRW S:$rn, AArch64Base_LDRW_offsetAsInt64:$offset),
        (LDRW S:$rn, AArch64Base_LDRW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDRX S:$rn, AArch64Base_LDRX_offsetAsInt64:$offset),
        (LDRX S:$rn, AArch64Base_LDRX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURB S:$rn, AArch64Base_LDURB_offsetAsInt64:$offset),
        (LDURB S:$rn, AArch64Base_LDURB_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURH S:$rn, AArch64Base_LDURH_offsetAsInt64:$offset),
        (LDURH S:$rn, AArch64Base_LDURH_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURSBW S:$rn, AArch64Base_LDURSBW_offsetAsInt64:$offset),
        (LDURSBW S:$rn, AArch64Base_LDURSBW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURSBX S:$rn, AArch64Base_LDURSBX_offsetAsInt64:$offset),
        (LDURSBX S:$rn, AArch64Base_LDURSBX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURSHW S:$rn, AArch64Base_LDURSHW_offsetAsInt64:$offset),
        (LDURSHW S:$rn, AArch64Base_LDURSHW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURSHX S:$rn, AArch64Base_LDURSHX_offsetAsInt64:$offset),
        (LDURSHX S:$rn, AArch64Base_LDURSHX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURSWX S:$rn, AArch64Base_LDURSWX_offsetAsInt64:$offset),
        (LDURSWX S:$rn, AArch64Base_LDURSWX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURW S:$rn, AArch64Base_LDURW_offsetAsInt64:$offset),
        (LDURW S:$rn, AArch64Base_LDURW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LDURX S:$rn, AArch64Base_LDURX_offsetAsInt64:$offset),
        (LDURX S:$rn, AArch64Base_LDURX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_LSLW W:$rn, W:$rm),
        (LSLW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_LSLX X:$rn, X:$rm),
        (LSLX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_LSRW W:$rn, W:$rm),
        (LSRW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_LSRX X:$rn, X:$rm),
        (LSRX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_MADDW W:$ra, W:$rn, W:$rm),
        (MADDW W:$ra, W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_MADDX X:$ra, X:$rn, X:$rm),
        (MADDX X:$ra, X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_MOVKWPos00 X:$rd_0, AArch64Base_MOVKWPos00_imm16AsInt32:$imm16),
        (MOVKWPos00 X:$rd_0, AArch64Base_MOVKWPos00_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVKWPos16 X:$rd_1, AArch64Base_MOVKWPos16_imm16AsInt32:$imm16),
        (MOVKWPos16 X:$rd_1, AArch64Base_MOVKWPos16_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVKXPos00 X:$rd_2, AArch64Base_MOVKXPos00_imm16AsInt32:$imm16),
        (MOVKXPos00 X:$rd_2, AArch64Base_MOVKXPos00_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVKXPos16 X:$rd_3, AArch64Base_MOVKXPos16_imm16AsInt32:$imm16),
        (MOVKXPos16 X:$rd_3, AArch64Base_MOVKXPos16_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVKXPos32 X:$rd_4, AArch64Base_MOVKXPos32_imm16AsInt32:$imm16),
        (MOVKXPos32 X:$rd_4, AArch64Base_MOVKXPos32_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVKXPos48 X:$rd_5, AArch64Base_MOVKXPos48_imm16AsInt32:$imm16),
        (MOVKXPos48 X:$rd_5, AArch64Base_MOVKXPos48_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVNWPos00 AArch64Base_MOVNWPos00_imm16AsInt32:$imm16),
        (MOVNWPos00 AArch64Base_MOVNWPos00_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVNWPos16 AArch64Base_MOVNWPos16_imm16AsInt32:$imm16),
        (MOVNWPos16 AArch64Base_MOVNWPos16_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVNXPos00 AArch64Base_MOVNXPos00_imm16AsInt32:$imm16),
        (MOVNXPos00 AArch64Base_MOVNXPos00_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVNXPos16 AArch64Base_MOVNXPos16_imm16AsInt32:$imm16),
        (MOVNXPos16 AArch64Base_MOVNXPos16_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVNXPos32 AArch64Base_MOVNXPos32_imm16AsInt32:$imm16),
        (MOVNXPos32 AArch64Base_MOVNXPos32_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVNXPos48 AArch64Base_MOVNXPos48_imm16AsInt32:$imm16),
        (MOVNXPos48 AArch64Base_MOVNXPos48_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVZWPos00 AArch64Base_MOVZWPos00_imm16AsInt32:$imm16),
        (MOVZWPos00 AArch64Base_MOVZWPos00_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVZWPos16 AArch64Base_MOVZWPos16_imm16AsInt32:$imm16),
        (MOVZWPos16 AArch64Base_MOVZWPos16_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVZXPos00 AArch64Base_MOVZXPos00_imm16AsInt32:$imm16),
        (MOVZXPos00 AArch64Base_MOVZXPos00_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVZXPos16 AArch64Base_MOVZXPos16_imm16AsInt32:$imm16),
        (MOVZXPos16 AArch64Base_MOVZXPos16_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVZXPos32 AArch64Base_MOVZXPos32_imm16AsInt32:$imm16),
        (MOVZXPos32 AArch64Base_MOVZXPos32_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MOVZXPos48 AArch64Base_MOVZXPos48_imm16AsInt32:$imm16),
        (MOVZXPos48 AArch64Base_MOVZXPos48_imm16AsInt32:$imm16)>;

def : Pat<(int_processorNameValue_MRSNZCV ),
        (MRSNZCV )>;

def : Pat<(int_processorNameValue_MSUBW W:$ra, W:$rn, W:$rm),
        (MSUBW W:$ra, W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_MSUBX X:$ra, X:$rn, X:$rm),
        (MSUBX X:$ra, X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ORNW W:$rn, W:$rm),
        (ORNW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ORNWASR W:$rn, W:$rm, AArch64Base_ORNWASR_imm6AsInt32:$imm6),
        (ORNWASR W:$rn, W:$rm, AArch64Base_ORNWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORNWLSL W:$rn, W:$rm, AArch64Base_ORNWLSL_imm6AsInt32:$imm6),
        (ORNWLSL W:$rn, W:$rm, AArch64Base_ORNWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORNWLSR W:$rn, W:$rm, AArch64Base_ORNWLSR_imm6AsInt32:$imm6),
        (ORNWLSR W:$rn, W:$rm, AArch64Base_ORNWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORNWROR W:$rn, W:$rm, AArch64Base_ORNWROR_imm6AsInt32:$imm6),
        (ORNWROR W:$rn, W:$rm, AArch64Base_ORNWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORNX X:$rn, X:$rm),
        (ORNX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ORNXASR X:$rn, X:$rm, AArch64Base_ORNXASR_imm6AsInt32:$imm6),
        (ORNXASR X:$rn, X:$rm, AArch64Base_ORNXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORNXLSL X:$rn, X:$rm, AArch64Base_ORNXLSL_imm6AsInt32:$imm6),
        (ORNXLSL X:$rn, X:$rm, AArch64Base_ORNXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORNXLSR X:$rn, X:$rm, AArch64Base_ORNXLSR_imm6AsInt32:$imm6),
        (ORNXLSR X:$rn, X:$rm, AArch64Base_ORNXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORNXROR X:$rn, X:$rm, AArch64Base_ORNXROR_imm6AsInt32:$imm6),
        (ORNXROR X:$rn, X:$rm, AArch64Base_ORNXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRIW W:$rn, AArch64Base_ORRIW_immWSizeAsInt32:$immWSize),
        (ORRIW W:$rn, AArch64Base_ORRIW_immWSizeAsInt32:$immWSize)>;

def : Pat<(int_processorNameValue_ORRIX X:$rn, AArch64Base_ORRIX_immXSizeAsInt64:$immXSize),
        (ORRIX X:$rn, AArch64Base_ORRIX_immXSizeAsInt64:$immXSize)>;

def : Pat<(int_processorNameValue_ORRW W:$rn, W:$rm),
        (ORRW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_ORRWASR W:$rn, W:$rm, AArch64Base_ORRWASR_imm6AsInt32:$imm6),
        (ORRWASR W:$rn, W:$rm, AArch64Base_ORRWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRWLSL W:$rn, W:$rm, AArch64Base_ORRWLSL_imm6AsInt32:$imm6),
        (ORRWLSL W:$rn, W:$rm, AArch64Base_ORRWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRWLSR W:$rn, W:$rm, AArch64Base_ORRWLSR_imm6AsInt32:$imm6),
        (ORRWLSR W:$rn, W:$rm, AArch64Base_ORRWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRWROR W:$rn, W:$rm, AArch64Base_ORRWROR_imm6AsInt32:$imm6),
        (ORRWROR W:$rn, W:$rm, AArch64Base_ORRWROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRX X:$rn, X:$rm),
        (ORRX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_ORRXASR X:$rn, X:$rm, AArch64Base_ORRXASR_imm6AsInt32:$imm6),
        (ORRXASR X:$rn, X:$rm, AArch64Base_ORRXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRXLSL X:$rn, X:$rm, AArch64Base_ORRXLSL_imm6AsInt32:$imm6),
        (ORRXLSL X:$rn, X:$rm, AArch64Base_ORRXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRXLSR X:$rn, X:$rm, AArch64Base_ORRXLSR_imm6AsInt32:$imm6),
        (ORRXLSR X:$rn, X:$rm, AArch64Base_ORRXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_ORRXROR X:$rn, X:$rm, AArch64Base_ORRXROR_imm6AsInt32:$imm6),
        (ORRXROR X:$rn, X:$rm, AArch64Base_ORRXROR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_RET X:$rn),
        (RET X:$rn)>;

def : Pat<(int_processorNameValue_REV16W X:$rn),
        (REV16W X:$rn)>;

def : Pat<(int_processorNameValue_REV16X X:$rn),
        (REV16X X:$rn)>;

def : Pat<(int_processorNameValue_REV32 X:$rn),
        (REV32 X:$rn)>;

def : Pat<(int_processorNameValue_REVW X:$rn),
        (REVW X:$rn)>;

def : Pat<(int_processorNameValue_REVX X:$rn),
        (REVX X:$rn)>;

def : Pat<(int_processorNameValue_RORW W:$rn, W:$rm),
        (RORW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_RORX X:$rn, X:$rm),
        (RORX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SBCSW W:$rn, W:$rm),
        (SBCSW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_SBCSX X:$rn, X:$rm),
        (SBCSX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SBCW W:$rn, W:$rm),
        (SBCW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_SBCX X:$rn, X:$rm),
        (SBCX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SBFMW W:$rn, AArch64Base_SBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_SBFMW_rightWSizeAsInt32:$rightWSize),
        (SBFMW W:$rn, AArch64Base_SBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_SBFMW_rightWSizeAsInt32:$rightWSize)>;

def : Pat<(int_processorNameValue_SBFMX X:$rn, AArch64Base_SBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_SBFMX_rightXSizeAsInt32:$rightXSize),
        (SBFMX X:$rn, AArch64Base_SBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_SBFMX_rightXSizeAsInt32:$rightXSize)>;

def : Pat<(int_processorNameValue_SDIVW W:$rm, W:$rn),
        (SDIVW W:$rm, W:$rn)>;

def : Pat<(int_processorNameValue_SDIVX X:$rm, X:$rn),
        (SDIVX X:$rm, X:$rn)>;

def : Pat<(int_processorNameValue_SMADDL X:$ra, W:$rn, W:$rm),
        (SMADDL X:$ra, W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_SMSUBL X:$ra, W:$rn, W:$rm),
        (SMSUBL X:$ra, W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_SMULH X:$rn, X:$rm),
        (SMULH X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_STPW S:$rn, X:$rt, X:$rt2, AArch64Base_STPW_offWSizeAsInt64:$offWSize),
        (STPW S:$rn, X:$rt, X:$rt2, AArch64Base_STPW_offWSizeAsInt64:$offWSize)>;

def : Pat<(int_processorNameValue_STPWPre S:$rn_40, X:$rt, X:$rt2, AArch64Base_STPWPre_offWSizeAsInt64:$offWSize),
        (STPWPre S:$rn_40, X:$rt, X:$rt2, AArch64Base_STPWPre_offWSizeAsInt64:$offWSize)>;

def : Pat<(int_processorNameValue_STPWPst S:$rn_41, X:$rt, X:$rt2, AArch64Base_STPWPst_offWSizeAsInt64:$offWSize),
        (STPWPst S:$rn_41, X:$rt, X:$rt2, AArch64Base_STPWPst_offWSizeAsInt64:$offWSize)>;

def : Pat<(int_processorNameValue_STPX S:$rn, X:$rt, X:$rt2, AArch64Base_STPX_offXSizeAsInt64:$offXSize),
        (STPX S:$rn, X:$rt, X:$rt2, AArch64Base_STPX_offXSizeAsInt64:$offXSize)>;

def : Pat<(int_processorNameValue_STPXPre S:$rn_42, X:$rt, X:$rt2, AArch64Base_STPXPre_offXSizeAsInt64:$offXSize),
        (STPXPre S:$rn_42, X:$rt, X:$rt2, AArch64Base_STPXPre_offXSizeAsInt64:$offXSize)>;

def : Pat<(int_processorNameValue_STPXPst S:$rn_43, X:$rt, X:$rt2, AArch64Base_STPXPst_offXSizeAsInt64:$offXSize),
        (STPXPst S:$rn_43, X:$rt, X:$rt2, AArch64Base_STPXPst_offXSizeAsInt64:$offXSize)>;

def : Pat<(int_processorNameValue_STRB S:$rn, W:$rt, AArch64Base_STRB_offsetAsInt64:$offset),
        (STRB S:$rn, W:$rt, AArch64Base_STRB_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRH S:$rn, W:$rt, AArch64Base_STRH_offsetAsInt64:$offset),
        (STRH S:$rn, W:$rt, AArch64Base_STRH_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPreB S:$rn_17, W:$rt, AArch64Base_STRPreB_offsetAsInt64:$offset),
        (STRPreB S:$rn_17, W:$rt, AArch64Base_STRPreB_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPreH S:$rn_18, W:$rt, AArch64Base_STRPreH_offsetAsInt64:$offset),
        (STRPreH S:$rn_18, W:$rt, AArch64Base_STRPreH_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPreW S:$rn_19, W:$rt, AArch64Base_STRPreW_offsetAsInt64:$offset),
        (STRPreW S:$rn_19, W:$rt, AArch64Base_STRPreW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPreX S:$rn_20, X:$rt, AArch64Base_STRPreX_offsetAsInt64:$offset),
        (STRPreX S:$rn_20, X:$rt, AArch64Base_STRPreX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPstB S:$rn_30, W:$rt, AArch64Base_STRPstB_offsetAsInt64:$offset),
        (STRPstB S:$rn_30, W:$rt, AArch64Base_STRPstB_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPstH S:$rn_31, W:$rt, AArch64Base_STRPstH_offsetAsInt64:$offset),
        (STRPstH S:$rn_31, W:$rt, AArch64Base_STRPstH_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPstW S:$rn_32, W:$rt, AArch64Base_STRPstW_offsetAsInt64:$offset),
        (STRPstW S:$rn_32, W:$rt, AArch64Base_STRPstW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRPstX S:$rn_33, X:$rt, AArch64Base_STRPstX_offsetAsInt64:$offset),
        (STRPstX S:$rn_33, X:$rt, AArch64Base_STRPstX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRRegBSXTWIs S:$rn, X:$rm, W:$rt),
        (STRRegBSXTWIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegBSXTWUn S:$rn, X:$rm, W:$rt),
        (STRRegBSXTWUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegBSXTXIs S:$rn, X:$rm, W:$rt),
        (STRRegBSXTXIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegBSXTXUn S:$rn, X:$rm, W:$rt),
        (STRRegBSXTXUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegBUXTWIs S:$rn, X:$rm, W:$rt),
        (STRRegBUXTWIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegBUXTWUn S:$rn, X:$rm, W:$rt),
        (STRRegBUXTWUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegBUXTXIs S:$rn, X:$rm, W:$rt),
        (STRRegBUXTXIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegBUXTXUn S:$rn, X:$rm, W:$rt),
        (STRRegBUXTXUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHSXTWIs S:$rn, X:$rm, W:$rt),
        (STRRegHSXTWIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHSXTWUn S:$rn, X:$rm, W:$rt),
        (STRRegHSXTWUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHSXTXIs S:$rn, X:$rm, W:$rt),
        (STRRegHSXTXIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHSXTXUn S:$rn, X:$rm, W:$rt),
        (STRRegHSXTXUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHUXTWIs S:$rn, X:$rm, W:$rt),
        (STRRegHUXTWIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHUXTWUn S:$rn, X:$rm, W:$rt),
        (STRRegHUXTWUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHUXTXIs S:$rn, X:$rm, W:$rt),
        (STRRegHUXTXIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegHUXTXUn S:$rn, X:$rm, W:$rt),
        (STRRegHUXTXUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWSXTWIs S:$rn, X:$rm, W:$rt),
        (STRRegWSXTWIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWSXTWUn S:$rn, X:$rm, W:$rt),
        (STRRegWSXTWUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWSXTXIs S:$rn, X:$rm, W:$rt),
        (STRRegWSXTXIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWSXTXUn S:$rn, X:$rm, W:$rt),
        (STRRegWSXTXUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWUXTWIs S:$rn, X:$rm, W:$rt),
        (STRRegWUXTWIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWUXTWUn S:$rn, X:$rm, W:$rt),
        (STRRegWUXTWUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWUXTXIs S:$rn, X:$rm, W:$rt),
        (STRRegWUXTXIs S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegWUXTXUn S:$rn, X:$rm, W:$rt),
        (STRRegWUXTXUn S:$rn, X:$rm, W:$rt)>;

def : Pat<(int_processorNameValue_STRRegXSXTWIs S:$rn, X:$rm, X:$rt),
        (STRRegXSXTWIs S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRRegXSXTWUn S:$rn, X:$rm, X:$rt),
        (STRRegXSXTWUn S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRRegXSXTXIs S:$rn, X:$rm, X:$rt),
        (STRRegXSXTXIs S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRRegXSXTXUn S:$rn, X:$rm, X:$rt),
        (STRRegXSXTXUn S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRRegXUXTWIs S:$rn, X:$rm, X:$rt),
        (STRRegXUXTWIs S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRRegXUXTWUn S:$rn, X:$rm, X:$rt),
        (STRRegXUXTWUn S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRRegXUXTXIs S:$rn, X:$rm, X:$rt),
        (STRRegXUXTXIs S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRRegXUXTXUn S:$rn, X:$rm, X:$rt),
        (STRRegXUXTXUn S:$rn, X:$rm, X:$rt)>;

def : Pat<(int_processorNameValue_STRW S:$rn, W:$rt, AArch64Base_STRW_offsetAsInt64:$offset),
        (STRW S:$rn, W:$rt, AArch64Base_STRW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STRX S:$rn, X:$rt, AArch64Base_STRX_offsetAsInt64:$offset),
        (STRX S:$rn, X:$rt, AArch64Base_STRX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STURB S:$rn, W:$rt, AArch64Base_STURB_offsetAsInt64:$offset),
        (STURB S:$rn, W:$rt, AArch64Base_STURB_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STURH S:$rn, W:$rt, AArch64Base_STURH_offsetAsInt64:$offset),
        (STURH S:$rn, W:$rt, AArch64Base_STURH_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STURW S:$rn, W:$rt, AArch64Base_STURW_offsetAsInt64:$offset),
        (STURW S:$rn, W:$rt, AArch64Base_STURW_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_STURX S:$rn, X:$rt, AArch64Base_STURX_offsetAsInt64:$offset),
        (STURX S:$rn, X:$rt, AArch64Base_STURX_offsetAsInt64:$offset)>;

def : Pat<(int_processorNameValue_SUBW W:$rn, W:$rm),
        (SUBW W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_SUBWASR W:$rn, W:$rm, AArch64Base_SUBWASR_imm6AsInt32:$imm6),
        (SUBWASR W:$rn, W:$rm, AArch64Base_SUBWASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBWI R:$rn, AArch64Base_SUBWI_imm12XAsInt64:$imm12X),
        (SUBWI R:$rn, AArch64Base_SUBWI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_SUBWI12 R:$rn, AArch64Base_SUBWI12_imm12SAsInt64:$imm12S),
        (SUBWI12 R:$rn, AArch64Base_SUBWI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_SUBWLSL W:$rn, W:$rm, AArch64Base_SUBWLSL_imm6AsInt32:$imm6),
        (SUBWLSL W:$rn, W:$rm, AArch64Base_SUBWLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBWLSR W:$rn, W:$rm, AArch64Base_SUBWLSR_imm6AsInt32:$imm6),
        (SUBWLSR W:$rn, W:$rm, AArch64Base_SUBWLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBWS W:$rn, W:$rm),
        (SUBWS W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_SUBWSASR W:$rn, W:$rm, AArch64Base_SUBWSASR_imm6AsInt32:$imm6),
        (SUBWSASR W:$rn, W:$rm, AArch64Base_SUBWSASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBWSI R:$rn, AArch64Base_SUBWSI_imm12XAsInt64:$imm12X),
        (SUBWSI R:$rn, AArch64Base_SUBWSI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_SUBWSI12 R:$rn, AArch64Base_SUBWSI12_imm12SAsInt64:$imm12S),
        (SUBWSI12 R:$rn, AArch64Base_SUBWSI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_SUBWSLSL W:$rn, W:$rm, AArch64Base_SUBWSLSL_imm6AsInt32:$imm6),
        (SUBWSLSL W:$rn, W:$rm, AArch64Base_SUBWSLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBWSLSR W:$rn, W:$rm, AArch64Base_SUBWSLSR_imm6AsInt32:$imm6),
        (SUBWSLSR W:$rn, W:$rm, AArch64Base_SUBWSLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBWSSXSB R:$rn, X:$rm, AArch64Base_SUBWSSXSB_imm3AsInt32:$imm3),
        (SUBWSSXSB R:$rn, X:$rm, AArch64Base_SUBWSSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSSXSH R:$rn, X:$rm, AArch64Base_SUBWSSXSH_imm3AsInt32:$imm3),
        (SUBWSSXSH R:$rn, X:$rm, AArch64Base_SUBWSSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSSXSW R:$rn, X:$rm, AArch64Base_SUBWSSXSW_imm3AsInt32:$imm3),
        (SUBWSSXSW R:$rn, X:$rm, AArch64Base_SUBWSSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSSXSX R:$rn, X:$rm, AArch64Base_SUBWSSXSX_imm3AsInt32:$imm3),
        (SUBWSSXSX R:$rn, X:$rm, AArch64Base_SUBWSSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSSXTB R:$rn, X:$rm),
        (SUBWSSXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSSXTH R:$rn, X:$rm),
        (SUBWSSXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSSXTW R:$rn, X:$rm),
        (SUBWSSXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSSXTX R:$rn, X:$rm),
        (SUBWSSXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSUXSB R:$rn, X:$rm, AArch64Base_SUBWSUXSB_imm3AsInt32:$imm3),
        (SUBWSUXSB R:$rn, X:$rm, AArch64Base_SUBWSUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSUXSH R:$rn, X:$rm, AArch64Base_SUBWSUXSH_imm3AsInt32:$imm3),
        (SUBWSUXSH R:$rn, X:$rm, AArch64Base_SUBWSUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSUXSW R:$rn, X:$rm, AArch64Base_SUBWSUXSW_imm3AsInt32:$imm3),
        (SUBWSUXSW R:$rn, X:$rm, AArch64Base_SUBWSUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSUXSX R:$rn, X:$rm, AArch64Base_SUBWSUXSX_imm3AsInt32:$imm3),
        (SUBWSUXSX R:$rn, X:$rm, AArch64Base_SUBWSUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSUXTB R:$rn, X:$rm),
        (SUBWSUXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSUXTH R:$rn, X:$rm),
        (SUBWSUXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSUXTW R:$rn, X:$rm),
        (SUBWSUXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSUXTX R:$rn, X:$rm),
        (SUBWSUXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSXSB R:$rn, X:$rm, AArch64Base_SUBWSXSB_imm3AsInt32:$imm3),
        (SUBWSXSB R:$rn, X:$rm, AArch64Base_SUBWSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSXSH R:$rn, X:$rm, AArch64Base_SUBWSXSH_imm3AsInt32:$imm3),
        (SUBWSXSH R:$rn, X:$rm, AArch64Base_SUBWSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSXSW R:$rn, X:$rm, AArch64Base_SUBWSXSW_imm3AsInt32:$imm3),
        (SUBWSXSW R:$rn, X:$rm, AArch64Base_SUBWSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSXSX R:$rn, X:$rm, AArch64Base_SUBWSXSX_imm3AsInt32:$imm3),
        (SUBWSXSX R:$rn, X:$rm, AArch64Base_SUBWSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWSXTB R:$rn, X:$rm),
        (SUBWSXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSXTH R:$rn, X:$rm),
        (SUBWSXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSXTW R:$rn, X:$rm),
        (SUBWSXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWSXTX R:$rn, X:$rm),
        (SUBWSXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWUXSB R:$rn, X:$rm, AArch64Base_SUBWUXSB_imm3AsInt32:$imm3),
        (SUBWUXSB R:$rn, X:$rm, AArch64Base_SUBWUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWUXSH R:$rn, X:$rm, AArch64Base_SUBWUXSH_imm3AsInt32:$imm3),
        (SUBWUXSH R:$rn, X:$rm, AArch64Base_SUBWUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWUXSW R:$rn, X:$rm, AArch64Base_SUBWUXSW_imm3AsInt32:$imm3),
        (SUBWUXSW R:$rn, X:$rm, AArch64Base_SUBWUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWUXSX R:$rn, X:$rm, AArch64Base_SUBWUXSX_imm3AsInt32:$imm3),
        (SUBWUXSX R:$rn, X:$rm, AArch64Base_SUBWUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBWUXTB R:$rn, X:$rm),
        (SUBWUXTB R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWUXTH R:$rn, X:$rm),
        (SUBWUXTH R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWUXTW R:$rn, X:$rm),
        (SUBWUXTW R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBWUXTX R:$rn, X:$rm),
        (SUBWUXTX R:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBX X:$rn, X:$rm),
        (SUBX X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXASR X:$rn, X:$rm, AArch64Base_SUBXASR_imm6AsInt32:$imm6),
        (SUBXASR X:$rn, X:$rm, AArch64Base_SUBXASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBXI S:$rn, AArch64Base_SUBXI_imm12XAsInt64:$imm12X),
        (SUBXI S:$rn, AArch64Base_SUBXI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_SUBXI12 S:$rn, AArch64Base_SUBXI12_imm12SAsInt64:$imm12S),
        (SUBXI12 S:$rn, AArch64Base_SUBXI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_SUBXLSL X:$rn, X:$rm, AArch64Base_SUBXLSL_imm6AsInt32:$imm6),
        (SUBXLSL X:$rn, X:$rm, AArch64Base_SUBXLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBXLSR X:$rn, X:$rm, AArch64Base_SUBXLSR_imm6AsInt32:$imm6),
        (SUBXLSR X:$rn, X:$rm, AArch64Base_SUBXLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBXS X:$rn, X:$rm),
        (SUBXS X:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSASR X:$rn, X:$rm, AArch64Base_SUBXSASR_imm6AsInt32:$imm6),
        (SUBXSASR X:$rn, X:$rm, AArch64Base_SUBXSASR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBXSI S:$rn, AArch64Base_SUBXSI_imm12XAsInt64:$imm12X),
        (SUBXSI S:$rn, AArch64Base_SUBXSI_imm12XAsInt64:$imm12X)>;

def : Pat<(int_processorNameValue_SUBXSI12 S:$rn, AArch64Base_SUBXSI12_imm12SAsInt64:$imm12S),
        (SUBXSI12 S:$rn, AArch64Base_SUBXSI12_imm12SAsInt64:$imm12S)>;

def : Pat<(int_processorNameValue_SUBXSLSL X:$rn, X:$rm, AArch64Base_SUBXSLSL_imm6AsInt32:$imm6),
        (SUBXSLSL X:$rn, X:$rm, AArch64Base_SUBXSLSL_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBXSLSR X:$rn, X:$rm, AArch64Base_SUBXSLSR_imm6AsInt32:$imm6),
        (SUBXSLSR X:$rn, X:$rm, AArch64Base_SUBXSLSR_imm6AsInt32:$imm6)>;

def : Pat<(int_processorNameValue_SUBXSSXSB S:$rn, X:$rm, AArch64Base_SUBXSSXSB_imm3AsInt32:$imm3),
        (SUBXSSXSB S:$rn, X:$rm, AArch64Base_SUBXSSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSSXSH S:$rn, X:$rm, AArch64Base_SUBXSSXSH_imm3AsInt32:$imm3),
        (SUBXSSXSH S:$rn, X:$rm, AArch64Base_SUBXSSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSSXSW S:$rn, X:$rm, AArch64Base_SUBXSSXSW_imm3AsInt32:$imm3),
        (SUBXSSXSW S:$rn, X:$rm, AArch64Base_SUBXSSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSSXSX S:$rn, X:$rm, AArch64Base_SUBXSSXSX_imm3AsInt32:$imm3),
        (SUBXSSXSX S:$rn, X:$rm, AArch64Base_SUBXSSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSSXTB S:$rn, X:$rm),
        (SUBXSSXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSSXTH S:$rn, X:$rm),
        (SUBXSSXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSSXTW S:$rn, X:$rm),
        (SUBXSSXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSSXTX S:$rn, X:$rm),
        (SUBXSSXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSUXSB S:$rn, X:$rm, AArch64Base_SUBXSUXSB_imm3AsInt32:$imm3),
        (SUBXSUXSB S:$rn, X:$rm, AArch64Base_SUBXSUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSUXSH S:$rn, X:$rm, AArch64Base_SUBXSUXSH_imm3AsInt32:$imm3),
        (SUBXSUXSH S:$rn, X:$rm, AArch64Base_SUBXSUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSUXSW S:$rn, X:$rm, AArch64Base_SUBXSUXSW_imm3AsInt32:$imm3),
        (SUBXSUXSW S:$rn, X:$rm, AArch64Base_SUBXSUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSUXSX S:$rn, X:$rm, AArch64Base_SUBXSUXSX_imm3AsInt32:$imm3),
        (SUBXSUXSX S:$rn, X:$rm, AArch64Base_SUBXSUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSUXTB S:$rn, X:$rm),
        (SUBXSUXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSUXTH S:$rn, X:$rm),
        (SUBXSUXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSUXTW S:$rn, X:$rm),
        (SUBXSUXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSUXTX S:$rn, X:$rm),
        (SUBXSUXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSXSB S:$rn, X:$rm, AArch64Base_SUBXSXSB_imm3AsInt32:$imm3),
        (SUBXSXSB S:$rn, X:$rm, AArch64Base_SUBXSXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSXSH S:$rn, X:$rm, AArch64Base_SUBXSXSH_imm3AsInt32:$imm3),
        (SUBXSXSH S:$rn, X:$rm, AArch64Base_SUBXSXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSXSW S:$rn, X:$rm, AArch64Base_SUBXSXSW_imm3AsInt32:$imm3),
        (SUBXSXSW S:$rn, X:$rm, AArch64Base_SUBXSXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSXSX S:$rn, X:$rm, AArch64Base_SUBXSXSX_imm3AsInt32:$imm3),
        (SUBXSXSX S:$rn, X:$rm, AArch64Base_SUBXSXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXSXTB S:$rn, X:$rm),
        (SUBXSXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSXTH S:$rn, X:$rm),
        (SUBXSXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSXTW S:$rn, X:$rm),
        (SUBXSXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXSXTX S:$rn, X:$rm),
        (SUBXSXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXUXSB S:$rn, X:$rm, AArch64Base_SUBXUXSB_imm3AsInt32:$imm3),
        (SUBXUXSB S:$rn, X:$rm, AArch64Base_SUBXUXSB_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXUXSH S:$rn, X:$rm, AArch64Base_SUBXUXSH_imm3AsInt32:$imm3),
        (SUBXUXSH S:$rn, X:$rm, AArch64Base_SUBXUXSH_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXUXSW S:$rn, X:$rm, AArch64Base_SUBXUXSW_imm3AsInt32:$imm3),
        (SUBXUXSW S:$rn, X:$rm, AArch64Base_SUBXUXSW_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXUXSX S:$rn, X:$rm, AArch64Base_SUBXUXSX_imm3AsInt32:$imm3),
        (SUBXUXSX S:$rn, X:$rm, AArch64Base_SUBXUXSX_imm3AsInt32:$imm3)>;

def : Pat<(int_processorNameValue_SUBXUXTB S:$rn, X:$rm),
        (SUBXUXTB S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXUXTH S:$rn, X:$rm),
        (SUBXUXTH S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXUXTW S:$rn, X:$rm),
        (SUBXUXTW S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_SUBXUXTX S:$rn, X:$rm),
        (SUBXUXTX S:$rn, X:$rm)>;

def : Pat<(int_processorNameValue_UBFMW W:$rn, AArch64Base_UBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_UBFMW_rightWSizeAsInt32:$rightWSize),
        (UBFMW W:$rn, AArch64Base_UBFMW_leftWSizeAsInt32:$leftWSize, AArch64Base_UBFMW_rightWSizeAsInt32:$rightWSize)>;

def : Pat<(int_processorNameValue_UBFMX X:$rn, AArch64Base_UBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_UBFMX_rightXSizeAsInt32:$rightXSize),
        (UBFMX X:$rn, AArch64Base_UBFMX_leftXSizeAsInt32:$leftXSize, AArch64Base_UBFMX_rightXSizeAsInt32:$rightXSize)>;

def : Pat<(int_processorNameValue_UDIVW W:$rm, W:$rn),
        (UDIVW W:$rm, W:$rn)>;

def : Pat<(int_processorNameValue_UDIVX X:$rm, X:$rn),
        (UDIVX X:$rm, X:$rn)>;

def : Pat<(int_processorNameValue_UMADDL X:$ra, W:$rn, W:$rm),
        (UMADDL X:$ra, W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_UMSUBL X:$ra, W:$rn, W:$rm),
        (UMSUBL X:$ra, W:$rn, W:$rm)>;

def : Pat<(int_processorNameValue_UMULH X:$rn, X:$rm),
        (UMULH X:$rn, X:$rm)>;

